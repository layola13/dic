incatenation:/ɪnˌkætəˈneɪʃn/| n.|串联，连锁，连接|The act of linking things together in a series or chain.  The concatenation of circumstances led to his downfall.  一系列事件的连锁反应导致了他的垮台。|近义词：linking, chaining, joining, connection|反义词:separation, division|noun

USAGE->

一般现在时 (Simple Present):
The program performs string concatenation. =  这个程序执行字符串连接。
Incatenation of data creates a longer sequence. = 数据的串联创建一个更长的序列。

现在进行时 (Present Continuous):
The system is performing the concatenation of the files. = 系统正在执行文件的串联。
He is concatenating the strings in his code. = 他正在他的代码中连接字符串。

现在完成时 (Present Perfect):
The software has concatenated the two datasets. = 该软件已经连接了两个数据集。
We have performed the concatenation successfully. = 我们已经成功地执行了串联操作。

现在完成进行时 (Present Perfect Continuous):
The program has been concatenating data for hours. = 程序已经连接数据好几个小时了。
They have been working on the concatenation process all day. = 他们一整天都在进行串联过程。

一般过去时 (Simple Past):
The command concatenated the files. = 该命令连接了文件。
She concatenated the strings to create a new variable. = 她连接了字符串以创建一个新的变量。

过去进行时 (Past Continuous):
The system was concatenating the data when the power went out. =  断电时系统正在连接数据。
He was concatenating strings when he found the error. = 他在连接字符串时发现了错误。

过去完成时 (Past Perfect):
He had concatenated the files before running the analysis. = 他在运行分析之前已经连接了文件。
She had already concatenated the strings before realizing her mistake. = 在意识到自己的错误之前，她已经连接了字符串。

过去完成进行时 (Past Perfect Continuous):
The program had been concatenating data for hours before it crashed. = 程序崩溃前已经连接数据好几个小时了。
They had been working on the concatenation of the databases all week. = 他们整个星期都在进行数据库的串联工作。

一般将来时 (Simple Future):
The script will concatenate the log files. = 脚本将连接日志文件。
We will concatenate the data from multiple sources. = 我们将连接来自多个来源的数据。

将来进行时 (Future Continuous):
The system will be concatenating the files overnight. = 系统将在一夜之间连接文件。
Tomorrow, they will be concatenating the data streams. = 明天，他们将连接数据流。

将来完成时 (Future Perfect):
By tomorrow, the system will have concatenated all the data. = 到明天，系统将连接所有数据。
She will have concatenated the strings by the time you arrive. = 你到达时，她将已经连接了字符串。

将来完成进行时 (Future Perfect Continuous):
By next week, the program will have been concatenating data for a month. = 到下周，该程序将已经连接数据一个月了。
They will have been working on the concatenation project for six months by the end of the year. = 到年底，他们将已经从事这个串联项目六个月了。

虚拟语气:
If I were to concatenate these strings, the result would be too long. = 如果我要连接这些字符串，结果会太长。
I wish I had concatenated the files differently. = 我希望我以不同的方式连接了文件。

被动语态:
The strings were concatenated by the program. = 字符串被程序连接起来了。
The data is being concatenated as we speak. = 数据正在被连接。

疑问句:
How do you concatenate strings in this language? = 在这种语言中如何连接字符串？
What happens when you concatenate these two lists? = 当你连接这两个列表时会发生什么？

否定句:
The function does not concatenate numbers and strings directly. = 该函数不直接连接数字和字符串。
We did not concatenate the files because they were incompatible. = 我们没有连接文件，因为它们不兼容。


PHRASE->
string concatenation = 字符串连接
file concatenation = 文件连接
data concatenation = 数据连接
concatenate and sort = 连接并排序
concatenate strings = 连接字符串
