incremented: /ˈɪŋkrəˌmɛntɪd/ | v. |增加的，递增的|Increased by a specific amount or step.  Meaning something has been made larger by a fixed amount.| The counter incremented each time a new customer entered the store.  每次有新顾客进入商店时，计数器都会递增。| synonyms: increased, advanced, augmented | antonyms: decreased, reduced, diminished | Verb (past participle)

increment: /ˈɪŋkrəmənt/ | n. |增量，递增|An increase by a specific amount or step.|  The annual salary increment was 3%. 年薪增额为 3%。| synonyms: increase, rise, gain | antonyms: decrease, reduction, loss | Noun

increment: /ˈɪŋkrəmənt/ | v. |使增加，使递增|To increase by a specific amount or step.| The program increments the counter by one each time a button is pressed.  每次按下按钮，程序都会将计数器加一。| synonyms: increase, advance, augment | antonyms: decrease, reduce, diminish | Verb



USAGE->

一般现在时 (Simple Present):
The program increments the counter every second.  程序每秒递增计数器。
The population increments steadily each year.  人口每年稳定增长。

现在进行时 (Present Continuous):
The system is incrementing the value as we speak. 系统正在增加值。
The software is incrementing the file size.  该软件正在增加文件大小。

现在完成时 (Present Perfect):
The variable has incremented several times during the process. 在此过程中，变量已增加多次。
The program has incremented the counter to 100.  程序已将计数器递增到 100。

现在完成进行时 (Present Perfect Continuous):
The system has been incrementing the load steadily.  系统一直在稳定地增加负载。
The server has been incrementing the number of connections.  服务器一直在增加连接数。

一般过去时 (Simple Past):
The counter incremented each time a new user logged in. 每次新用户登录时，计数器都会递增。
The program incremented the value by five.  程序将值增加了 5。

过去进行时 (Past Continuous):
The system was incrementing the data while the user was offline.  用户离线时，系统正在递增数据。
The program was incrementing the counter when the error occurred.  发生错误时，程序正在递增计数器。

过去完成时 (Past Perfect):
The counter had incremented to 100 before the system crashed.  系统崩溃前，计数器已递增到 100。
The program had incremented the variable several times before the output was displayed.  在显示输出之前，程序已将变量递增了多次。

过去完成进行时 (Past Perfect Continuous):
The system had been incrementing the load for hours before it reached capacity.  系统在达到容量之前已经增加了几个小时的负载。
The program had been incrementing the counter smoothly until the power outage.  在断电之前，程序一直在平稳地递增计数器。

一般将来时 (Simple Future):
The system will increment the value automatically.  系统将自动增加值。
The program will increment the counter when the button is pressed.  按下按钮时，程序将递增计数器。

将来进行时 (Future Continuous):
The system will be incrementing the load during peak hours.  系统将在高峰时段增加负载。
The program will be incrementing the counter while the data is being processed.  数据处理时，程序将递增计数器。

将来完成时 (Future Perfect):
By the end of the year, the program will have incremented the counter thousands of times.  到年底，程序将把计数器增加数千次。
The system will have incremented the value significantly by next month. 到下个月，系统将显著增加该值。

将来完成进行时 (Future Perfect Continuous):
By next year, the system will have been incrementing the load continuously for a decade.  到明年，系统将连续十年不断增加负载。
The program will have been incrementing the counter steadily for a week by the time the experiment concludes.  到实验结束时，程序将稳定地递增计数器一周。



PHRASE->
increment by = 以...增量递增
increment value = 增量值
automatic increment = 自动递增
incremented counter = 递增计数器
increment operator =  递增运算符


