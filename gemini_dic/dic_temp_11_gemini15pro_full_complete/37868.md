compiler:/kəmˈpaɪlər/ (US) /kəmˈpaɪlə(r)/ (UK)| n.|编译器|A program that translates code written in a high-level programming language into machine-readable code that a computer can execute.|The compiler detected several errors in the code.  编译器在代码中检测到几个错误。|assembler, translator|interpreter|noun

USAGE->

USAGE->
一般现在时 (Simple Present):
The compiler translates source code into machine code. 编译器将源代码翻译成机器码。

The compiler checks for syntax errors. 编译器检查语法错误。

现在进行时 (Present Continuous):
The compiler is processing the code. 编译器正在处理代码。

The compiler is generating an executable file. 编译器正在生成可执行文件。

现在完成时 (Present Perfect):
The compiler has finished compiling the program. 编译器已经完成程序的编译。

The compiler has identified a critical error.  编译器已经识别出一个严重错误。

现在完成进行时 (Present Perfect Continuous):
The compiler has been working on this complex code for hours. 编译器已经在这个复杂的代码上工作了几个小时。

The compiler has been optimizing the code for performance. 编译器一直在优化代码的性能。

一般过去时 (Simple Past):
The compiler reported several warnings during the compilation. 编译在编译期间报告了几个警告。

The compiler generated the assembly code successfully. 编译器成功生成了汇编代码。

过去进行时 (Past Continuous):
The compiler was processing the input file when the power went out.  断电时编译器正在处理输入文件。

The compiler was checking for errors when I interrupted it. 当我打断它时，编译器正在检查错误。

过去完成时 (Past Perfect):
The compiler had already finished compiling the code before I ran the program. 在我运行程序之前，编译器已经完成了代码的编译。

The compiler had detected the error before I noticed it. 在我注意到之前，编译器已经检测到了错误。

过去完成进行时 (Past Perfect Continuous):
The compiler had been working on the code for hours before it finally finished.  编译器一直在处理代码几个小时，直到最终完成。

The compiler had been optimizing the code for performance before I stopped the process. 在我停止该过程之前，编译器一直在优化代码的性能。


一般将来时 (Simple Future):
The new compiler will support more programming languages. 新的编译器将支持更多的编程语言。

The compiler will generate more efficient code. 编译器将生成更高效的代码。


将来进行时 (Future Continuous):
The compiler will be processing the code tomorrow morning.  编译器明天早上将处理代码。

The team will be testing the new compiler next week.  团队下周将测试新的编译器。

将来完成时 (Future Perfect):
By the end of the day, the compiler will have processed all the files.  到今天结束时，编译器将处理完所有文件。

By next year, the compiler will have been updated with new features. 到明年，编译器将更新新功能。

将来完成进行时 (Future Perfect Continuous):
By the time you finish writing the code, the compiler will have been compiling other files for hours.  到你写完代码时，编译器将已经编译其他文件几个小时了。

By the end of the project, the team will have been using the compiler for several months.  到项目结束时，团队将已经使用该编译器几个月了。

虚拟语气:
If I had a faster compiler, the build process would be quicker.  如果我有一个更快的编译器，构建过程会更快。

I wish the compiler would give me more helpful error messages. 我希望编译器能给我更多有用的错误信息。


被动语态:
The code is compiled by the compiler. 代码由编译器编译。

The program was compiled successfully. 程序已成功编译。


疑问句:
What compiler are you using? 你在使用什么编译器？

Which compiler is best for this project? 哪个编译器最适合这个项目？


否定句:
The code doesn't compile without errors. 代码没有错误就无法编译。

The compiler didn't generate the expected output. 编译器没有生成预期的输出。

PHRASE->
just-in-time compiler = 即时编译器
source-to-source compiler = 源到源编译器
cross compiler = 交叉编译器
compiler optimization = 编译器优化
compiler construction = 编译器构造
