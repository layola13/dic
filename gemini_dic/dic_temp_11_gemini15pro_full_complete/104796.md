kba:/ˈkeɪ biː ˈeɪ/ | abbreviation |  千字节数组 | kilobyte array, a unit of computer memory equal to 1024 bytes | The program requires a kba of memory.  该程序需要一个千字节数组的内存。 | 近义词: KB (kilobyte) | 反义词:  (No direct antonym for a unit of measurement) | noun


USAGE->

一般现在时 (Simple Present):
The system uses a kba of memory for each process.  系统为每个进程使用一个千字节数组的内存。

A standard kba holds 1024 bytes of data. 一个标准的千字节数组包含1024字节的数据。


现在进行时 (Present Continuous):
The computer is allocating a kba of memory to the new program.  计算机正在为新程序分配一个千字节数组的内存。

The system is currently using several kbas of memory. 系统当前正在使用几个千字节数组的内存。


现在完成时 (Present Perfect):
The program has used several kbas of memory already.  该程序已经使用了几个千字节数组的内存。

The system has allocated a kba of memory to the process. 系统已经为该进程分配了一个千字节数组的内存。


现在完成进行时 (Present Perfect Continuous):
The system has been using several kbas of memory for hours.  系统已经使用几个千字节数组的内存好几个小时了。

The program has been allocating kbas of memory continuously.  程序一直在连续分配千字节数组的内存。


一般过去时 (Simple Past):
The old computer only had a few kbas of memory.  旧电脑只有几个千字节数组的内存。

The program used a kba of memory for the operation.  程序使用了一个千字节数组的内存进行操作。


过去进行时 (Past Continuous):
The system was allocating kbas of memory when the power went out.  断电时系统正在分配千字节数组的内存。

The program was using several kbas of memory while running.  程序运行时正在使用几个千字节数组的内存。


过去完成时 (Past Perfect):
The program had already used several kbas of memory before it crashed.  程序崩溃前已经使用了几个千字节数组的内存。

The system had allocated a kba of memory to the process before it failed.  系统在失败之前已经为进程分配了一个千字节数组的内存。


过去完成进行时 (Past Perfect Continuous):
The system had been using kbas of memory for hours before the error occurred.  在错误发生之前，系统已经使用千字节数组的内存好几个小时了。

The program had been allocating kbas of memory for a while before it stopped.  程序在停止之前已经分配千字节数组的内存一段时间了。


一般将来时 (Simple Future):
The new system will have several kbas of memory available.  新系统将有几个千字节数组的内存可用。

The program will use a kba of memory for the task.  程序将使用一个千字节数组的内存来完成任务。


将来进行时 (Future Continuous):
The system will be allocating kbas of memory during the installation process.  系统将在安装过程中分配千字节数组的内存。

The program will be using several kbas of memory while running. 程序运行时将使用几个千字节数组的内存。


将来完成时 (Future Perfect):
By next year, the system will have upgraded to several kbas of memory.  到明年，系统将升级到几个千字节数组的内存。

The program will have used a kba of memory by the time it finishes.  程序完成时将已经使用了一个千字节数组的内存。


将来完成进行时 (Future Perfect Continuous):
By the end of the year, the system will have been using several kbas of memory for months.  到年底，系统将已经使用几个千字节数组的内存好几个月了。

The program will have been allocating kbas of memory for a while by then.  到那时，程序将已经分配千字节数组的内存一段时间了。


PHRASE->
allocate a kba of memory  分配一个千字节数组的内存
require a kba of memory  需要一个千字节数组的内存
use a kba of memory  使用一个千字节数组的内存
free a kba of memory  释放一个千字节数组的内存
kba of memory available 可用的千字节数组内存
