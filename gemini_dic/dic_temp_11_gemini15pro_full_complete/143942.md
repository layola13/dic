outputted:/ˈaʊtpʊtɪd/| v. (past participle)|输出的|Something that has been produced or delivered by a computer or other device.  It's the past participle of the verb "output".|The data was outputted to a printer. 数据被输出到打印机。|The program outputted the results to a file. 程序将结果输出到一个文件。|近义词：produced, generated, delivered, emitted|反义词：inputted, received|词性:动词(过去分词)

USAGE->

一般现在时 (Simple Present):
The system outputs the data every hour. 系统每小时输出数据。
The printer outputs high-quality images. 这台打印机输出高质量的图像。

现在进行时 (Present Continuous):
The computer is outputting the results now.  计算机现在正在输出结果。
The program is outputting the data to a file. 程序正在将数据输出到一个文件。

现在完成时 (Present Perfect):
The system has outputted the report. 系统已经输出了报告。
The program has outputted the results successfully. 程序已经成功地输出了结果。


现在完成进行时 (Present Perfect Continuous):
The computer has been outputting data all day.  计算机一整天都在输出数据。
The program has been outputting results for hours. 程序已经输出了几个小时的结果。

一般过去时 (Simple Past):
The system outputted the data yesterday. 系统昨天输出了数据。
The printer outputted the document last week. 打印机上周输出了文件。


过去进行时 (Past Continuous):
The computer was outputting the results when the power went out.  断电时，计算机正在输出结果。
The program was outputting data to the server. 程序正在向服务器输出数据。

过去完成时 (Past Perfect):
The system had outputted the data before the error occurred.  错误发生之前，系统已经输出了数据。
The program had outputted the results before it crashed. 程序崩溃前已经输出了结果。


过去完成进行时 (Past Perfect Continuous):
The computer had been outputting data for hours before it crashed.  电脑崩溃前已经输出了几个小时的数据。
The program had been outputting results to the network for days.  程序已经向网络输出了好几天的结果。


一般将来时 (Simple Future):
The system will output the data tomorrow. 系统明天将输出数据。
The printer will output the document next week. 打印机下周将输出文档。


将来进行时 (Future Continuous):
The computer will be outputting the results tomorrow morning.  计算机明天早上将正在输出结果。
The program will be outputting data to the server next week. 程序下周将正在向服务器输出数据。


将来完成时 (Future Perfect):
The system will have outputted the data by next week.  系统下周之前将已经输出数据。
The program will have outputted the results by the end of the month. 程序将在月底前输出结果。


将来完成进行时 (Future Perfect Continuous):
The computer will have been outputting data for a week by then. 到那时，计算机将已经输出数据一周了。
The program will have been outputting results to the network for months by the time the project is complete. 到项目完成时，程序将已经向网络输出结果好几个月了。


虚拟语气:
If the program outputted the correct data, we would be finished by now. 如果程序输出了正确的数据，我们现在就完成了。
I wish the system outputted the information in a different format. 我希望系统以不同的格式输出信息。


被动语态:
The data was outputted to the printer. 数据被输出到打印机。
The results are being outputted to a file. 结果正在被输出到一个文件中。


疑问句:
Was the data outputted correctly? 数据是否正确输出？
What information is being outputted? 正在输出什么信息？


否定句:
The system didn't output the data. 系统没有输出数据。
The results weren't outputted correctly. 结果没有正确输出。


PHRASE->
output to a file = 输出到文件
output to a printer = 输出到打印机
output the results = 输出结果
output the data = 输出数据
output a report = 输出报告

