operand:/ˈɒpərænd/ (UK) /ˈɑːpərand/ (US)| n. |操作数，运算对象|A quantity on which a mathematical operation is performed.  It's the input to an operator.|Example: In the equation 5 + 2 = 7, both 5 and 2 are operands.|近义词：argument, parameter, input|反义词: result, output|词性:noun

USAGE->

USAGE->
一般现在时 (Simple Present):
The computer processes the operands. = 计算机处理操作数。
The operand determines the outcome of the calculation. =  操作数决定了计算的结果。

现在进行时 (Present Continuous):
The system is currently processing the operands. = 系统正在处理操作数。
The programmer is defining the operands for the function. = 程序员正在为函数定义操作数。

现在完成时 (Present Perfect):
The calculator has processed the operands successfully. = 计算器已成功处理操作数。
The system has identified the operands in the equation. = 系统已识别方程式中的操作数。

现在完成进行时 (Present Perfect Continuous):
The computer has been processing the operands for hours. = 计算机已经处理操作数好几个小时了。
The program has been using these operands since it started. = 程序自启动以来就一直在使用这些操作数。

一般过去时 (Simple Past):
The function processed the operands correctly. = 函数正确处理了操作数。
The system identified the operands in the input. = 系统识别了输入中的操作数。


过去进行时 (Past Continuous):
The computer was processing the operands when the power went out. = 断电时，计算机正在处理操作数。
The programmer was defining the operands when he was interrupted. = 程序员在定义操作数时被打断了。


过去完成时 (Past Perfect):
The system had processed the operands before the error occurred. = 在错误发生之前，系统已经处理了操作数。
The programmer had defined the operands before running the program. = 程序员在运行程序之前已经定义了操作数。

过去完成进行时 (Past Perfect Continuous):
The computer had been processing the operands for hours before it crashed. = 计算机在崩溃前已经处理了操作数好几个小时。
The system had been using these operands for a long time before the update. = 在更新之前，系统已经使用这些操作数很长时间了。

一般将来时 (Simple Future):
The program will process the operands in the next step. = 程序将在下一步处理操作数。
The system will identify the operands automatically. = 系统将自动识别操作数。

将来进行时 (Future Continuous):
The computer will be processing the operands during the simulation. = 在模拟过程中，计算机将处理操作数。
The program will be using these operands throughout the calculation. = 在整个计算过程中，程序将使用这些操作数。

将来完成时 (Future Perfect):
By tomorrow, the system will have processed all the operands. = 到明天为止，系统将处理完所有操作数。
The program will have finished using the operands by the end of the week. = 到本周末，程序将完成对操作数的使用。

将来完成进行时 (Future Perfect Continuous):
By next year, the system will have been processing these operands for five years. = 到明年，系统将已经处理这些操作数五年了。
The program will have been using these operands for a decade by the time it's retired. = 到程序退役时，它将已经使用这些操作数十年了。


虚拟语气:
If I knew the operands, I could calculate the result. = 如果我知道操作数，我就能计算结果。
I wish I had access to the operands. = 我希望我能访问操作数。

被动语态:
The operands are processed by the computer. = 操作数由计算机处理。
The operands were identified by the system. = 操作数由系统识别。

疑问句:
What are the operands in this equation? = 这个方程式中的操作数是什么？
How are the operands processed? = 操作数是如何处理的？

否定句:
The function does not require any operands. = 该函数不需要任何操作数。
The system did not recognize the operands. = 系统无法识别操作数。


PHRASE->
binary operands = 二元操作数
logical operands = 逻辑操作数
arithmetic operands = 算术操作数
operand address = 操作数地址
operand type = 操作数类型
