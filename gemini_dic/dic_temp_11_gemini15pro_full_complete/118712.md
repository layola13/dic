/ˈmetədeɪtə/ | n. | 元数据 | Data that describes other data. Metadata summarizes basic information about data, which can make finding and working with particular instances of data easier. |  The photo's metadata included the date and time it was taken, as well as the camera model. 这张照片的元数据包括拍摄日期和时间，以及相机型号。

|  The website uses metadata to categorize and organize its content. 该网站使用元数据对内容进行分类和组织。 | synonym: data information | antonym: N/A | noun


ETYMOLOGY->
From meta- (beyond, after, higher) + data.  "Meta" in this context refers to data about data.


USAGE->

一般现在时 (Simple Present):
Metadata describes the content of a file. 元数据描述文件的内容。
Metadata helps users find relevant information. 元数据帮助用户找到相关信息。

现在进行时 (Present Continuous):
The system is adding metadata to the new files. 系统正在向新文件添加元数据。
They are updating the metadata for the website. 他们正在更新网站的元数据。


现在完成时 (Present Perfect):
The librarian has added metadata to all the new books. 图书管理员已将元数据添加到所有新书中。
We have implemented a new metadata schema. 我们已经实施了一个新的元数据模式。


现在完成进行时 (Present Perfect Continuous):
The team has been working on improving the metadata quality. 团队一直在努力提高元数据质量。
She has been adding metadata to the images all day. 她一整天都在给图片添加元数据。


一般过去时 (Simple Past):
The photographer embedded metadata into the image. 摄影师将元数据嵌入到图像中。
They updated the metadata last week. 他们上周更新了元数据。


过去进行时 (Past Continuous):
I was adding metadata to the files when the power went out. 我在添加元数据到文件时断电了。
He was editing the metadata when he received the phone call. 他在编辑元数据时接到了电话。


过去完成时 (Past Perfect):
The system had already added the metadata before I saved the file. 在我保存文件之前，系统已经添加了元数据。
They had updated the metadata before launching the website. 他们在启动网站之前更新了元数据。


过去完成进行时 (Past Perfect Continuous):
She had been working on the metadata for hours before she took a break.  在她休息之前，她已经处理元数据好几个小时了。
They had been discussing the metadata schema for weeks before reaching a decision. 他们在做出决定之前已经讨论了几个星期的元数据模式。


一般将来时 (Simple Future):
The software will automatically generate metadata for the videos. 该软件将自动为视频生成元数据。
We will update the metadata next month. 我们将在下个月更新元数据。


将来进行时 (Future Continuous):
The team will be working on the metadata next week.  团队下周将处理元数据。
She will be adding metadata to the documents tomorrow. 她明天将向文档添加元数据。


将来完成时 (Future Perfect):
By next year, we will have implemented a new metadata system. 到明年，我们将实施一个新的元数据系统。
By the end of the week, they will have finished updating the metadata. 到本周末，他们将完成元数据的更新。


将来完成进行时 (Future Perfect Continuous):
By next month, we will have been using the new metadata schema for six months. 到下个月，我们将已经使用新的元数据模式六个月了。
By the end of the project, she will have been working on the metadata for two years. 到项目结束时，她将已经处理元数据两年了。


虚拟语气:
If the file had metadata, it would be easier to find. 如果文件有元数据，就更容易找到。
I wish the images had more descriptive metadata. 我希望图像有更具描述性的元数据。


被动语态:
Metadata is added to the files automatically. 元数据会自动添加到文件中。
The metadata was updated last week. 元数据已于上周更新。


疑问句:
What metadata is available for this document?  这份文件有哪些可用的元数据？
Where is the metadata stored? 元数据存储在哪里？


否定句:
The file doesn't contain any metadata. 该文件不包含任何元数据。
The image didn't have any location metadata. 该图像没有任何位置元数据。


PHRASE->
metadata schema 元数据模式
metadata management 元数据管理
metadata standard 元数据标准
embedded metadata 嵌入式元数据
descriptive metadata 描述性元数据
