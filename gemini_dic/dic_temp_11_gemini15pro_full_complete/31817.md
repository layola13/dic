checksums:/ˈtʃɛksʌmz/ | n. | 校验和 | A number produced by a checksum algorithm, used to verify data integrity. It is typically generated before and after data transmission or storage, and the two checksums are compared to ensure no errors have occurred. | The checksums of the downloaded files matched the ones on the website, so I knew they hadn't been corrupted. 下载文件的校验和与网站上的校验和匹配，因此我知道它们没有被损坏。 | synonyms: hash, hash value, digital fingerprint | antonyms: N/A | noun

checksum:/ˈtʃɛksʌm/ | n. | 校验和 | A number produced by a checksum algorithm, used to verify data integrity. |  We use checksums to ensure the integrity of the downloaded files. 我们使用校验和来确保下载文件的完整性。| synonyms: hash, hash value, digital fingerprint | antonyms: N/A | noun



USAGE->

一般现在时 (Simple Present):
The system generates a checksum for every file. 系统为每个文件生成一个校验和。
He checks the checksum before installing the software. 他在安装软件之前检查校验和。

现在进行时 (Present Continuous):
The program is calculating the checksum now. 程序正在计算校验和。
They are verifying the checksums of the downloaded data. 他们正在验证下载数据的校验和。

现在完成时 (Present Perfect):
The software has calculated the checksum successfully.  软件已成功计算校验和。
Have you checked the checksum yet? 你检查过校验和了吗？

现在完成进行时 (Present Perfect Continuous):
The computer has been calculating the checksum for hours. 电脑一直在计算校验和好几个小时了。
They have been verifying checksums all morning. 他们整个上午一直在验证校验和。


一般过去时 (Simple Past):
I checked the checksum yesterday. 我昨天检查了校验和。
The program generated the checksum automatically. 程序自动生成了校验和。


过去进行时 (Past Continuous):
I was checking the checksum when the power went out.  我正在检查校验和时停电了。
He was verifying the checksums of the files. 他正在验证文件的校验和。


过去完成时 (Past Perfect):
I had checked the checksum before installing the program. 在安装程序之前，我已经检查过校验和了。
She had already generated the checksums by the time I arrived. 在我到达之前，她已经生成了校验和。


过去完成进行时 (Past Perfect Continuous):
I had been checking checksums for hours before I found the error. 在我发现错误之前，我已经检查了几个小时的校验和。
They had been verifying the checksums all day. 他们一整天都在验证校验和。


一般将来时 (Simple Future):
I will check the checksum tomorrow. 我明天会检查校验和。
The program will generate the checksum automatically. 程序将自动生成校验和。


将来进行时 (Future Continuous):
I will be checking the checksums this time tomorrow. 明天这个时候我将会在检查校验和。
He will be verifying the integrity of the data using checksums. 他将使用校验和验证数据的完整性。


将来完成时 (Future Perfect):
By next week, I will have checked all the checksums. 到下周，我将检查所有校验和。
The system will have generated all the necessary checksums by then. 到那时，系统将生成所有必要的校验和。


将来完成进行时 (Future Perfect Continuous):
By next month, I will have been checking checksums for a year. 到下个月，我将已经检查校验和一年了。
They will have been working on implementing checksum verification for several months. 他们将已经致力于实施校验和验证好几个月了。


PHRASE->
compare checksums = 比较校验和
generate a checksum = 生成校验和
verify checksums = 验证校验和
checksum algorithm = 校验和算法
calculate a checksum = 计算校验和
