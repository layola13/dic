misencoded:/ˌmɪsɪnˈkoʊdɪd/ | adj. | 编码错误的，错误编码的 | Incorrectly encoded; containing errors in encoding. | The misencoded data caused the program to crash.  编码错误的数据导致程序崩溃。 |  The misencoded file was unusable. 错误编码的文件无法使用。 | synonyms: corrupted, garbled, faulty, flawed | antonyms: correct, accurate, valid | adjective

USAGE->

一般现在时 (Simple Present):
The system sometimes misencodes data. 系统有时会错误地编码数据。
Misencoded characters appear as gibberish. 编码错误的字符显示为乱码。

现在进行时 (Present Continuous):
The software is misencoding the video file.  该软件正在错误地编码视频文件。
The server is misencoding some of the incoming data. 服务器正在错误地编码一些传入的数据。

现在完成时 (Present Perfect):
The program has misencoded several files. 该程序已经错误地编码了几个文件。
The technician has misencoded the settings.  技术人员错误地编码了设置。

现在完成进行时 (Present Perfect Continuous):
The system has been misencoding data all day. 系统一整天都在错误地编码数据。
The device has been misencoding the audio signal for hours. 该设备几个小时以来一直在错误地编码音频信号。

一般过去时 (Simple Past):
The computer misencoded the document. 计算机错误地编码了文档。
The transmission misencoded the message. 传输错误地编码了消息。

过去进行时 (Past Continuous):
The software was misencoding the data when the power went out. 断电时，软件正在错误地编码数据。
The system was misencoding files while it was updating. 系统更新时，正在错误地编码文件。

过去完成时 (Past Perfect):
The system had misencoded the data before we realized the problem. 在我们意识到问题之前，系统已经错误地编码了数据。
The programmer had misencoded the algorithm before testing it. 程序员在测试之前错误地编码了算法。

过去完成进行时 (Past Perfect Continuous):
The software had been misencoding files for weeks before the bug was discovered. 在发现错误之前，软件已经错误地编码文件数周了。
The system had been misencoding data all night before the technician noticed it. 在技术员注意到之前，系统整晚都在错误地编码数据。

一般将来时 (Simple Future):
If we don't fix the bug, the system will misencode the data again. 如果我们不修复这个错误，系统将再次错误地编码数据。
The program will misencode the file if the settings are incorrect. 如果设置不正确，程序将错误地编码文件。


将来进行时 (Future Continuous):
The system will be misencoding data if we don't intervene. 如果我们不干预，系统将会错误地编码数据。
The software will be misencoding files while the server is down. 服务器关闭时，软件将会错误地编码文件。

将来完成时 (Future Perfect):
By the time we discover the error, the system will have misencoded a lot of data. 当我们发现错误时，系统将已经错误地编码了大量数据。
The program will have misencoded several files by the end of the day. 到今天结束时，程序将已经错误地编码了几个文件。

将来完成进行时 (Future Perfect Continuous):
The system will have been misencoding data for days if we don't fix it soon. 如果我们不尽快修复它，系统将已经错误地编码数据数天。
The software will have been misencoding files for hours by the time we find the cause. 当我们找到原因时，软件将已经错误地编码文件数小时。


虚拟语气:
If the data were misencoded, the system would crash. 如果数据被错误编码，系统将会崩溃。
If the file had been misencoded, we would have noticed the error. 如果文件被错误编码，我们就会注意到这个错误。

被动语态:
The data was misencoded by the faulty software. 数据被错误的软件错误地编码了。
The file is being misencoded by the system. 文件正在被系统错误地编码。

疑问句:
Was the data misencoded? 数据是否被错误编码？
Why is the file being misencoded? 为什么文件被错误编码？

否定句:
The data was not misencoded. 数据没有被错误编码。
The file is not being misencoded. 文件没有被错误编码。



PHRASE->
misencoded data 编码错误的数据
misencoded characters 编码错误的字符
misencoded file 编码错误的文件
misencoded information 编码错误的信息
misencoded message 编码错误的消息
