/ˈæski/ |adj. |ASCII码的|  Relating to or denoting the American Standard Code for Information Interchange. | This server only accepts ASCII characters.  这个服务器只接受ASCII字符。| synonym: text-based | antonym: binary | adjective

/ˈæski/ |n. |ASCII码| American Standard Code for Information Interchange, a character encoding standard for electronic communication. | The file was saved in ASCII format. 该文件以ASCII格式保存。| synonym: character set | antonym: Unicode | noun


USAGE->

一般现在时 (Simple Present):
Does this program support ASCII input?  这个程序支持ASCII输入吗？
Many older systems still rely on ASCII. 许多旧系统仍然依赖ASCII。


现在进行时 (Present Continuous):
The system is converting the file to ASCII. 系统正在将文件转换为ASCII。
We are currently using ASCII for data transmission. 我们目前正在使用ASCII进行数据传输。


现在完成时 (Present Perfect):
We have switched to using ASCII for compatibility. 为了兼容性，我们已经切换到使用ASCII。
The program has processed the ASCII data successfully.  程序已成功处理ASCII数据。


现在完成进行时 (Present Perfect Continuous):
They have been working with ASCII files for years.  他们使用ASCII文件已经很多年了。
The system has been transmitting ASCII data continuously. 系统一直在连续传输ASCII数据。


一般过去时 (Simple Past):
The original message was encoded in ASCII. 原始消息以ASCII编码。
We used ASCII for communication in the early days of the internet. 在互联网早期，我们使用ASCII进行通信。


过去进行时 (Past Continuous):
We were using ASCII when the error occurred.  发生错误时我们正在使用ASCII。
They were converting the file to ASCII format at the time. 他们当时正在将文件转换为ASCII格式。


过去完成时 (Past Perfect):
Before Unicode, we had used ASCII extensively. 在Unicode之前，我们广泛使用过ASCII。
They had already converted the file to ASCII before sending it. 他们在发送之前已经将文件转换成了ASCII格式。


过去完成进行时 (Past Perfect Continuous):
We had been using ASCII for years before switching to Unicode.  在切换到Unicode之前，我们已经使用ASCII很多年了。
They had been working with ASCII data for a long time before encountering this problem.  在遇到这个问题之前，他们已经使用ASCII数据很长时间了。


一般将来时 (Simple Future):
We will use ASCII for the basic communication protocol. 我们将使用ASCII作为基本通信协议。
The system will convert the data to ASCII automatically. 系统将自动将数据转换为ASCII。


将来进行时 (Future Continuous):
We will be using ASCII for data transfer next week.  下周我们将使用ASCII进行数据传输。
The system will be processing ASCII data during the test. 系统将在测试期间处理ASCII数据。


将来完成时 (Future Perfect):
By the end of the year, we will have converted all files to ASCII. 到今年年底，我们将把所有文件转换为ASCII。
The system will have finished processing the ASCII data by tomorrow.  到明天，系统将完成ASCII数据的处理。


将来完成进行时 (Future Perfect Continuous):
By next year, we will have been using ASCII for over 20 years. 到明年，我们将使用ASCII超过20年。
The system will have been transmitting ASCII data continuously for 72 hours by the time we finish the test. 到我们完成测试时，系统将连续传输ASCII数据72小时。



虚拟语气:
If the system supported ASCII, it would be much simpler. 如果系统支持ASCII，那就简单多了。
I wish all systems still used ASCII. 我希望所有系统仍然使用ASCII。


被动语态:
The data was encoded in ASCII. 数据以ASCII编码。
ASCII is still used in many legacy systems.  ASCII仍然在许多遗留系统中使用。


疑问句:
Is the file in ASCII format?  该文件是ASCII格式吗？
What are the limitations of using ASCII? 使用ASCII的局限性是什么？


否定句:
The file is not in ASCII format. 该文件不是ASCII格式。
We don't use ASCII anymore. 我们不再使用ASCII了。



PHRASE->
convert to ASCII  转换为ASCII
ASCII character  ASCII字符
ASCII code ASCII码
ASCII file ASCII文件
ASCII art  ASCII艺术
