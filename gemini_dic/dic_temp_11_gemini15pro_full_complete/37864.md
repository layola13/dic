compiler:/kəmˈpaɪlər/ | n. | 编译器 | A program that translates source code written in a high-level language (like Java or C++) into a low-level language (like assembly language or machine code) that a computer can understand and execute. | The compiler detected an error in the code. 编译器检测到代码中有一个错误。 | assembler, translator | interpreter | noun

USAGE->

一般现在时 (Simple Present):
The compiler checks for syntax errors.  编译器检查语法错误。
This compiler supports multiple programming languages.  这个编译器支持多种编程语言。

现在进行时 (Present Continuous):
The compiler is processing the source code. 编译器正在处理源代码。
He is using a new compiler for his project. 他正在为他的项目使用一个新的编译器。

现在完成时 (Present Perfect):
The compiler has finished compiling the code. 编译器已经完成代码编译。
She has used several different compilers over the years.  这些年来她使用过几种不同的编译器。

现在完成进行时 (Present Perfect Continuous):
The compiler has been running for hours. 编译器已经运行了几个小时。
They have been working on improving the compiler's performance.  他们一直在致力于改进编译器的性能。

一般过去时 (Simple Past):
The compiler detected an error and stopped. 编译器检测到一个错误并停止了。
He used a different compiler for that project. 他为那个项目使用了不同的编译器。

过去进行时 (Past Continuous):
The compiler was processing the code when the power went out.  断电时编译器正在处理代码。
She was learning how to use the new compiler. 她正在学习如何使用新的编译器。

过去完成时 (Past Perfect):
The compiler had finished compiling the code before the deadline.  编译器在截止日期前完成了代码编译。
He had never used that compiler before. 他以前从未使用过那个编译器。

过去完成进行时 (Past Perfect Continuous):
The compiler had been running for hours before it finally finished. 编译器运行了几个小时才最终完成。
They had been trying to fix the compiler bug for days.  他们一直试图修复编译器错误好几天了。

一般将来时 (Simple Future):
The new compiler will improve performance significantly.  新的编译器将显著提高性能。
She will use the latest version of the compiler. 她将使用最新版本的编译器。

将来进行时 (Future Continuous):
The compiler will be processing the code overnight. 编译器将通宵处理代码。
He will be testing the new compiler next week. 他下周将测试新的编译器。

将来完成时 (Future Perfect):
By next week, the compiler will have processed all the code.  到下周，编译器将处理完所有代码。
She will have finished installing the new compiler by tomorrow.  到明天她将完成新编译器的安装。

将来完成进行时 (Future Perfect Continuous):
By the end of the month, they will have been working on the compiler for a year.  到月底，他们将已经开发这个编译器一年了。
He will have been using the same compiler for five years by next summer.  到明年夏天，他将已经使用同一个编译器五年了。


PHRASE->
compiler optimization 编译器优化
just-in-time compiler 即时编译器
compiler construction 编译器构造
compiler error  编译器错误
cross compiler 交叉编译器
