microinstructions:/ˌmaɪkroʊɪnˈstrʌkʃənz/ | n. |微指令 |A very small, low-level instruction used in the control unit of a central processing unit (CPU) to specify elementary operations that can be combined to form machine instructions. | Microinstructions control the flow of data within the CPU.  微指令控制CPU内部的数据流。| Microinstructions are fundamental to the operation of the processor. 微指令是处理器运行的基础。 | near synonyms: microcode, micro-ops | antonym: macroinstruction | noun

microinstruction:/ˌmaɪkroʊɪnˈstrʌkʃən/ | n. |微指令 |A very small, low-level instruction used in the control unit of a central processing unit (CPU) to specify elementary operations that can be combined to form machine instructions. |  The execution of a single machine instruction often involves several microinstructions. 执行一条机器指令通常需要多条微指令。 | Each microinstruction performs a very specific task within the CPU. 每条微指令都在CPU内执行一项非常具体的任务。 | near synonyms: microcode, micro-ops | antonym: macroinstruction | noun



USAGE->

一般现在时 (Simple Present):
The CPU uses microinstructions to execute complex operations.  CPU使用微指令来执行复杂的操作。
A microinstruction specifies a basic operation within the CPU. 微指令指定了CPU内的基本操作。

现在进行时 (Present Continuous):
The control unit is currently fetching microinstructions. 控制单元正在获取微指令。
The CPU is executing a sequence of microinstructions. CPU正在执行一系列微指令。

现在完成时 (Present Perfect):
The system has executed thousands of microinstructions. 系统已经执行了数千条微指令。
The programmer has optimized the microcode for better performance. 程序员已经优化了微代码以获得更好的性能。

现在完成进行时 (Present Perfect Continuous):
The CPU has been fetching microinstructions continuously. CPU一直在连续地获取微指令。
The system has been executing microcode for several hours. 系统已经执行微代码几个小时了。


一般过去时 (Simple Past):
The CPU fetched the microinstruction from control memory. CPU从控制存储器中获取了微指令。
The microinstruction executed successfully. 微指令成功执行。

过去进行时 (Past Continuous):
The control unit was fetching microinstructions when the error occurred.  错误发生时，控制单元正在获取微指令。
The CPU was executing a microinstruction sequence. CPU正在执行微指令序列。

过去完成时 (Past Perfect):
The CPU had already fetched the microinstruction before the interrupt.  在中断之前，CPU已经获取了微指令。
The system had executed the microcode multiple times. 系统已经多次执行了微代码。

过去完成进行时 (Past Perfect Continuous):
The CPU had been fetching microinstructions for several cycles. CPU已经获取微指令多个周期了。
The system had been executing microcode for hours before the failure.  在故障发生前，系统已经执行了数小时的微代码。


一般将来时 (Simple Future):
The CPU will fetch the next microinstruction. CPU将获取下一条微指令。
The microinstruction will control the data flow. 微指令将控制数据流。


将来进行时 (Future Continuous):
The CPU will be executing microinstructions during that time.  在那段时间内，CPU将执行微指令。
The control unit will be fetching microinstructions from memory. 控制单元将从内存中获取微指令。

将来完成时 (Future Perfect):
By then, the CPU will have executed all the microinstructions. 到那时，CPU将已经执行了所有微指令。
The system will have completed the microcode execution.  系统将已经完成了微代码的执行。


将来完成进行时 (Future Perfect Continuous):
The CPU will have been fetching microinstructions for a long time. CPU将已经获取微指令很长时间了。
The system will have been executing microcode for days by the end of the test. 到测试结束时，系统将已经执行了数天的微代码。


PHRASE->
microinstruction sequence  微指令序列
microinstruction format 微指令格式
execute a microinstruction 执行一条微指令
fetch a microinstruction 获取一条微指令
microinstruction set 微指令集
