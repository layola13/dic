deallocates:/diːˈæləkeɪts/| v.|释放，解除分配|To release (something, especially computer memory) from allocation.|The system deallocates the memory when the program finishes.|近义词:releases, frees|反义词:allocates, reserves|verb

USAGE->

一般现在时 (Simple Present):
The operating system deallocates memory when a program finishes. = 当程序结束时，操作系统会释放内存。
This function deallocates the resources used by the object. = 这个函数释放对象使用的资源。

现在进行时 (Present Continuous):
The system is deallocating the memory assigned to that process. = 系统正在释放分配给那个进程的内存。
The program is deallocating unused resources as we speak. = 程序正在释放我们正在谈论的未使用的资源。

现在完成时 (Present Perfect):
The program has deallocated the memory it was using. = 程序已经释放了它正在使用的内存。
The system has deallocated all the temporary files. = 系统已释放所有临时文件。

现在完成进行时 (Present Perfect Continuous):
The system has been deallocating memory gradually throughout the process. = 在整个过程中，系统一直在逐步释放内存。
It has been deallocating resources for the last hour. =  它在过去的一个小时里一直在释放资源。

一般过去时 (Simple Past):
The program deallocated the memory after it finished running. = 程序运行结束后释放了内存。
The system deallocated the resources when they were no longer needed. = 当不再需要资源时，系统释放了它们。

过去进行时 (Past Continuous):
The system was deallocating memory when the power went out. = 断电时，系统正在释放内存。
The program was deallocating resources while it was closing. = 程序在关闭时正在释放资源。

过去完成时 (Past Perfect):
The program had deallocated the memory before it crashed. = 程序崩溃前已经释放了内存。
The system had deallocated the unnecessary resources before the new task started. =  在新任务开始之前，系统已经释放了不必要的资源。

过去完成进行时 (Past Perfect Continuous):
The system had been deallocating memory for several minutes before the error occurred. = 在错误发生之前，系统已经释放内存几分钟了。
The program had been deallocating resources for a while before it finally closed. = 程序在最终关闭之前已经释放资源一段时间了。


一般将来时 (Simple Future):
The system will deallocate the memory after the task is complete. = 任务完成后，系统将释放内存。
The program will deallocate the resources when it shuts down. = 程序关闭时将释放资源。

将来进行时 (Future Continuous):
The system will be deallocating memory during the shutdown process. = 系统将在关机过程中释放内存。
The program will be deallocating resources while it's updating. = 程序将在更新时释放资源。

将来完成时 (Future Perfect):
By the time the process finishes, the system will have deallocated all the associated memory. = 到进程完成时，系统将已经释放所有关联的内存。
The program will have deallocated all temporary files by the time it closes. = 程序关闭时将已经释放所有临时文件。

将来完成进行时 (Future Perfect Continuous):
The system will have been deallocating memory for hours by the time the simulation is complete. = 到模拟完成时，系统将已经释放内存数小时。
The program will have been deallocating resources throughout the entire operation. = 在整个操作过程中，程序将一直在释放资源。


虚拟语气:
If the program were to finish, it would deallocate the memory. = 如果程序结束，它将释放内存。
If the system had deallocated the memory, the error would not have occurred. = 如果系统释放了内存，就不会发生错误。


被动语态:
The memory is deallocated by the operating system. = 内存由操作系统释放。
Resources are deallocated when they are no longer needed. = 当不再需要资源时，资源将被释放。


疑问句:
Does the system deallocate the memory automatically? = 系统是否自动释放内存？
When are the resources deallocated? = 资源何时被释放？


否定句:
The program does not deallocate the memory immediately. = 程序不会立即释放内存。
The system did not deallocate the resources properly. = 系统没有正确释放资源。



PHRASE->
automatically deallocates = 自动释放
manually deallocates = 手动释放
deallocates memory = 释放内存
deallocates resources = 释放资源
deallocates objects = 释放对象
