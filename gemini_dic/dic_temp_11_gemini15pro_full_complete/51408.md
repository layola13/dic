deterministically:/dɪˌtɜːrmɪˈnɪstɪkli/ | adv. | 确定地；必然地 | In a way that is certain or inevitable, according to fixed rules or laws. | The computer program deterministically generates the same output for a given input.  这个计算机程序根据给定的输入确定性地生成相同的输出。| deterministically chooses the next action. 程序确定性地选择下一个动作。 |  Near Synonyms: predictably, invariably, systematically, automatically, necessarily | Near Antonyms: randomly, stochastically, probabilistically | Adverb

USAGE->

一般现在时 (Simple Present):
The algorithm deterministically calculates the result.  该算法确定性地计算结果。
The system deterministically responds to user input. 系统确定性地响应用户输入。


现在进行时 (Present Continuous):
The program is deterministically processing the data. 程序正在确定性地处理数据。
The machine is deterministically generating a sequence of numbers. 机器正在确定性地生成一串数字。


现在完成时 (Present Perfect):
The simulation has deterministically evolved over time. 仿真已经随着时间的推移确定性地演化。
The process has deterministically produced the expected outcome. 该过程确定性地产生了预期的结果。


现在完成进行时 (Present Perfect Continuous):
The computer has been deterministically running the same program for hours. 计算机一直在确定性地运行相同的程序几个小时了。
The system has been deterministically calculating the trajectory of the rocket.  系统一直在确定性地计算火箭的轨迹。


一般过去时 (Simple Past):
The experiment deterministically confirmed the hypothesis. 实验确定性地证实了这个假设。
The function deterministically returned the correct value. 函数确定性地返回了正确的值。


过去进行时 (Past Continuous):
The system was deterministically executing the instructions. 系统当时正在确定性地执行指令。
The model was deterministically predicting the future state. 模型当时正在确定性地预测未来状态。


过去完成时 (Past Perfect):
The program had deterministically generated the output before the error occurred.  在错误发生之前，程序已经确定性地生成了输出。
The simulation had deterministically reached a stable state. 仿真已经确定性地达到了稳定状态。


过去完成进行时 (Past Perfect Continuous):
The computer had been deterministically running the simulation for days before it crashed.  计算机在崩溃之前已经确定性地运行了仿真好几天。
The researchers had been deterministically studying the phenomenon for years. 研究人员多年来一直在确定性地研究这一现象。


一般将来时 (Simple Future):
The algorithm will deterministically solve the problem. 该算法将确定性地解决这个问题。
The system will deterministically react to changes in the environment. 系统将确定性地对环境变化做出反应。


将来进行时 (Future Continuous):
The computer will be deterministically processing the data at that time.  届时计算机将确定性地处理数据。
The machine will be deterministically generating a sequence of random numbers. 机器届时将确定性地生成一系列随机数。


将来完成时 (Future Perfect):
By next week, the program will have deterministically generated the final results. 到下周，程序将确定性地生成最终结果。
By the end of the year, the system will have deterministically analyzed all the data. 到年底，系统将确定性地分析所有数据。


将来完成进行时 (Future Perfect Continuous):
By next month, the computer will have been deterministically running the simulation for a full year. 到下个月，计算机将已经确定性地运行仿真整整一年了。
By the time the project is complete, the team will have been deterministically studying the problem for five years. 到项目完成时，团队将已经确定性地研究这个问题五年了。


PHRASE->
operate deterministically  确定性地运作
behave deterministically 确定性地运行
deterministically computed 确定性地计算
deterministically defined 确定性地定义
deterministically generated  确定性地生成
