iterates:/ˈɪtəreɪts/ | v. | 迭代，重复 | repeats a process or set of instructions. | The program iterates through the list of names. 程序迭代遍历姓名列表。|  The artist iterated the same pattern throughout the design.  这位艺术家在整个设计中重复相同的图案。 | synonyms: repeats, loops, cycles | antonyms: halts, stops | verb

iterate:/ˈɪtəreɪt/ | n. | 迭代，重复 | the result of iterating; something that has been repeated. |  Each iterate of the design improved on the last. 每个设计的迭代都比上一个有所改进。| The latest iterate of the software includes several new features.  最新版本的软件包含几个新功能。 | synonyms: repetition, version, iteration | antonyms: original, first | noun

USAGE->

一般现在时 (Simple Present):
The system iterates through the data every hour. 系统每小时迭代一次数据。
The function iterates over the array. 函数迭代数组。


现在进行时 (Present Continuous):
The program is iterating through a large dataset. 程序正在迭代一个大型数据集。
The algorithm is iterating towards a solution. 该算法正在迭代以寻求解决方案。


现在完成时 (Present Perfect):
The process has iterated several times. 该过程已经迭代了几次。
The loop has iterated until a condition was met. 循环已经迭代，直到满足某个条件。


现在完成进行时 (Present Perfect Continuous):
The software has been iterating through the data for hours. 软件已经迭代数据好几个小时了。
The team has been iterating on the design for weeks. 团队已经迭代设计好几周了。


一般过去时 (Simple Past):
The program iterated through the list and found the match. 程序迭代了列表并找到了匹配项。
The function iterated over the array until it found the value. 函数迭代数组直到找到该值。


过去进行时 (Past Continuous):
The system was iterating through the logs when the error occurred. 系统正在迭代日志时发生错误。
The algorithm was iterating towards a solution when it ran out of memory.  算法正在迭代以寻求解决方案时内存不足。


过去完成时 (Past Perfect):
The program had iterated through the data before the crash. 程序在崩溃之前已经迭代了数据。
The loop had iterated several times before it terminated. 循环在终止之前已经迭代了几次。


过去完成进行时 (Past Perfect Continuous):
The software had been iterating through the data for hours before it finished. 软件在完成之前已经迭代数据好几个小时了。
The team had been iterating on the design for weeks before they reached a consensus. 团队在达成共识之前已经迭代设计好几周了。


一般将来时 (Simple Future):
The program will iterate through the list and print the results. 程序将迭代列表并打印结果。
The function will iterate over the array to calculate the sum. 函数将迭代数组来计算总和。


将来进行时 (Future Continuous):
The system will be iterating through the logs tomorrow. 系统明天将迭代日志。
The algorithm will be iterating towards a solution overnight. 该算法将在夜间迭代以寻求解决方案。


将来完成时 (Future Perfect):
By next week, the team will have iterated on the design several times. 到下周，团队将已经迭代设计几次。
By the end of the month, the software will have iterated through all the data. 到月底，软件将已经迭代所有数据。


将来完成进行时 (Future Perfect Continuous):
By next year, the team will have been iterating on the project for two years. 到明年，团队将已经迭代该项目两年了。
By the time the project is finished, the algorithm will have been iterating for months. 到项目完成时，算法将已经迭代了几个月。



PHRASE->
iterate over = 迭代遍历
iterate through = 迭代通过
iterate on (a design/idea) = 迭代（设计/想法）
iterate until (a condition) = 迭代直到（条件）
perform iterations = 执行迭代
