outpath:/ˈaʊtˌpæθ/| n.|输出路径|The location or directory where output files are saved.  It specifies where the results of a process or program should be written.|The program wrote the results to the specified outpath. 程序将结果写入指定的输出路径。|近义词:output directory, output location, destination path|反义词:input path, source path|noun


USAGE->

一般现在时 (Simple Present):
The system always saves the report to the default outpath. 系统总是将报告保存到默认输出路径。
The script checks if the outpath exists. 脚本检查输出路径是否存在。


现在进行时 (Present Continuous):
The program is currently writing the data to the outpath. 程序当前正在将数据写入输出路径。
The system is redirecting the output to a different outpath. 系统正在将输出重定向到不同的输出路径。


现在完成时 (Present Perfect):
The process has successfully written the file to the outpath.  该进程已成功将文件写入输出路径。
The script has created a new outpath for the log files. 脚本已为日志文件创建了新的输出路径。


现在完成进行时 (Present Perfect Continuous):
The program has been writing to the outpath for the last hour. 程序一直在将数据写入输出路径一个小时了。
The system has been storing temporary files in this outpath. 系统一直将临时文件存储在此输出路径中。


一般过去时 (Simple Past):
The program wrote the output to the specified outpath. 程序将输出写入指定的输出路径。
I changed the outpath for the downloaded files. 我更改了下载文件的输出路径。


过去进行时 (Past Continuous):
The system was writing to the outpath when the error occurred.  发生错误时，系统正在写入输出路径。
The program was using a temporary outpath during the installation. 安装期间，程序正在使用临时输出路径。


过去完成时 (Past Perfect):
The program had already written the file to the outpath before the error occurred. 在发生错误之前，程序已经将文件写入输出路径。
I had changed the outpath before running the script. 在运行脚本之前，我已经更改了输出路径。


过去完成进行时 (Past Perfect Continuous):
The program had been writing to the outpath for several hours before the disk became full.  在磁盘变满之前，程序已经向输出路径写入数据数小时了。
The system had been using that outpath for months before we decided to change it.  在我们决定更改之前，系统已经使用了该输出路径好几个月了。


一般将来时 (Simple Future):
The program will write the results to the specified outpath. 程序会将结果写入指定的输出路径。
The system will create a new outpath if it doesn't exist. 如果输出路径不存在，系统将创建一个新的。


将来进行时 (Future Continuous):
The program will be writing to the outpath tomorrow morning. 程序明天早上将写入输出路径。
The system will be using a different outpath during the maintenance period. 在维护期间，系统将使用不同的输出路径。


将来完成时 (Future Perfect):
By tomorrow, the program will have written all the data to the outpath.  到明天，程序将把所有数据写入输出路径。
The system will have created the necessary outpaths by the time the installation begins. 在安装开始之前，系统将创建必要的输出路径。


将来完成进行时 (Future Perfect Continuous):
By next week, the program will have been writing to the outpath for a month. 到下周，程序将已经向输出路径写入数据一个月了。
By the end of the year, the system will have been using this outpath for over six months. 到年底，系统将已经使用此输出路径超过六个月了。


虚拟语气:
If I knew the correct outpath, I would configure the program. 如果我知道正确的输出路径，我会配置程序。
If the outpath were writable, the program would have saved the file. 如果输出路径可写，程序就会保存文件了。


被动语态:
The outpath was specified in the configuration file. 输出路径在配置文件中指定。
The results are being written to the default outpath. 结果正在写入默认输出路径。


疑问句:
What is the outpath for the log files? 日志文件的输出路径是什么？
Where is the outpath located? 输出路径在哪里？


否定句:
The program did not write the file to the specified outpath. 程序没有将文件写入指定的输出路径。
The outpath is not valid. 输出路径无效。


PHRASE->
specify the outpath 指定输出路径
default outpath 默认输出路径
valid outpath  有效输出路径
change the outpath 更改输出路径
create an outpath 创建输出路径
