asciis:/ˈæskiːz/| n. |ASCII 码（复数）|American Standard Code for Information Interchange (ASCII) is a character encoding standard for electronic communication. ASCII codes represent text in computers, telecommunications equipment, and other devices.|The website uses only standard asciis.|近义词：character codes|反义词: (No direct antonym for a technical term like this)|noun


USAGE->

一般现在时 (Simple Present):
ASCII represents characters as numbers. = ASCII 将字符表示为数字。
Many systems still use asciis for basic text. = 许多系统仍然使用 ASCII 码来表示基本文本。

现在进行时 (Present Continuous):
The system is processing asciis to display text. = 系统正在处理 ASCII 码以显示文本。
They are converting the file to standard asciis. = 他们正在将文件转换为标准 ASCII 码。

现在完成时 (Present Perfect):
We have used asciis for decades in computing. = 我们在计算领域已经使用 ASCII 码几十年了。
The industry has standardized asciis for basic communication. = 业界已经将 ASCII 码标准化用于基本通信。

现在完成进行时 (Present Perfect Continuous):
Programmers have been using asciis since the early days of computing. = 程序员从计算的早期就开始使用 ASCII 码。
We have been working with asciis in this project for a while. = 我们在这个项目中已经使用 ASCII 码一段时间了。

一般过去时 (Simple Past):
Early computers used asciis to store and process text. = 早期计算机使用 ASCII 码来存储和处理文本。
They transmitted the message using standard asciis. = 他们使用标准 ASCII 码传输了消息。

过去进行时 (Past Continuous):
We were using asciis when the system crashed. = 系统崩溃时我们正在使用 ASCII 码。
He was studying asciis when he discovered the bug. = 他在研究 ASCII 码时发现了这个错误。

过去完成时 (Past Perfect):
Before Unicode, asciis had been the dominant character encoding. = 在 Unicode 之前，ASCII 码一直是主要的字符编码。
They had already converted the file to asciis before sending it. = 他们在发送之前已经将文件转换为 ASCII 码。

过去完成进行时 (Past Perfect Continuous):
We had been using asciis for years before switching to Unicode. = 在切换到 Unicode 之前，我们已经使用 ASCII 码多年。
They had been working with asciis in that project for months. = 他们一直在那个项目中使用 ASCII 码好几个月了。

一般将来时 (Simple Future):
We will use asciis for the basic text file. = 我们将对基本文本文件使用 ASCII 码。
The system will convert the input to asciis automatically. = 系统将自动将输入转换为 ASCII 码。

将来进行时 (Future Continuous):
We will be using asciis throughout the development process. = 我们将在整个开发过程中使用 ASCII 码。
They will be working with asciis on the new project next week. = 下周他们将在新项目中使用 ASCII 码。

将来完成时 (Future Perfect):
By next year, we will have transitioned fully to using asciis. = 到明年，我们将完全过渡到使用 ASCII 码。
The system will have converted all files to asciis by the deadline. = 系统将在截止日期前将所有文件转换为 ASCII 码。

将来完成进行时 (Future Perfect Continuous):
By the end of the project, we will have been using asciis for over two years. = 到项目结束时，我们将已经使用 ASCII 码超过两年。
They will have been working with asciis on this system for a decade by then. = 到那时，他们将已经在这个系统中使用 ASCII 码十年了。



PHRASE->
extended asciis = 扩展 ASCII 码
standard asciis = 标准 ASCII 码
ASCII character set = ASCII 字符集
ASCII code table = ASCII 码表
convert to asciis = 转换为 ASCII 码
