invokable:/ɪnˈvoʊkəbəl/| adj.|可调用的，可祈求的|Able to be invoked; capable of being called upon.|The function is invokable from any part of the program.  这个函数可以在程序的任何部分调用。|近义词：callable, executable|反义词: non-invokable|词性:形容词
词源: From invoke + -able.  来自 invoke + -able.


USAGE->

一般现在时 (Simple Present):
The system makes the function invokable. 系统使函数可调用。
This method is invokable only from within the class.  此方法只能从类内部调用。

现在进行时 (Present Continuous):
The programmer is making the module invokable. 程序员正在使模块可调用。
They are currently testing if the script is invokable. 他们目前正在测试脚本是否可调用。

现在完成时 (Present Perfect):
The developers have made the API invokable. 开发人员已使 API 可调用。
He has designed the class to be invokable from other modules. 他已将类设计为可从其他模块调用。

现在完成进行时 (Present Perfect Continuous):
We have been working on making this service invokable remotely. 我们一直在努力使这项服务可以远程调用。
They have been trying to figure out why the function isn't invokable.  他们一直试图弄清楚为什么该函数不可调用。

一般过去时 (Simple Past):
The update made several functions invokable. 更新使几个函数可调用。
We tested whether the script was invokable after the changes.  我们测试了更改后脚本是否可调用。

过去进行时 (Past Continuous):
We were checking if the method was invokable when the error occurred. 当错误发生时，我们正在检查该方法是否可调用。
The team was working on making the component invokable from the command line.  团队正在努力使组件可从命令行调用。

过去完成时 (Past Perfect):
After they had made the necessary changes, the function became invokable.  在他们进行了必要的更改之后，该函数变得可调用。
Before the bug fix, the method hadn't been invokable.  在修复错误之前，该方法一直不可调用。


过去完成进行时 (Past Perfect Continuous):
They had been trying to make the library invokable for weeks before they found the solution.  他们在找到解决方案之前几周一直在尝试使库可调用。
We had been debugging the code for hours trying to understand why it wasn't invokable. 我们已经调试代码好几个小时了，试图理解为什么它不可调用。

一般将来时 (Simple Future):
The next release will make all these functions invokable. 下一个版本将使所有这些函数可调用。
The new library will be invokable from multiple platforms. 新库将可从多个平台调用。

将来进行时 (Future Continuous):
Next week, we will be testing if the updated API is invokable. 下周，我们将测试更新后的 API 是否可调用。
The team will be working on making the module invokable remotely.  团队将致力于使模块可远程调用。

将来完成时 (Future Perfect):
By the end of the month, we will have made the entire system invokable. 到月底，我们将使整个系统可调用。
They will have finished making the component invokable by next week.  他们将在下周之前完成使组件可调用的工作。

将来完成进行时 (Future Perfect Continuous):
By the time the project is finished, we will have been working on making this service invokable for over a year.  到项目完成时，我们将已经为使这项服务可调用而工作了一年多。
By next year, the team will have been developing invokable components for several different projects.  到明年，团队将已经为几个不同的项目开发了可调用的组件。


PHRASE->
make something invokable 使某物可调用
ensure something is invokable 确保某物可调用
test if something is invokable 测试某物是否可调用
an invokable function 一个可调用的函数
invokable service 可调用的服务
