asynchronously: /eɪˈsɪŋkrənəsli/ /æˈsɪŋkrənəsli/ | adv. | 异步地 |  In computing, it refers to processes or events that do not occur at the same time or are not coordinated in time.  Generally, it means "not at the same time." | The data was transferred asynchronously. 数据异步传输。|  concurrently, simultaneously, independently | synchronously | adverb

USAGE->

USAGE->
一般现在时 (Simple Present):
The server asynchronously handles client requests.  服务器异步处理客户端请求。
They asynchronously update the database. 他们异步更新数据库。

现在进行时 (Present Continuous):
The system is asynchronously processing the transactions. 系统正在异步处理事务。
We are asynchronously communicating with the remote server. 我们正在与远程服务器异步通信。

现在完成时 (Present Perfect):
The program has asynchronously downloaded the files. 程序已异步下载文件。
The API has asynchronously sent the notification.  API 已异步发送通知。

现在完成进行时 (Present Perfect Continuous):
The application has been asynchronously fetching data for the past hour.  该应用程序过去一小时一直在异步获取数据。
They have been asynchronously collaborating on the project. 他们一直在异步协作该项目。


一般过去时 (Simple Past):
The function asynchronously executed the task. 该函数异步执行了任务。
The system asynchronously logged the events. 系统异步记录了事件。

过去进行时 (Past Continuous):
The server was asynchronously sending responses. 服务器正在异步发送响应。
The application was asynchronously updating its content. 应用程序正在异步更新其内容。

过去完成时 (Past Perfect):
The program had asynchronously completed the operation before the user noticed. 在用户注意到之前，程序已异步完成了操作。
The system had asynchronously processed all the requests. 系统已异步处理了所有请求。


过去完成进行时 (Past Perfect Continuous):
The network had been asynchronously transferring data for several hours. 网络已经异步传输数据数小时了。
The application had been asynchronously backing up the files. 应用程序一直在异步备份文件。


一般将来时 (Simple Future):
The system will asynchronously process the data. 系统将异步处理数据。
The application will asynchronously notify the user. 应用程序将异步通知用户。

将来进行时 (Future Continuous):
The server will be asynchronously handling requests during peak hours.  服务器将在高峰时段异步处理请求。
The system will be asynchronously updating the database overnight. 系统将在夜间异步更新数据库。

将来完成时 (Future Perfect):
By tomorrow, the program will have asynchronously downloaded all the necessary files. 到明天，程序将异步下载所有必要的文件。
By next week, the team will have asynchronously completed the project. 到下周，团队将异步完成该项目。

将来完成进行时 (Future Perfect Continuous):
By the end of the month, the system will have been asynchronously collecting data for a year. 到月底，系统将已异步收集数据一年。
By the time you arrive, the server will have been asynchronously processing the information. 到达时，服务器将已异步处理信息。

虚拟语气:
If the operations were performed asynchronously, it would be more efficient. 如果操作是异步执行的，效率会更高。
I wish the updates were handled asynchronously. 我希望更新是异步处理的。

被动语态:
The data is asynchronously transferred by the system. 数据由系统异步传输。
The tasks are asynchronously executed by the application. 任务由应用程序异步执行。

疑问句:
Is the data being processed asynchronously? 数据是否正在异步处理？
Were the files downloaded asynchronously? 文件是否异步下载？

否定句:
The requests are not processed asynchronously. 请求不是异步处理的。
The data was not transferred asynchronously. 数据不是异步传输的。



PHRASE->
operate asynchronously 异步操作
communicate asynchronously 异步通信
run asynchronously 异步运行
execute asynchronously 异步执行
work asynchronously 异步工作
