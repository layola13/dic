instantiated:/ɪnˈstænʃieɪtɪd/ | v. |  实例化，具体化 |  represent as or by an instance; represent by a concrete example |  The class was instantiated to create a new object.  这个类被实例化以创建一个新的对象。 |  created, exemplified, materialized | abstracted, generalized | verb

USAGE->

一般现在时 (Simple Present):
The program instantiates several objects at startup. 程序在启动时实例化几个对象。
This function instantiates a new database connection. 这个函数实例化一个新的数据库连接。


现在进行时 (Present Continuous):
The system is instantiating a new virtual machine. 系统正在实例化一个新的虚拟机。
The developer is instantiating a test object. 开发人员正在实例化一个测试对象。


现在完成时 (Present Perfect):
The class has been instantiated successfully.  该类已被成功实例化。
The system has instantiated all necessary components. 系统已经实例化了所有必要的组件。


现在完成进行时 (Present Perfect Continuous):
The program has been instantiating objects for the last hour. 程序在过去的一个小时里一直在实例化对象。
The server has been instantiating new user sessions. 服务器一直在实例化新的用户会话。


一般过去时 (Simple Past):
The script instantiated the object without errors. 脚本无错误地实例化了该对象。
We instantiated a new server yesterday. 我们昨天实例化了一个新的服务器。


过去进行时 (Past Continuous):
The system was instantiating a new object when the power went out.  断电时，系统正在实例化一个新对象。
The program was instantiating several processes while I was working. 我工作的时候，程序正在实例化几个进程。


过去完成时 (Past Perfect):
The object had been instantiated before the error occurred.  在错误发生之前，对象已经被实例化了。
The system had already instantiated the required classes.  系统已经实例化了所需的类。


过去完成进行时 (Past Perfect Continuous):
The program had been instantiating objects for hours before it crashed. 程序崩溃前已经实例化对象数小时了。
The server had been instantiating new connections all day. 服务器一整天都在实例化新的连接。


一般将来时 (Simple Future):
The system will instantiate a new object when requested. 系统将在请求时实例化一个新的对象。
The program will instantiate the necessary components on startup. 程序将在启动时实例化必要的组件。


将来进行时 (Future Continuous):
The system will be instantiating new objects throughout the day. 系统将全天都在实例化新的对象。
The program will be instantiating the required classes during the installation process. 程序将在安装过程中实例化所需的类。


将来完成时 (Future Perfect):
By tomorrow, the system will have instantiated all required objects. 到明天，系统将已经实例化所有需要的对象。
The program will have instantiated the necessary components by the time you log in.  当你登录时，程序将已经实例化了必要的组件。


将来完成进行时 (Future Perfect Continuous):
By next week, the system will have been instantiating objects for a month. 到下周，系统将已经实例化对象一个月了。
The program will have been instantiating new user sessions for several hours. 程序将已经实例化新的用户会话几个小时了。



PHRASE->
is instantiated as  被实例化为
instantiated object 实例化对象
instantiated class 实例化类
be instantiated from 从...实例化
get instantiated 被实例化
