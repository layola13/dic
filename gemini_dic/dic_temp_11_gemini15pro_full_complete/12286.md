ascians: /ˈæskiənz/ | n. | ASCII 字符 | Characters that are part of the ASCII standard. |  The website only accepts asciians in the form data.  该网站只接受表单数据中的 ASCII 字符。 | 近义词: ASCII characters | 反义词: Unicode characters | 词性: 名词

词源:  Derived from ASCII (American Standard Code for Information Interchange).  源自 ASCII（美国信息交换标准代码）。


USAGE->

一般现在时 (Simple Present):
The system uses asciians to represent text. 系统使用 ASCII 字符来表示文本。
Many older programs only work with asciians. 许多较旧的程序只能使用 ASCII 字符。

现在进行时 (Present Continuous):
The program is processing asciians from the input file. 程序正在处理来自输入文件的 ASCII 字符。
She is converting the text to asciians. 她正在将文本转换为 ASCII 字符。

现在完成时 (Present Perfect):
We have used asciians for many years. 我们已经使用 ASCII 字符很多年了。
The software has processed all the asciians in the document. 该软件已处理文档中的所有 ASCII 字符。

现在完成进行时 (Present Perfect Continuous):
They have been working with asciians all day. 他们整天都在使用 ASCII 字符。
He has been studying asciians and their limitations. 他一直在研究 ASCII 字符及其局限性。


一般过去时 (Simple Past):
The old computer used asciians for its display. 旧电脑使用 ASCII 字符进行显示。
They transmitted asciians over the network. 他们在网络上传输了 ASCII 字符。

过去进行时 (Past Continuous):
The system was processing asciians when the power went out.  系统正在处理 ASCII 字符时断电了。
She was typing asciians into the terminal. 她正在终端输入 ASCII 字符。


过去完成时 (Past Perfect):
Before Unicode, we had used asciians for text encoding. 在 Unicode 之前，我们使用 ASCII 字符进行文本编码。
They had converted the file to asciians before sending it. 他们在发送文件之前已将其转换为 ASCII 字符。


过去完成进行时 (Past Perfect Continuous):
They had been using asciians for years before adopting Unicode.  在采用 Unicode 之前，他们已经使用 ASCII 字符多年了。
She had been studying asciians and their limitations before the new standard was released.  在新标准发布之前，她一直在研究 ASCII 字符及其局限性。


一般将来时 (Simple Future):
The program will process only asciians. 该程序将仅处理 ASCII 字符。
We will use asciians for the basic text file. 我们将对基本文本文件使用 ASCII 字符。


将来进行时 (Future Continuous):
The system will be processing asciians during the update. 系统将在更新期间处理 ASCII 字符。
They will be using asciians for the communication protocol. 他们将在通信协议中使用 ASCII 字符。


将来完成时 (Future Perfect):
By next year, we will have converted all documents to asciians. 到明年，我们将把所有文档转换为 ASCII 字符。
They will have finished processing the asciians by tomorrow.  到明天他们将完成 ASCII 字符的处理。


将来完成进行时 (Future Perfect Continuous):
By the end of the project, they will have been working with asciians for months.  到项目结束时，他们将已经使用 ASCII 字符几个月了。
She will have been studying asciians for years by the time she graduates.  到她毕业时，她将已经学习 ASCII 字符很多年了。


虚拟语气:
If the system supported Unicode, we wouldn't need to use asciians. 如果系统支持 Unicode，我们就不需要使用 ASCII 字符了。
I wish the file contained only asciians. 我希望该文件只包含 ASCII 字符。


被动语态:
Asciians were used in the early days of computing. 在早期计算中使用了 ASCII 字符。
The text is being converted to asciians by the program. 文本正被程序转换为 ASCII 字符。


疑问句:
Are these characters asciians? 这些字符是 ASCII 字符吗？
How many asciians are in this file?  这个文件中包含多少个 ASCII 字符？


否定句:
The file does not contain only asciians.  该文件不只包含 ASCII 字符。
The program cannot process non-asciians. 程序无法处理非 ASCII 字符。


PHRASE->
convert to asciians = 转换为 ASCII 字符
represent with asciians = 使用 ASCII 字符表示
process asciians = 处理 ASCII 字符
valid asciians = 有效的 ASCII 字符
filter out non-asciians = 过滤掉非 ASCII 字符

