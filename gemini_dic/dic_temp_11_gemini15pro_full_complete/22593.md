bootloader:/ˈbuːtˌləʊdə(r)/ (US) /ˈbuːtˌloʊdər/| n. |引导加载程序，启动加载程序|A small program that loads the operating system or other system software into the main memory or random access memory (RAM) of a computer after power on or general reset.|The bootloader is essential for starting the computer. 引导加载程序对于启动计算机至关重要。|The bootloader initializes the hardware and then loads the operating system. 引导加载程序初始化硬件，然后加载操作系统。|近义词：boot manager, boot program|反义词: N/A|名词

bootloader:/ˈbuːtˌləʊdə(r)/ (US) /ˈbuːtˌloʊdər/| n. |引导程序，启动程序|A program that loads the main operating system or other system software for the computer after power-on self-tests (POST) are complete.|The bootloader is the first software to run when a computer starts. 引导程序是计算机启动时运行的第一个软件。|A damaged bootloader can prevent the computer from starting. 损坏的引导程序会阻止计算机启动。|近义词：boot manager, boot strap loader|反义词: N/A|名词


USAGE->

USAGE->
一般现在时 (Simple Present):
The bootloader loads the operating system.  引导加载程序加载操作系统。
The computer uses a bootloader to start. 计算机使用引导加载程序启动。

现在进行时 (Present Continuous):
The bootloader is loading the operating system now. 引导加载程序现在正在加载操作系统。
The computer is using the bootloader to start up. 计算机正在使用引导加载程序启动。

现在完成时 (Present Perfect):
The bootloader has loaded the operating system successfully. 引导加载程序已成功加载操作系统。
The computer has started using the bootloader.  计算机已使用引导加载程序启动。

现在完成进行时 (Present Perfect Continuous):
The bootloader has been loading the operating system for a few seconds. 引导加载程序一直在加载操作系统几秒钟。
The computer has been using the bootloader to start for a while. 计算机一直使用引导加载程序启动一段时间了。

一般过去时 (Simple Past):
The bootloader loaded the operating system quickly. 引导加载程序快速加载了操作系统。
The computer used the bootloader to start yesterday. 计算机昨天使用引导加载程序启动。


过去进行时 (Past Continuous):
The bootloader was loading the operating system when the power went out.  断电时，引导加载程序正在加载操作系统。
The computer was using the bootloader when it crashed. 计算机崩溃时正在使用引导加载程序。

过去完成时 (Past Perfect):
The bootloader had loaded the operating system before the error occurred.  在错误发生之前，引导加载程序已经加载了操作系统。
The computer had used the bootloader before it shut down. 计算机在关机之前已经使用了引导加载程序。

过去完成进行时 (Past Perfect Continuous):
The bootloader had been loading the operating system for several minutes before the interruption. 在中断之前，引导加载程序已经加载操作系统几分钟了。
The computer had been using the bootloader for a while before it restarted. 计算机在重新启动之前已经使用引导加载程序一段时间了。

一般将来时 (Simple Future):
The bootloader will load the new operating system tomorrow. 引导加载程序明天将加载新的操作系统。
The computer will use the bootloader to start after the update. 更新后，计算机将使用引导加载程序启动。

将来进行时 (Future Continuous):
The bootloader will be loading the operating system at this time tomorrow. 明天这个时候，引导加载程序将正在加载操作系统。
The computer will be using the bootloader during the installation process. 在安装过程中，计算机将正在使用引导加载程序。

将来完成时 (Future Perfect):
The bootloader will have loaded the operating system by the time you log in. 当您登录时，引导加载程序将已经加载了操作系统。
The computer will have used the bootloader to start by then. 到那时，计算机将已经使用引导加载程序启动。

将来完成进行时 (Future Perfect Continuous):
The bootloader will have been loading the operating system for a minute by the time the screen turns on.  到屏幕亮起时，引导加载程序将已经加载操作系统一分钟了。
The computer will have been using the bootloader for a few minutes by the time you see the desktop. 当您看到桌面时，计算机将已经使用引导加载程序几分钟了。


PHRASE->
configure the bootloader  配置引导加载程序
update the bootloader 更新引导加载程序
install a bootloader 安装引导加载程序
boot from the bootloader 从引导加载程序启动
a damaged bootloader 损坏的引导加载程序
