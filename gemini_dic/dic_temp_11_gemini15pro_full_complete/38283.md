concatenated:/kənˈkætɪneɪtɪd/ | adj. |连接的，串联的 |Linked together in a series or chain.  Formed by linking together. |The file was created by concatenated several smaller files.  该文件是由几个较小的文件连接而成的。| joined, combined, linked, attached, unified | separated, detached, divided | adjective

concatenated:/kənˈkætɪneɪtɪd/ | v. |连接，串联（concatenate的过去式和过去分词）| To link together in a series or chain. | The program concatenated the two strings. 程序连接了两个字符串。|  joined, combined, linked, attached, unified | separated, detached, divided | verb



USAGE->

一般现在时 (Simple Present):
The system concatenates the log files daily. 系统每天连接日志文件。
He concatenates the strings in alphabetical order. 他按字母顺序连接字符串。

现在进行时 (Present Continuous):
The program is concatenating the data files as we speak.  程序正在连接数据文件。
She is concatenating the video clips to make a longer film. 她正在连接视频剪辑以制作更长的电影。

现在完成时 (Present Perfect):
The script has concatenated all the necessary files. 脚本已连接所有必要的文件。
He has concatenated the images into a single document. 他已将图像连接到单个文档中。

现在完成进行时 (Present Perfect Continuous):
The software has been concatenating the data for hours. 软件已经连接数据好几个小时了。
They have been concatenating the audio files to create a podcast. 他们一直在连接音频文件以创建播客。

一般过去时 (Simple Past):
We concatenated the reports into one master document. 我们将报告连接成一个主文档。
She concatenated the strings using a special function. 她使用特殊函数连接了字符串。

过去进行时 (Past Continuous):
I was concatenating the files when the power went out. 我正在连接文件时停电了。
He was concatenating the video clips when he got interrupted. 他正在连接视频剪辑时被打断了。

过去完成时 (Past Perfect):
They had concatenated the data before analyzing it. 他们在分析数据之前已将其连接起来。
She had concatenated the files before sending them to the client.  她在将文件发送给客户端之前已将其连接起来。

过去完成进行时 (Past Perfect Continuous):
We had been concatenating the data for days before we found the error. 我们连接数据好几天后才发现错误。
He had been concatenating the audio files for hours before the program crashed.  他连接音频文件几个小时后程序崩溃了。

一般将来时 (Simple Future):
The program will concatenate the files automatically. 程序将自动连接文件。
I will concatenate the strings using this command. 我将使用此命令连接字符串。

将来进行时 (Future Continuous):
The system will be concatenating the data overnight. 系统将在夜间连接数据。
He will be concatenating the video clips tomorrow. 他明天将连接视频剪辑。

将来完成时 (Future Perfect):
By tomorrow, the program will have concatenated all the data. 到明天，程序将连接所有数据。
By next week, I will have concatenated all the necessary files. 到下周，我将连接所有必要的文件。

将来完成进行时 (Future Perfect Continuous):
By the end of the month, the system will have been concatenating data for a full year. 到月底，系统将连接数据整整一年。
By the time you arrive, I will have been concatenating these files for hours. 到你到达时，我将已经连接这些文件几个小时了。


PHRASE->
concatenate files  连接文件
concatenate strings 连接字符串
concatenate data 连接数据
concatenate lists  连接列表
concatenate arrays 连接数组
