ascian: /ˈæʃən/
adjective
ASCII码的
Relating to or denoting the American Standard Code for Information Interchange.
Example: The ascian character set includes 128 characters.  ASCII字符集包含128个字符。

Example:  The file is in ascian format.  该文件是ASCII格式的。
Synonym: ASCII
Antonym: (No direct antonym, perhaps "binary" or "Unicode" could be considered contextual antonyms)
词性: adjective


USAGE->

一般现在时 (Simple Present):
The ascian character set represents text in computers. ASCII字符集在计算机中表示文本。

Many systems still rely on ascian encoding. 许多系统仍然依赖ASCII编码。


现在进行时 (Present Continuous):
The program is converting the file to ascian format.  程序正在将文件转换为ASCII格式。

We are using ascian characters for this transmission. 我们正在使用ASCII字符进行此传输。


现在完成时 (Present Perfect):
The system has processed the ascian data successfully.  系统已成功处理ASCII数据。

We have used ascian encoding for years.  我们已经使用ASCII编码多年了。


现在完成进行时 (Present Perfect Continuous):
They have been working with ascian files all week. 他们整个星期都在处理ASCII文件。

The computer has been processing ascian characters for hours.  计算机已经处理ASCII字符好几个小时了。


一般过去时 (Simple Past):
The original message was in ascian.  原始消息是ASCII格式的。

We used ascian encoding for the older systems. 我们对旧系统使用ASCII编码。


过去进行时 (Past Continuous):
We were using ascian characters when the error occurred.  发生错误时，我们正在使用ASCII字符。

The system was converting the file to ascian when it crashed. 系统崩溃时正在将文件转换为ASCII格式。


过去完成时 (Past Perfect):
They had converted the file to ascian before sending it.  他们在发送之前已将文件转换为ASCII格式。

We had been using ascian for years before adopting Unicode. 在采用Unicode之前，我们已经使用ASCII多年。


过去完成进行时 (Past Perfect Continuous):
The system had been processing ascian data for hours before the failure. 系统在故障前已处理ASCII数据数小时。

We had been working with ascian files all week before the deadline.  在截止日期之前，我们整个星期都在处理ASCII文件。


一般将来时 (Simple Future):
We will use ascian encoding for the basic text file. 我们将对基本文本文件使用ASCII编码。

The system will process the ascian data automatically. 系统将自动处理ASCII数据。


将来进行时 (Future Continuous):
We will be using ascian characters for the next transmission.  下次传输时我们将使用ASCII字符。

The system will be converting the file to ascian format tomorrow. 系统明天将把文件转换为ASCII格式。


将来完成时 (Future Perfect):
By next week, we will have converted all files to ascian. 到下周，我们将把所有文件转换为ASCII。

The system will have processed all the ascian data by the end of the day. 系统将在一天结束前处理完所有ASCII数据。


将来完成进行时 (Future Perfect Continuous):
By next year, we will have been using ascian encoding for twenty years. 到明年，我们将已经使用ASCII编码二十年了。

The system will have been processing ascian characters for days by the time it finishes.  到完成时，系统将已经处理ASCII字符数天。




PHRASE->
ascian character ASCII字符
ascian code ASCII码
ascian file ASCII文件
ascian format ASCII格式
ascian text ASCII文本


注意事项:  Although "ascian" appears in some online dictionaries, it's more commonly and correctly spelled "ASCII".  The analysis above assumes the intended word was "ASCII".  Using "ascian" may be considered a misspelling.
