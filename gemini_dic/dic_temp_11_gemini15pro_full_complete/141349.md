operands:/ˈɒpərændz/ /ˈɑːpərændz/| n. |操作数|A quantity on which an operation is performed.  The objects of a mathematical operation.|The operands in the equation 2 + 3 = 5 are 2 and 3.  等式 2 + 3 = 5 中的操作数是 2 和 3。|arguments, parameters| N/A |noun


USAGE->

一般现在时 (Simple Present):
The computer processes the operands. = 计算机处理操作数。
The formula uses two operands. = 该公式使用两个操作数。

现在进行时 (Present Continuous):
The system is currently processing the operands. = 系统正在处理操作数。
The calculator is displaying the operands. = 计算器正在显示操作数。


现在完成时 (Present Perfect):
The program has successfully processed the operands. = 程序已成功处理操作数。
The user has entered the operands. = 用户已输入操作数。


现在完成进行时 (Present Perfect Continuous):
The system has been processing the operands for an hour. = 系统已处理操作数一个小时了。
The computer has been calculating with these operands all day. = 计算机整天都在用这些操作数进行计算。


一般过去时 (Simple Past):
The function processed the operands correctly. = 函数正确地处理了操作数。
The system retrieved the operands from memory. = 系统从内存中检索了操作数。


过去进行时 (Past Continuous):
The computer was processing the operands when the power went out. = 断电时，计算机正在处理操作数。
The program was accessing the operands when it crashed. = 程序崩溃时正在访问操作数。



过去完成时 (Past Perfect):
The system had processed the operands before the error occurred. = 在错误发生之前，系统已经处理了操作数。
The program had already accessed the operands. = 程序已经访问了操作数。


过去完成进行时 (Past Perfect Continuous):
The computer had been processing the operands for hours before it finished. = 计算机在完成之前已经处理了几个小时的操作数。
The system had been working with the operands for a while before the interruption. = 在中断之前，系统已经使用操作数一段时间了。


一般将来时 (Simple Future):
The program will process the operands next. = 程序下一步将处理操作数。
The system will retrieve the operands from the database. = 系统将从数据库中检索操作数。


将来进行时 (Future Continuous):
The computer will be processing the operands tomorrow. = 计算机明天将处理操作数。
The system will be using these operands in the next calculation. = 系统将在下次计算中使用这些操作数。


将来完成时 (Future Perfect):
By tomorrow, the system will have processed all the operands. = 到明天，系统将已经处理了所有操作数。
The program will have finished working with the operands by the end of the week. = 到本周末，程序将完成对操作数的处理。


将来完成进行时 (Future Perfect Continuous):
By next week, the system will have been processing operands for a month. = 到下周，系统将已经处理操作数一个月了。
The computer will have been working with these operands for several days by then. = 到那时，计算机将已经使用这些操作数好几天了。



虚拟语气:
If I had the correct operands, I could solve the equation. = 如果我有正确的操作数，我就能解出这个方程。
If the operands were different, the result would be different. = 如果操作数不同，结果也会不同。



被动语态:
The operands are processed by the computer. = 操作数由计算机处理。
The operands were entered into the system. = 操作数被输入到系统中。



疑问句:
What are the operands in this equation? = 这个方程中的操作数是什么？
How are the operands used in this calculation? = 在此计算中如何使用操作数？


否定句:
The equation does not have any operands. = 该方程没有任何操作数。
The program did not process the operands correctly. = 程序没有正确处理操作数。





PHRASE->
logical operands = 逻辑操作数
arithmetic operands = 算术操作数
binary operands = 二元操作数
operand address = 操作数地址
input operands = 输入操作数
