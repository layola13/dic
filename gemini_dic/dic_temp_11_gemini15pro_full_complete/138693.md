nul:/nʌl/| adj. n.|空的，无效的；零|Having no legal or binding force; invalid.  A zero value.|The contract was declared nul and void.  合同被宣布无效。 The program returned a nul value. 程序返回了一个空值。|近义词：invalid, void, zero|反义词: valid, effective|词性:形容词, 名词

USAGE->

一般现在时 (Simple Present):
The function returns nul if no match is found. = 如果没有找到匹配项，函数将返回空值。
This field is often left nul. = 此字段通常留空。


现在进行时 (Present Continuous):
The system is currently returning a nul value. = 系统当前正在返回空值。
He is making the variable nul. = 他正在将变量置空。


现在完成时 (Present Perfect):
The program has returned a nul result. = 程序返回了一个空结果。
The variable has been set to nul. = 变量已设置为null。


现在完成进行时 (Present Perfect Continuous):
The system has been returning nul values all day. = 系统一整天都在返回空值。
We have been trying to fix this nul pointer exception. = 我们一直在尝试修复此空指针异常。


一般过去时 (Simple Past):
The query returned nul. = 查询返回了空值。
The function returned nul unexpectedly. = 函数意外返回了空值。


过去进行时 (Past Continuous):
The program was returning nul when I checked. = 我检查时程序正在返回空值。
He was expecting a string but got nul. = 他期待一个字符串，但得到了空值。


过去完成时 (Past Perfect):
The variable had been set to nul before the error occurred. = 在错误发生之前，变量已被设置为null。
The function had returned nul several times before we found the bug. = 在我们找到错误之前，该函数已经多次返回空值。


过去完成进行时 (Past Perfect Continuous):
The system had been returning nul values for hours before we noticed. = 在我们注意到之前，系统已经返回了几个小时的空值。
They had been struggling with nul pointer exceptions all week. = 他们整个星期都在努力解决空指针异常。


一般将来时 (Simple Future):
If you enter incorrect data, the system will return nul. = 如果您输入不正确的数据，系统将返回空值。
The function will return nul if no data is found. = 如果未找到数据，该函数将返回空值。


将来进行时 (Future Continuous):
The program will be returning nul values until the bug is fixed. = 程序将返回空值，直到修复错误为止。
We will be testing for nul values throughout the process. = 我们将在整个过程中测试空值。


将来完成时 (Future Perfect):
By the end of the day, the system will have returned hundreds of nul values. = 到今天结束时，系统将返回数百个空值。
The program will have finished processing the nul values by tomorrow. = 到明天，程序将完成对空值的处理。


将来完成进行时 (Future Perfect Continuous):
By next week, the system will have been returning nul values for a month. = 到下周，系统将返回空值一个月了。
They will have been working on the nul pointer issue for weeks by the time it's resolved. = 到问题解决时，他们将已经处理空指针问题数周了。


PHRASE->
return nul = 返回空值
nul value = 空值
nul pointer = 空指针
nul character = 空字符
set to nul = 设置为空


词源: From Latin nullus (“no, none”).
