outport:/ˈaʊtpɔːrt/| n. |输出端口; 外港|A port used for sending data out of a computer or other device; A harbor or port, especially one in a remote or less developed area. |Data is transmitted to the printer through the outport. 数据通过输出端口传输到打印机。|The small outport relies heavily on fishing.  这个小外港主要依靠渔业。|近义词：output port, exit port, harbor|反义词:inport|名词


USAGE->

一般现在时 (Simple Present):
The computer outport transmits data.  计算机输出端口传输数据。
The outport handles a lot of cargo.  这个外港处理大量的货物。


现在进行时 (Present Continuous):
The system is transmitting data through the outport. 系统正在通过输出端口传输数据。
The ship is leaving the outport.  船正在离开外港。


现在完成时 (Present Perfect):
The outport has handled a significant amount of cargo this year. 今年这个外港处理了大量的货物。
The computer has sent the data through the outport. 计算机已经通过输出端口发送了数据。


现在完成进行时 (Present Perfect Continuous):
The engineers have been monitoring the outport's performance all day. 工程师们一整天都在监控输出端口的性能。
The ships have been using this outport for decades.  船只几十年来一直使用这个外港。


一般过去时 (Simple Past):
The outport handled less cargo last year. 去年这个外港处理的货物较少。
The system sent the data through a different outport. 系统通过不同的输出端口发送了数据。


过去进行时 (Past Continuous):
The engineers were testing the outport when the power failed.  工程师们正在测试输出端口时断电了。
The ship was approaching the outport when the storm hit.  船正在接近外港时，暴风雨来袭了。


过去完成时 (Past Perfect):
The ship had left the outport before the storm arrived.  船在暴风雨来临之前就离开了外港。
The system had already transmitted the data through the outport.  系统已经通过输出端口传输了数据。


过去完成进行时 (Past Perfect Continuous):
The engineers had been working on the outport for weeks before they fixed the problem.  工程师们在修复问题之前已经在输出端口上工作了好几个星期。
The ships had been using that outport for generations before it was modernized.  在现代化之前，船只已经使用那个外港好几代了。


一般将来时 (Simple Future):
The new outport will handle more cargo.  新的外港将处理更多的货物。
The system will send the data through the designated outport.  系统将通过指定的输出端口发送数据。


将来进行时 (Future Continuous):
The engineers will be monitoring the outport's performance during the test. 测试期间，工程师将监控输出端口的性能。
The ship will be approaching the outport tomorrow morning.  船将于明天早上接近外港。


将来完成时 (Future Perfect):
By next year, the new outport will have handled a record amount of cargo. 到明年，新的外港将处理创纪录的货物量。
The system will have transmitted all the data by the end of the day. 到今天结束，系统将传输所有数据。


将来完成进行时 (Future Perfect Continuous):
By next year, the ships will have been using the new outport for a full year.  到明年，船只将已经使用新的外港整整一年了。
The system will have been transmitting data continuously for 72 hours by tomorrow. 到明天，系统将已经连续传输数据 72 小时了。



PHRASE->
check the outport = 检查输出端口
configure the outport = 配置输出端口
monitor the outport = 监控输出端口
connect to the outport = 连接到输出端口
data through the outport = 通过输出端口的数据
