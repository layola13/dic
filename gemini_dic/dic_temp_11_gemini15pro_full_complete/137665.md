nonuniquely:/ˌnɒnjuːˈniːkli/ | adv. | 非唯一地，不唯一地 | Not uniquely; in a way that is not unique. | The solution to the problem is nonuniquely determined.  这个问题的解决方案并非唯一确定。 | The design can be nonuniquely implemented in several ways. 该设计可以用几种非唯一的方式实现。 |  近义词: variously, diversely | 反义词: uniquely, exclusively | 词性: 副词

USAGE->

USAGE->
一般现在时 (Simple Present):
The system nonuniquely identifies the user based on their IP address.  该系统根据用户的IP地址非唯一地识别用户。

He nonuniquely assigns tasks to his team members. 他以非唯一的方式将任务分配给他的团队成员。


现在进行时 (Present Continuous):
The software is nonuniquely assigning resources to different processes.  该软件正在以非唯一的方式将资源分配给不同的进程。

They are nonuniquely categorizing the data based on several criteria.  他们正在根据几个标准以非唯一的方式对数据进行分类。


现在完成时 (Present Perfect):
The algorithm has nonuniquely identified several possible solutions. 该算法已经非唯一地识别出几种可能的解决方案。

The researchers have nonuniquely classified the samples based on their properties. 研究人员根据样本的特性对样本进行了非唯一的分类。


现在完成进行时 (Present Perfect Continuous):
The team has been nonuniquely analyzing the data for several weeks.  该团队几周来一直在以非唯一的方式分析数据。

He has been nonuniquely assigning values to the variables. 他一直在以非唯一的方式为变量赋值。



一般过去时 (Simple Past):
The program nonuniquely generated the output based on the input parameters.  程序根据输入参数非唯一地生成了输出。

The experiment nonuniquely demonstrated the phenomenon.  该实验以非唯一的方式证明了这一现象。


过去进行时 (Past Continuous):
The system was nonuniquely processing the requests when the error occurred.  发生错误时，系统正在以非唯一的方式处理请求。

They were nonuniquely distributing the workload among the servers.  他们当时正在以非唯一的方式在服务器之间分配工作负载。


过去完成时 (Past Perfect):
The researchers had nonuniquely identified the cause of the problem before the solution was found.  在找到解决方案之前，研究人员已经非唯一地确定了问题的原因。

The team had nonuniquely categorized the data before the analysis began.  团队在开始分析之前已经非唯一地对数据进行了分类。


过去完成进行时 (Past Perfect Continuous):
The system had been nonuniquely allocating resources for several hours before the crash.  系统在崩溃前几个小时一直在以非唯一的方式分配资源。

They had been nonuniquely labeling the samples for several days. 他们已经以非唯一的方式标记样本好几天了。


一般将来时 (Simple Future):
The algorithm will nonuniquely determine the optimal solution.  该算法将非唯一地确定最佳解决方案。

The system will nonuniquely assign identifiers to each user. 系统将非唯一地为每个用户分配标识符。


将来进行时 (Future Continuous):
The program will be nonuniquely processing the data in real-time.  该程序将以非唯一的方式实时处理数据。

The team will be nonuniquely analyzing the results of the experiment. 该团队将以非唯一的方式分析实验结果。


将来完成时 (Future Perfect):
By the end of the project, the team will have nonuniquely identified several potential solutions.  到项目结束时，团队将已经非唯一地确定了几种可能的解决方案。

The system will have nonuniquely assigned resources to all the users.  系统将已经非唯一地将资源分配给所有用户。


将来完成进行时 (Future Perfect Continuous):
By next year, the researchers will have been nonuniquely studying the phenomenon for five years.  到明年，研究人员将已经以非唯一的方式研究这一现象五年了。

The team will have been nonuniquely collecting data for several months. 该团队将已经以非唯一的方式收集数据好几个月了。




PHRASE->
(This word doesn't typically form common phrases.  Here are some examples showing its potential use in context)

It can be implemented nonuniquely. 它可以以非唯一的方式实现。

The data is labeled nonuniquely. 数据被标记为非唯一的。

The solution is defined nonuniquely. 解决方案被定义为非唯一的。

The system identifies users nonuniquely.  系统以非唯一的方式识别用户。

The algorithm assigns values nonuniquely. 该算法以非唯一的方式分配值。


词源: non- (not) + uniquely (uniquely)
