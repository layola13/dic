concatenating:/kənˈkætɪneɪtɪŋ/ | v. | 连接，串联 | The process of linking things together in a series or chain. |  The program is concatenating the two files. 程序正在连接这两个文件。 |  linking, joining, combining, appending | separating, dividing, splitting | verb

USAGE->

一般现在时 (Simple Present):
The system concatenates the data files every night. 系统每晚连接数据文件。
He often concatenates strings of text in his programming work. 他经常在他的编程工作中连接文本字符串。


现在进行时 (Present Continuous):
The program is concatenating the video clips right now. 程序现在正在连接视频片段。
She is concatenating the different parts of the report. 她正在连接报告的不同部分。


现在完成时 (Present Perfect):
The software has concatenated all the images into a single file.  软件已将所有图像连接成一个文件。
He has concatenated the audio files for the podcast. 他已经为播客连接了音频文件。


现在完成进行时 (Present Perfect Continuous):
The computer has been concatenating the data for hours.  计算机已连接数据数小时了。
They have been concatenating the genetic sequences all week. 他们整个星期都在连接基因序列。


一般过去时 (Simple Past):
I concatenated the two lists of names. 我连接了两个姓名列表。
She concatenated the files before sending them. 她在发送文件之前连接了它们。


过去进行时 (Past Continuous):
He was concatenating the data when the power went out. 断电时他正在连接数据。
We were concatenating the strings when the program crashed. 程序崩溃时我们正在连接字符串。


过去完成时 (Past Perfect):
She had concatenated the files before I arrived. 在我到达之前，她已经连接了文件。
They had concatenated the data sets before running the analysis. 在运行分析之前，他们已经连接了数据集。


过去完成进行时 (Past Perfect Continuous):
He had been concatenating the data for hours before he realized his mistake. 他在意识到自己的错误之前已经连接数据数小时了。
We had been concatenating the strings for a long time before the program finished. 在程序完成之前，我们已经连接字符串很长时间了。


一般将来时 (Simple Future):
The system will concatenate the files automatically. 系统将自动连接文件。
I will concatenate the documents tomorrow. 我明天将连接文档。


将来进行时 (Future Continuous):
The program will be concatenating the files overnight. 程序将在夜间连接文件。
She will be concatenating the data during the update.  她将在更新期间连接数据。


将来完成时 (Future Perfect):
By tomorrow, the system will have concatenated all the files. 到明天，系统将连接所有文件。
He will have concatenated the data by the end of the week. 他将在周末之前连接数据。


将来完成进行时 (Future Perfect Continuous):
By next week, the computer will have been concatenating the data for a month. 到下周，计算机将已经连接数据一个月了。
She will have been concatenating the sequences for several days by then. 到那时，她将已经连接序列好几天了。



PHRASE->
concatenate strings  连接字符串
concatenate files  连接文件
concatenate data  连接数据
concatenate lists  连接列表
concatenate arrays  连接数组
