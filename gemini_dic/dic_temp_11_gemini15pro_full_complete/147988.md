overridable:/ˌoʊvərˈraɪdəbl/| adj.|可覆盖的，可重写的|able to be overridden, especially of a method in object-oriented programming that a subclass can replace with its own implementation.|The default settings are overridable by the user. 用户可以覆盖默认设置。| synonyms: replaceable, alterable, modifiable, changeable| antonyms: fixed, unchangeable, permanent| adjective

USAGE->

一般现在时 (Simple Present):
This method is overridable in subclasses.  这个方法可以在子类中被覆盖。
The function is overridable if necessary. 如果需要，这个函数是可以覆盖的。

现在进行时 (Present Continuous):
The developer is overridable the default settings.  开发者正在覆盖默认设置。
We are considering making this function overridable. 我们正在考虑将此函数设为可覆盖的。


现在完成时 (Present Perfect):
The programmer has overridden the default method. 程序员已经覆盖了默认方法。
They have made several functions overridable. 他们已经将几个函数设为可覆盖的。


现在完成进行时 (Present Perfect Continuous):
He has been overriding the settings for hours.  他一直在覆盖这些设置好几个小时了。
They have been working on making the system more overridable. 他们一直在努力使系统更易于覆盖。

一般过去时 (Simple Past):
We overridden the function to improve performance. 我们覆盖了该函数以提高性能。
The system allowed us to override the default settings. 系统允许我们覆盖默认设置。

过去进行时 (Past Continuous):
I was overriding the default settings when the power went out.  断电时我正在覆盖默认设置。
They were discussing whether the method should be overridable. 他们在讨论这个方法是否应该可覆盖。


过去完成时 (Past Perfect):
She had overridden the default settings before testing the program.  她在测试程序之前已经覆盖了默认设置。
He had already made the function overridable before we requested it. 在我们提出请求之前，他已经将该函数设为可覆盖的。

过去完成进行时 (Past Perfect Continuous):
They had been overriding the default settings for days before finding the solution. 他们在找到解决方案之前已经覆盖默认设置好几天了。
She had been trying to make the function overridable for weeks.  她已经尝试将该函数设为可覆盖好几个星期了。


一般将来时 (Simple Future):
You will be able to override these settings later.  你以后可以覆盖这些设置。
The system will allow you to override the default values. 系统将允许您覆盖默认值。


将来进行时 (Future Continuous):
I will be overriding the default settings tomorrow. 我明天将覆盖默认设置。
The team will be discussing making the function overridable next week.  团队下周将讨论使该函数可覆盖。

将来完成时 (Future Perfect):
By the end of the week, I will have overridden all the necessary settings.  到本周末，我将覆盖所有必要的设置。
They will have made the entire system overridable by next month.  到下个月，他们将使整个系统可覆盖。

将来完成进行时 (Future Perfect Continuous):
By next year, I will have been overriding settings in this system for five years. 到明年，我将已经在这个系统中覆盖设置五年了。
They will have been working on making the system more overridable for months by the time it's released. 到发布时，他们将在使系统更易于覆盖方面工作数月。



PHRASE->
overridable method  可覆盖方法
overridable function 可覆盖函数
overridable settings 可覆盖设置
make a method overridable 使一个方法可覆盖
is overridable  是可覆盖的
