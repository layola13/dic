rasterized:/ˈræstəraɪzd/| adj.|栅格化的；光栅化的|Converted into a raster image, a grid of pixels.|The image was rasterized for printing. 这张图片被栅格化以用于打印。|近义词：pixelated|反义词：vectorized


USAGE->
一般现在时:
The software rasterizes images quickly. = 该软件快速地栅格化图像。
This printer rasterizes documents efficiently. = 这台打印机有效地栅格化文档。

一般过去时:
The designer rasterized the logo before printing. = 设计师在打印前对标志进行了栅格化处理。
The program rasterized the vector graphic into a bitmap. = 程序将矢量图形栅格化成位图。

现在完成时:
The system has rasterized all the input files. = 系统已经栅格化了所有输入文件。
We have rasterized the map for better display on low-resolution screens. = 我们已经对地图进行了栅格化处理，以便在低分辨率屏幕上更好地显示。

过去完成时:
The printer had rasterized the image before the error occurred. = 打印机在发生错误之前已经栅格化了图像。
They had rasterized the data before running the analysis. = 他们在运行分析之前已经对数据进行了栅格化处理。

将来时:
The software will rasterize the image automatically. = 该软件将自动栅格化图像。
The new system will rasterize the data more efficiently. = 新系统将更有效地栅格化数据。


虚拟语气:
If the software rasterized the images faster, we would finish earlier. = 如果软件栅格化图像更快，我们会早点完成。
I wish the system had rasterized the data correctly. = 我希望系统能正确地栅格化数据。


被动语态:
The image was rasterized by the program. = 图像被程序栅格化了。
The data is being rasterized for the upcoming presentation. = 数据正在被栅格化，用于即将到来的演示。


疑问句:
Has the image been rasterized yet? = 图片已经栅格化了吗？
Will the document be rasterized before printing? = 文档会在打印前被栅格化吗？


否定句:
The image was not rasterized correctly. = 图像没有被正确栅格化。
The system does not rasterize vector graphics. = 系统不栅格化矢量图形。


PHRASE->
rasterized image = 栅格化图像
rasterized graphics = 栅格化图形
rasterized map = 栅格化地图
rasterized font = 栅格化字体
rasterized data = 栅格化数据

