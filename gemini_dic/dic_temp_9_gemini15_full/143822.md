outpaths: /ˈaʊtˌpæθz/ | n. | 输出路径 | Output paths are the directories or files where the results of a process are written or stored.  They are frequently used in computing, especially in software development and data processing.  For example,  a compiler might specify an outpath for the executable file it generates, or a data analysis script might specify an outpath for the resulting report. | The compiler saved the executable to the specified outpath. 编译器将可执行文件保存到指定的输出路径。 The script wrote the results to the outpath in CSV format.  脚本以CSV格式将结果写入输出路径。 | output directories, destination paths | input paths  | n.
