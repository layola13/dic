interplate:/ɪnˈtɜːpleɪt/ | vt. |插入；内插；(在数据序列中)估算|To insert (something) between other things; to calculate values between known data points.|The editor interplated a new paragraph into the article. 编辑在文章中插入了一个新段落。


The geologist interplated the missing data points to create a complete graph. 地质学家内插缺失的数据点以创建完整的图表。|intercalate, interpolate, insert|remove, delete|动词


USAGE->
一般现在时:
He interpolates new data into the existing model. 他将新的数据插入到现有的模型中。
They interpolate missing values in the time series. 他们内插时间序列中的缺失值。


一般过去时:
She interpolated the missing information from surrounding context. 她根据周围语境推断出缺失的信息。
The scientist interpolated the data to create a smooth curve. 科学家对数据进行内插以创建平滑曲线。


现在完成时:
They have interpolated the seismic data to create a 3D model. 他们已经内插地震数据以创建三维模型。
We have interpolated the missing values using linear interpolation. 我们已经使用线性插值法内插缺失的值。


过去完成时:
The researchers had interpolated the data before they ran the analysis. 研究人员在运行分析之前已经内插了数据。
Before presenting the results, they had interpolated the gaps in their dataset. 在呈现结果之前，他们已经内插了数据集中的缺口。


将来时:
She will interpolate the missing sections of the manuscript. 她将内插手稿中缺失的部分。
They will interpolate the data using a spline function. 他们将使用样条函数内插数据。


虚拟语气:
If we interpolated the data differently, we might get a different result. 如果我们以不同的方式内插数据，我们可能会得到不同的结果。
I wish I had interpolated the data more carefully. 我希望我当时更仔细地内插数据。


被动语态:
The missing values were interpolated using a sophisticated algorithm. 缺失值是用一种复杂的算法内插的。
The data will be interpolated before further analysis. 数据将在进一步分析之前进行内插。


疑问句:
How did they interpolate the missing data points? 他们是如何内插缺失的数据点的？
Will the missing data be interpolated automatically? 缺失数据会自动内插吗？


否定句:
They did not interpolate the data before conducting the analysis. 他们在进行分析之前没有内插数据。
The software cannot interpolate data of this type. 该软件无法内插此类型的数据。


PHRASE->
linear interpolation 线性插值
polynomial interpolation 多项式插值
spline interpolation 样条插值
cubic interpolation 三次插值
temporal interpolation 时间插值

