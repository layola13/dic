globals:/ˈɡloʊbəlz/ | n. | 全局变量，全局对象 | Global variables are variables that are accessible from any part of a program.  全局变量是可以从程序的任何部分访问的变量。|  The function modified a global variable unintentionally.  该函数无意中修改了一个全局变量。 |  global data, overall variables | local variables | 名词


USAGE->
一般现在时:
The globals are defined at the top level. = 全局变量在顶层定义。
These globals are crucial for the application. = 这些全局变量对应用程序至关重要。


一般过去时:
The globals were initialized before the program started. = 全局变量在程序启动之前被初始化。
The developers changed the globals last week. = 开发人员上周更改了全局变量。


现在完成时:
We have accessed these globals many times. = 我们已经多次访问了这些全局变量。
The team has updated the globals to fix bugs. = 团队已经更新了全局变量来修复错误。


过去完成时:
The globals had already been set before the function call. = 在函数调用之前，全局变量已经被设置好了。
The error occurred because the globals had not been properly initialized. = 错误发生是因为全局变量没有被正确初始化。


将来时:
The globals will be updated in the next release. = 全局变量将在下一个版本中更新。
The system will automatically manage the globals. = 系统将自动管理全局变量。


虚拟语气:
If the globals were properly managed, the program would run efficiently. = 如果全局变量得到妥善管理，程序将高效运行。
It would be better if the globals were documented clearly. = 如果全局变量有清晰的文档说明会更好。


被动语态:
The globals are accessed by multiple functions. = 全局变量被多个函数访问。
The globals were modified without permission. = 全局变量在未经许可的情况下被修改。


疑问句:
Are these globals necessary for this function? = 这些全局变量对于这个函数是必要的吗？
Where are the globals defined in this code? = 在这段代码中全局变量在哪里定义？


否定句:
These globals are not accessible from this module. = 这些全局变量不能从这个模块访问。
The program doesn't use these globals anymore. = 程序不再使用这些全局变量了。



PHRASE->
global variables = 全局变量
global scope = 全局作用域
global namespace = 全局命名空间
global state = 全局状态
declare globals = 声明全局变量

