recomputed:/ˌriːkəmˈpjuːtɪd/ | v. |重新计算 | To compute again; to calculate again.  | The program recomputed the results after detecting an error.  程序在检测到错误后重新计算了结果。| recalculate, refigure |  uncompute (反义词含义相反，并非完全反义词) | 动词


USAGE->
一般现在时:
They recompute the data every hour. = 他们每小时重新计算一次数据。
The system recomputes the trajectory constantly. = 系统不断地重新计算轨迹。

一般过去时:
The scientist recomputed the figures after finding a mistake. = 科学家发现错误后重新计算了数据。
We recomputed the budget to reflect the new expenses. = 我们重新计算了预算以反映新的支出。

现在完成时:
They have recomputed the forecast based on new information. = 他们根据新信息重新计算了预测。
The engineers have recomputed the stress on the bridge. = 工程师们重新计算了桥梁的压力。


过去完成时:
After discovering an error, the team had recomputed the entire dataset. = 发现错误后，团队重新计算了整个数据集。
She had recomputed the equation several times before getting the correct answer. = 在得到正确答案之前，她已经多次重新计算了方程式。

将来时:
The software will recompute the values automatically. = 软件将自动重新计算这些值。
We will recompute the cost once we receive the final invoice. = 一旦收到最终发票，我们将重新计算成本。

虚拟语气:
If we had more time, we would recompute the model. = 如果我们有更多时间，我们会重新计算模型。
It would be best if we recomputed the figures before submitting the report. = 最好我们在提交报告之前重新计算一下数据。

被动语态:
The results were recomputed by the computer. = 结果由电脑重新计算。
The calculations are being recomputed to ensure accuracy. = 正在重新计算结果以确保准确性。

疑问句:
Has the program recomputed the results yet? = 程序已经重新计算结果了吗？
Will the data be recomputed before the presentation? = 在演示文稿之前是否会重新计算数据？

否定句:
The system did not recompute the data due to a power outage. = 由于停电，系统没有重新计算数据。
They haven't recomputed the numbers, so the report is inaccurate. = 他们还没有重新计算数字，所以报告不准确。


PHRASE->
recompute the figures = 重新计算数据
recompute the results = 重新计算结果
recompute the model = 重新计算模型
recompute the trajectory = 重新计算轨迹
recompute the budget = 重新计算预算

