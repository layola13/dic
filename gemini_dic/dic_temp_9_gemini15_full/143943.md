outputter:/ˈaʊtpʊtər/| n. |输出器，输出装置|A device or program that produces output.  It can refer to a physical device like a printer or a software component that generates data.| Example: The computer's outputter printed the document quickly. 这台电脑的输出器快速打印了文档。|近义词：emitter, producer, generator |反义词：inputter, receiver


USAGE->
一般现在时:
The outputter sends data to the printer. = 输出器将数据发送到打印机。
This program is an efficient outputter of reports. = 这个程序是一个高效的报表输出器。

一般过去时:
The old outputter malfunctioned yesterday. = 旧的输出器昨天发生故障了。
The system outputter successfully transferred the files. = 系统输出器成功地传输了文件。

现在完成时:
The outputter has processed all the data. = 输出器已处理所有数据。
The software outputter has generated several reports this week. = 软件输出器本周生成了几份报告。

过去完成时:
The outputter had already finished its task before the power outage. = 断电前输出器已完成其任务。
The system had completed the outputting process before the shutdown. = 系统在关闭之前已完成输出过程。

将来时:
The new outputter will improve efficiency. = 新的输出器将提高效率。
The upgraded outputter will handle larger datasets. = 升级后的输出器将处理更大的数据集。

虚拟语气:
If we had a better outputter, we would get the results faster. = 如果我们有一个更好的输出器，我们会更快地得到结果。
I wish the outputter were more reliable. = 我希望输出器更可靠一些。

被动语态:
The data was outputted by the system. = 数据由系统输出。
The report is being outputted to the screen. = 报告正在输出到屏幕上。

疑问句:
What kind of outputter do you need? = 你需要什么样的输出器？
Is this outputter compatible with my system? = 这个输出器与我的系统兼容吗？

否定句:
The outputter did not produce the expected results. = 输出器没有产生预期的结果。
The old outputter is no longer functioning correctly. = 旧的输出器不再正常工作。


PHRASE->
data outputter = 数据输出器
system outputter = 系统输出器
report outputter = 报表输出器
file outputter = 文件输出器
image outputter = 图像输出器

