checkstring:/tʃɛkˈstrɪŋ/| n.|检查字符串|A string of characters that is used to check something.|The program uses a checkstring to verify the integrity of the data.|近义词：verification string, control string|反义词：无


USAGE->
一般现在时:
The system checks the checkstring every time a file is saved. = 系统每次保存文件时都会检查校验字符串。
This function checks the input string for errors. = 此函数检查输入字符串是否存在错误。


一般过去时:
The technician checked the checkstring and found no errors. = 技术人员检查了校验字符串，没有发现错误。
The program checked the checkstring before processing the data. = 程序在处理数据之前检查了校验字符串。


现在完成时:
The software has checked the checkstring and confirmed the data integrity. = 软件已经检查了校验字符串并确认了数据的完整性。
We have checked the checkstring multiple times, and it is still valid. = 我们已经多次检查了校验字符串，它仍然有效。


过去完成时:
Before the program started, it had checked the checkstring for validity. = 在程序启动之前，它已经检查了校验字符串的有效性。
They had checked the checkstring earlier, so they knew the data was safe. = 他们之前已经检查过校验字符串，所以他们知道数据是安全的。


将来时:
The system will check the checkstring automatically upon startup. = 系统将在启动时自动检查校验字符串。
The program will check the checkstring before writing the data to the database. = 程序将在将数据写入数据库之前检查校验字符串。


虚拟语气:
If the checkstring were invalid, the program would terminate. = 如果校验字符串无效，程序将终止。
I wish the checkstring had been checked before the data was transmitted. = 我希望在数据传输之前已经检查过校验字符串。


被动语态:
The checkstring is checked by the system every minute. = 校验字符串每分钟由系统检查一次。
The checkstring was checked and found to be correct. = 校验字符串经过检查，发现是正确的。


疑问句:
Has the checkstring been checked? = 校验字符串是否已检查？
Is the checkstring valid? = 校验字符串有效吗？


否定句:
The checkstring was not checked. = 校验字符串未经检查。
The system did not check the checkstring properly. = 系统没有正确检查校验字符串。


PHRASE->
checkstring validation = 校验字符串验证
checkstring generation = 校验字符串生成
checkstring comparison = 校验字符串比较
invalid checkstring = 无效校验字符串
checkstring error = 校验字符串错误

