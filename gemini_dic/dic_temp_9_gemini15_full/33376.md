chunked:/tʃʌŋkt/ | adj. | 分块的，分成块的 | Divided into chunks or sections.  | The text was chunked into manageable paragraphs. 这篇文章被分成易于管理的段落。 | segmented, divided, portioned | continuous, undivided |


USAGE->
一般现在时:
The data is chunked for efficient processing. = 数据被分块以便高效处理。
The software chunks the files automatically. = 软件自动将文件分块。


一般过去时:
The researcher chunked the data into categories. = 研究人员将数据分成几类。
The video was chunked for easier downloading. = 视频被分块以便更容易下载。


现在完成时:
They have chunked the project into smaller tasks. = 他们已将项目分解成较小的任务。
The team has chunked the workload to improve efficiency. = 团队已将工作量分解以提高效率。


过去完成时:
She had already chunked the manuscript before submitting it. = 她提交稿件之前已将其分块。
The programmers had chunked the code for better readability. = 程序员为了更好的可读性而将代码分块。


将来时:
We will chunk the data according to the new algorithm. = 我们将根据新算法对数据进行分块。
The system will automatically chunk the large files. = 系统将自动分块大型文件。


虚拟语气:
If we chunked the data differently, the results might be better. = 如果我们以不同的方式分块数据，结果可能会更好。
I wish I had chunked the project into smaller phases. = 我希望我把项目分成更小的阶段。


被动语态:
The data was chunked by the server. = 数据由服务器分块。
The audio file is being chunked for streaming. = 音频文件正在被分块以进行流式传输。


疑问句:
How is the data chunked in this system? = 在这个系统中数据是如何分块的？
Are the files chunked before uploading? = 文件上传前是否被分块？


否定句:
The data was not chunked correctly. = 数据没有被正确分块。
The program did not chunk the video file. = 程序没有分块视频文件。



PHRASE->
data chunking = 数据分块
chunking strategy = 分块策略
chunked transfer encoding = 分块传输编码
memory chunking = 内存分块
chunked text = 分块文本

