reinitialized:/ˌriːɪˈnɪʃəlaɪzd/| v. |重新初始化，重新设置初始值 |To initialize something again; to restore it to its original or default settings.| The system was reinitialized after the crash. 系统在崩溃后被重新初始化了。  The database was reinitialized to clear all previous data. 数据库被重新初始化以清除所有先前的数据。|initialized, reset|uninitialized

USAGE->
一般现在时:
We reinitialize the counters every hour. = 我们每小时重新初始化计数器。
The program reinitializes the variables before each run. = 程序在每次运行前重新初始化变量。


一般过去时:
The engineer reinitialized the server after the update. = 工程师在更新后重新初始化了服务器。
They reinitialized the network settings to fix the connectivity issue. = 他们重新初始化网络设置以解决连接问题。


现在完成时:
The team has reinitialized the software multiple times today. = 团队今天已经多次重新初始化了软件。
We have reinitialized the database after detecting a corruption. = 在检测到损坏后，我们已经重新初始化了数据库。


过去完成时:
They had reinitialized the system before the critical test. = 他们在关键测试前已经重新初始化了系统。
The developers had reinitialized the application several times during debugging. = 开发人员在调试期间已经多次重新初始化了应用程序。


将来时:
We will reinitialize the application after the maintenance is complete. = 我们将在维护完成后重新初始化应用程序。
The system administrator will reinitialize the router if the problem persists. = 如果问题仍然存在，系统管理员将重新初始化路由器。


虚拟语气:
It would be best to reinitialize the program if the error persists. = 如果错误仍然存在，最好重新初始化程序。
If we had reinitialized the database earlier, we could have avoided data loss. = 如果我们早些重新初始化数据库，我们本可以避免数据丢失。


被动语态:
The application was reinitialized by the system automatically. = 应用程序由系统自动重新初始化。
The database was reinitialized after the security breach. = 数据库在安全漏洞后被重新初始化。


疑问句:
Was the system reinitialized after the power outage? = 停电后系统是否重新初始化了？
Should we reinitialize the variables before the next iteration? = 我们是否应该在下次迭代前重新初始化变量？


否定句:
The program did not reinitialize the variables correctly. = 程序没有正确地重新初始化变量。
The technician did not reinitialize the network card. = 技术员没有重新初始化网卡。


PHRASE->
reinitialize the database = 重新初始化数据库
reinitialize the system = 重新初始化系统
reinitialize the application = 重新初始化应用程序
reinitialize the variables = 重新初始化变量
reinitialize the network settings = 重新初始化网络设置

