nested:/ˈnes.tɪd/| adj.|嵌套的，层叠的|Containing or consisting of something similar enclosed within another.|The dollhouse had nested boxes, each one smaller than the last.  这个玩具屋里装着套娃，每个都比前一个更小。|近义词：embedded, enclosed, layered|反义词：unnested, single-level


USAGE->

一般现在时:
The program uses nested loops. = 这个程序使用了嵌套循环。
Nested tables are difficult to style. = 嵌套表格很难设置样式。

一般过去时:
The engineer designed a nested system. = 工程师设计了一个嵌套系统。
They used nested functions in their code. = 他们在代码中使用了嵌套函数。

现在完成时:
We have completed the nested analysis. = 我们已经完成了嵌套分析。
She has successfully implemented nested structures. = 她已经成功地实现了嵌套结构。

过去完成时:
The project had already used nested components before the update. = 在更新之前，项目已经使用了嵌套组件。
They had implemented nested if statements in the program. = 他们在程序中已经实现了嵌套的if语句。

将来时:
We will use nested queries in the database. = 我们将在数据库中使用嵌套查询。
They will create a nested JSON response. = 他们将创建一个嵌套的JSON响应。

虚拟语气:
If I understood nested functions, I would use them. = 如果我理解嵌套函数，我就会使用它们。
It would be easier if the data weren't nested. = 如果数据不是嵌套的，就会更容易。

被动语态:
Nested structures are often used in databases. = 嵌套结构经常用于数据库。
The data was nested for better organization. = 数据被嵌套以更好地组织。

疑问句:
Are these nested lists? = 这些是嵌套列表吗？
Is the code using nested loops? = 代码是否使用了嵌套循环？

否定句:
The design does not use nested elements. = 该设计不使用嵌套元素。
The file structure is not nested. = 文件结构不是嵌套的。


PHRASE->
nested loop  嵌套循环
nested function 嵌套函数
nested list 嵌套列表
nested structure 嵌套结构
nested table 嵌套表格
