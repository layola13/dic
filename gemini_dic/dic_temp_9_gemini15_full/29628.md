catenate:/ˈkætənˌeɪt/| vt.|连接，连锁|To link or connect together in a chain or series.|The program will catenate the files into a single archive.  程序会将这些文件连接成单个存档。|The individual segments were catenated to form a complete sequence.  各个片段被连接起来形成一个完整的序列。|link, join, concatenate, chain|separate, disconnect, unlink|动词

USAGE->
一般现在时:
They catenate the data packets. = 他们连接数据包。
The machine catenates the files automatically. = 机器自动连接文件。

一般过去时:
She catenated the two strings together. = 她把两串字符串连接在一起。
The programmer catenated the code modules. = 程序员连接了代码模块。

现在完成时:
He has catenated all the files. = 他已经连接了所有文件。
They have catenated the database records. = 他们已经连接了数据库记录。

过去完成时:
She had catenated the documents before submitting the report. = 在提交报告之前，她已经连接了文件。
The system had catenated the data streams before processing. = 系统在处理之前已经连接了数据流。

将来时:
We will catenate the images into a slideshow. = 我们将把图像连接成幻灯片。
They will catenate the video clips together. = 他们将把视频剪辑连接在一起。

虚拟语气:
If we catenated the data, the analysis would be simpler. = 如果我们连接数据，分析将会更简单。
I wish I had catenated the files earlier. = 我希望我早点连接这些文件。

被动语态:
The files were catenated by the script. = 文件由脚本连接。
The data streams are being catenated in real time. = 数据流正在实时连接。

疑问句:
Will the program catenate these files correctly? = 程序会正确连接这些文件吗？
Have they catenated the audio tracks? = 他们是否已经连接了音频轨道？

否定句:
The system did not catenate the files correctly. = 系统没有正确连接文件。
We cannot catenate these incompatible data formats. = 我们不能连接这些不兼容的数据格式。


PHRASE->
catenate strings = 连接字符串
catenate files = 连接文件
catenate data = 连接数据
catenate sequences = 连接序列
catenate records = 连接记录
