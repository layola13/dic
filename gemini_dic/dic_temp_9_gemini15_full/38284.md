concatenates:/kənˈkætəneɪts/ | v. |连接，串联|To link things together in a chain or series.|The program concatenates several files into a single archive.  该程序将多个文件连接到单个存档中。|The computer automatically concatenates the data. 计算机自动连接数据。|combine, join, unite |separate, disconnect |动词


USAGE->
一般现在时:
He concatenates the strings. = 他连接这些字符串。
The system concatenates the data streams. = 系统连接数据流。

一般过去时:
She concatenated the files yesterday. = 她昨天连接了这些文件。
They concatenated the lists together. = 他们把列表连接在一起。

现在完成时:
He has concatenated all the documents. = 他已经连接了所有文档。
We have concatenated the data successfully. = 我们已成功连接数据。

过去完成时:
She had concatenated the data before the analysis. = 在分析之前，她已经连接了数据。
They had concatenated the files before the backup. = 备份之前，他们已经连接了文件。

将来时:
He will concatenate the images tomorrow. = 他明天将连接这些图像。
The software will concatenate the audio tracks. = 该软件将连接音频轨道。

虚拟语气:
If I had the tool, I would concatenate the files. = 如果我有工具，我会连接这些文件。
It would be helpful if he concatenated the data sets. = 如果他能连接数据集，那就太好了。

被动语态:
The files were concatenated by the program. = 这些文件是由程序连接的。
The data is being concatenated in real time. = 数据正在实时连接。

疑问句:
Does the function concatenate the arrays? = 该函数是否连接数组？
How does the software concatenate the videos? = 该软件如何连接视频？

否定句:
The program doesn't concatenate the files correctly. = 该程序没有正确连接文件。
He didn't concatenate the data before running the analysis. = 他在运行分析之前没有连接数据。


PHRASE->
concatenate strings = 连接字符串
concatenate files = 连接文件
concatenate data = 连接数据
concatenate arrays = 连接数组
concatenate lists = 连接列表
