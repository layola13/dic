encodable:/ɪnˈkoʊdəbl/ | adj. | 可编码的，可编码化的 | Capable of being encoded; able to be converted into a code or a set of codes.  | Example: The data is encodable using a variety of compression algorithms.  这些数据可以使用多种压缩算法进行编码。 |近义词:codeable, encipherable| 反义词:unencodable


USAGE->
一般现在时:
The system is encodable with this new software. = 这个系统可以使用这个新软件进行编码。
This data is encodable using a simple algorithm. = 这些数据可以使用简单的算法进行编码。

一般过去时:
The message was encodable using a Caesar cipher. = 这条消息可以使用凯撒密码进行编码。
The image was encodable into a smaller file size. = 这张图片可以编码成更小的文件大小。

现在完成时:
The information has been encodable since the new update. = 自从更新后，信息就可以编码了。
We have successfully encoded all the data; it has been encodable for months. = 我们已经成功地编码了所有数据；它已经可以编码几个月了。

过去完成时:
The data had been encodable before the system crash. = 系统崩溃之前，数据是可以编码的。
They had already ensured the file was encodable before sending it. = 他们发送文件之前已经确保文件是可以编码的。

将来时:
The video will be encodable once the compression software is installed. = 安装压缩软件后，视频就可以编码了。
We will be able to encode the audio file; it will be encodable by tomorrow. = 我们将能够编码音频文件；到明天它就可以编码了。


虚拟语气:
If the data were encodable, we could transmit it more efficiently. = 如果数据可以编码，我们可以更高效地传输它。
It would be helpful if the information were encodable in a standardized format. = 如果信息可以用标准格式编码，那将很有帮助。


被动语态:
The message was encoded; it had been encodable from the start. = 消息已被编码；它从一开始就可以编码。
The audio file is being encoded; it was encodable using a lossless codec. = 音频文件正在编码；它可以使用无损编解码器进行编码。


疑问句:
Is the video encodable with this software? = 这个软件可以编码视频吗？
Are all the files encodable using the same algorithm? = 所有文件都可以使用相同的算法进行编码吗？


否定句:
The data is not encodable using this method. = 使用这种方法无法编码数据。
That file was not encodable because of its large size. = 由于文件太大，无法编码该文件。


PHRASE->
easily encodable = 易于编码的
highly encodable = 高度可编码的
securely encodable = 安全可编码的
efficiently encodable = 高效可编码的
readily encodable = 容易编码的

