interpolable:/ɪnˈtɜːrpələbl/ | adj. | 可内插的，可内推的 | Capable of being interpolated; able to be inserted into a series or sequence.  This typically refers to data points or values that can be estimated or filled in between known values. | Example: The missing data points were interpolable using linear interpolation.  缺失的数据点可以使用线性插值法进行内插。 |近义词：insertable, estimable | 反义词：uninterpolable


USAGE->
一般现在时:
The sensor data is interpolable. = 传感器数据是可内插的。
This function is interpolable. = 此函数是可内插的。


一般过去时:
The missing values were interpolable. = 缺失的值是可内插的。
The data was interpolable using a cubic spline. = 使用三次样条可以内插数据。


现在完成时:
We have found that the signal has been interpolable. = 我们已经发现该信号是可内插的。
The researchers have successfully interpolated the missing data. = 研究人员已成功内插缺失数据。


过去完成时:
They had determined that the data was interpolable before analysis. = 他们在分析之前已经确定数据是可内插的。
The engineers had already interpolable the curve before presentation. = 工程师们在演示之前已经对曲线进行了内插。


将来时:
The software will be able to interpolate the data points. = 该软件将能够对数据点进行插值。
The algorithm will interpolate the missing values accurately. = 该算法将精确地内插缺失值。


虚拟语气:
If the data were interpolable, we could create a smoother curve. = 如果数据是可内插的，我们可以创建更平滑的曲线。
It would be helpful if the signal were interpolable. = 如果信号可内插，将非常有用。


被动语态:
The data can be interpolated using various methods. = 数据可以使用各种方法进行插值。
The missing values were interpolated using a linear model. = 缺失值使用线性模型进行了插值。


疑问句:
Is the function interpolable? = 此函数是否可内插？
Are the data points interpolable using this method? = 使用此方法数据点是否可内插？


否定句:
The data is not interpolable using this algorithm. = 使用此算法数据不可内插。
The values are not readily interpolable. = 这些值不容易内插。



PHRASE->
linearly interpolable 线性可插值
spatially interpolable 空间可插值
temporally interpolable 时间可插值
easily interpolable 易于内插
difficult to interpolate 难以内插

