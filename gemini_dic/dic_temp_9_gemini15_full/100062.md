interpolar:/ɪnˈtɜːrpəleɪt/ | v. |内插，插入；篡改 | To insert (something) between other things; to interpolate data points into a dataset.  |  Example: The editor interpolated a few paragraphs into the manuscript to clarify the author's meaning. 编辑在手稿中插入了几段话以阐明作者的意思。 |近义词：insert, inject, intercalate | 反义词：extract, remove


USAGE->
一般现在时:
He interpolates new data points into the graph. 他将新的数据点插入图表中。
She interpolates missing information based on context. 她根据上下文插入缺失的信息。


一般过去时:
The scientist interpolated the missing values in the data set. 科学家内插了数据集中缺失的值。
They interpolated a new scene into the movie. 他们在电影中插入了一个新的场景。


现在完成时:
We have interpolated the missing sections of the text. 我们已经内插了文本中缺失的部分。
She has interpolated several new facts into her presentation. 她已经在她演讲中插入了一些新的事实。


过去完成时:
They had interpolated the missing data before running the analysis. 他们在运行分析之前已经内插了缺失的数据。
He had interpolated the missing chapters before publishing the book. 他在出版这本书之前已经内插了缺失的章节。


将来时:
They will interpolate the results into the existing database. 他们将把结果内插到现有的数据库中。
We will interpolate the curve to find the missing value. 我们将内插曲线以找到缺失的值。


虚拟语气:
If I had the data, I would interpolate the missing values. 如果我有数据，我会内插缺失的值。
If they interpolated the data correctly, the results would be accurate. 如果他们正确地内插数据，结果将是准确的。


被动语态:
The missing data were interpolated by the computer program. 缺失的数据由计算机程序内插。
The new paragraph was interpolated into the text. 新段落被插入文本中。


疑问句:
How did they interpolate the missing values? 他们是如何内插缺失的值？
Will the software interpolate the data automatically? 该软件会自动内插数据吗？


否定句:
The system did not interpolate the data correctly. 系统没有正确地内插数据。
He did not interpolate any new information into his report. 他没有在他的报告中插入任何新的信息。


PHRASE->
linear interpolation 线性插值
polynomial interpolation 多项式插值
cubic interpolation 三次插值
spline interpolation 样条插值
interpolation method 插值方法

