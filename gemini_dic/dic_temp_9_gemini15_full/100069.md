interpolating:/ɪnˈtɜːrpəleɪtɪŋ/| v.|内插，插值，窜改 |To interpolate means to insert something of a different nature into something else; to estimate values of a function between known data points.|The software interpolates missing data points.  软件内插缺失的数据点。|They interpolated a new paragraph into the report. 他们在报告中插入了一段新文字。|extrapolate, infer|delete, remove|


USAGE->
一般现在时:
He interpolates data points regularly. 他定期插入数据点。
She interpolates missing values in the dataset. 她插入数据集中缺失的值。


一般过去时:
The scientist interpolated the missing data. 科学家内插了缺失的数据。
They interpolated a new scene into the film. 他们在电影中插入了一个新的场景。


现在完成时:
She has interpolated the missing values using linear interpolation. 她已经使用线性插值法插入了缺失的值。
The team has interpolated several data points to smooth the curve.  团队已经内插了几个数据点以使曲线平滑。


过去完成时:
Before submitting the report, they had interpolated the missing figures. 在提交报告之前，他们已经内插了缺失的数字。
He had interpolated the missing information before presenting his findings. 在展示他的发现之前，他已经内插了缺失的信息。


将来时:
The researchers will interpolate the data using a new algorithm. 研究人员将使用一种新的算法来内插数据。
We will interpolate the missing sections of the text. 我们将内插文本中缺失的部分。


虚拟语气:
If we interpolated the data differently, we might get different results. 如果我们以不同的方式内插数据，我们可能会得到不同的结果。
I wish I had interpolated the missing data before the deadline. 我希望我在截止日期前已经内插了缺失的数据。


被动语态:
The data was interpolated using a sophisticated algorithm. 数据是使用一种复杂的算法进行内插的。
The missing values were interpolated by the software.  缺失的值由软件进行内插。


疑问句:
How did they interpolate the data? 他们是如何内插数据的？
Will the software automatically interpolate the missing values? 软件会自动内插缺失的值吗？


否定句:
The program did not interpolate the data correctly. 程序没有正确地内插数据。
We did not interpolate the missing values. 我们没有内插缺失的值。


PHRASE->
linear interpolation 线性插值
polynomial interpolation 多项式插值
spline interpolation 样条插值
cubic interpolation 三次插值
nearest-neighbor interpolation 最近邻插值

