char:/tʃɑːr/| n. |字符，特性|A char is a single character, such as a letter, number, or symbol.  It's a fundamental data type in many programming languages.|The password must contain at least one char.|character, symbol|  | noun

USAGE->
一般现在时:
This program reads a char from the keyboard. = 这个程序从键盘读取一个字符。
The char variable stores the letter 'A'. = 字符变量存储字母 'A'。


一般过去时:
The program printed the char to the console. = 程序将字符打印到控制台。
The function returned a char value. = 函数返回一个字符值。


现在完成时:
The user has entered a char. = 用户已经输入了一个字符。
The system has processed all chars. = 系统已经处理了所有字符。


过去完成时:
The program had already read the char before the error occurred. = 错误发生之前，程序已经读取了该字符。
The function had returned a char before the loop ended. = 循环结束之前，函数已经返回了一个字符。


将来时:
The program will read a char from the file. = 程序将从文件中读取一个字符。
The function will return a char indicating success or failure. = 函数将返回一个字符以指示成功或失败。


虚拟语气:
If the char were a digit, the program would process it differently. = 如果字符是数字，程序将以不同的方式处理它。
I wish the char was a lowercase letter. = 我希望字符是小写字母。


被动语态:
The char is displayed on the screen. = 字符显示在屏幕上。
The char was processed by the function. = 字符由函数处理。


疑问句:
What char is stored in the variable? = 变量中存储的是哪个字符？
Is the char a letter or a number? = 这个字符是字母还是数字？


否定句:
The variable does not contain a char. = 变量不包含字符。
The function did not return a valid char. = 函数没有返回有效的字符。


PHRASE->
char array = 字符数组
char pointer = 字符指针
char literal = 字符字面量
char constant = 字符常量
char type = 字符类型
