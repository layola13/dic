precomputes:/ˌpriːkəmˈpjuːts/| v.|预先计算，预计算|To compute beforehand; to calculate in advance.|The system precomputes the shortest path between any two points on the map.  系统预先计算了地图上任意两点之间的最短路径。

The program precomputes the necessary values to speed up processing. 程序预先计算必要的数值以加快处理速度。|近义词：calculate beforehand, precalculate|反义词：postcompute (这是一个相对较少使用的词，表示事后计算)|动词
