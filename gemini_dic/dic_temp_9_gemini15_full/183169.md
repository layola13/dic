repr:/rɛp/ | n. |表示；代表；再现 |  A representation, especially a textual representation of an object; also, the function or method that creates such a representation.  It is often used in programming to convert an object into a string representation. |例句：The `repr()` function returns a string representation of an object. = `repr()`函数返回对象的字符串表示。  The repr of the list is '[1, 2, 3]'. = 列表的repr是'[1, 2, 3]'。|近义词：representation, portrayal, depiction |反义词：None | 名词


