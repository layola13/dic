integers:/ˈɪntɪdʒərz/| n. |整数|Integers are whole numbers, both positive and negative, including zero. They are numbers without fractions or decimals.  |Example: The integers from -3 to 3 are -3, -2, -1, 0, 1, 2, 3.  整数从-3到3是-3，-2，-1，0，1，2，3。|近义词：whole numbers|反义词：fractions, decimals|名词


USAGE->
一般现在时:
The program calculates integers. = 程序计算整数。
Integers are used in many mathematical operations. = 整数用于许多数学运算。

一般过去时:
We counted the integers in the sequence. = 我们计算了序列中的整数。
She added the integers correctly. = 她正确地加了这些整数。

现在完成时:
He has identified all the integers in the dataset. = 他已经识别了数据集中所有的整数。
We have used integers extensively in this project. = 我们在这个项目中广泛使用了整数。

过去完成时:
They had already processed the integers before the error occurred. = 在错误发生之前，他们已经处理了这些整数。
She had calculated the sum of integers before the meeting. = 会议前她已经计算出整数的和。

将来时:
We will use integers to represent the data. = 我们将使用整数来表示数据。
The system will generate random integers. = 系统将生成随机整数。

虚拟语气:
If the input were an integer, the program would run correctly. = 如果输入是整数，程序将正确运行。
It would be helpful if the data were represented as integers. = 如果数据表示为整数，将会有所帮助。

被动语态:
The integers are processed by the computer. = 整数由计算机处理。
The results are expressed as integers. = 结果表示为整数。

疑问句:
Are these numbers integers? = 这些数字是整数吗？
How many integers are there in this range? = 在这个范围内有多少个整数？

否定句:
The result is not an integer. = 结果不是整数。
These values are not integers. = 这些值不是整数。


PHRASE->
set of integers: 整数集
positive integers: 正整数
negative integers: 负整数
integer programming: 整数规划
integer division: 整数除法

