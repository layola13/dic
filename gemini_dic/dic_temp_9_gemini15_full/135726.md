nonprinting: /ˌnɒnˈprɪntɪŋ/ | adj. |不可打印的|Not capable of being printed; referring to characters or data that do not produce a visible mark on a printed page or screen.|The document contains some nonprinting characters that are causing errors.|近义词：invisible, unprintable|反义词：printable


USAGE->
一般现在时:
The file contains nonprinting characters. = 文件包含一些不可打印的字符。
This software ignores nonprinting data. = 这个软件忽略不可打印的数据。


一般过去时:
The printer refused to print the nonprinting characters. = 打印机拒绝打印不可打印的字符。
The program detected and removed the nonprinting data. = 程序检测并删除了不可打印的数据。


现在完成时:
We have encountered several nonprinting characters in the text file. = 我们在文本文件中遇到了一些不可打印的字符。
The system has successfully filtered out all nonprinting data. = 系统已成功过滤掉所有不可打印的数据。


过去完成时:
The error had been caused by a hidden nonprinting character. = 错误是由隐藏的不可打印字符引起的。
They had already removed all the nonprinting elements before sending the document. = 他们在发送文档之前已经删除了所有不可打印的元素。


将来时:
The updated software will handle nonprinting characters more effectively. = 更新后的软件将更有效地处理不可打印字符。
We will need to convert the nonprinting data to a printable format. = 我们需要将不可打印的数据转换为可打印的格式。


虚拟语气:
If the document contained only printable characters, it would print perfectly. = 如果文档只包含可打印字符，它将完美打印。
I wish the software could automatically detect and remove all nonprinting characters. = 我希望软件能够自动检测并删除所有不可打印字符。


被动语态:
Nonprinting characters are often overlooked in text editing. = 在文本编辑中，不可打印字符经常被忽略。
The nonprinting data was successfully converted to a visual representation. = 不可打印的数据已成功转换为可视化表示。


疑问句:
Are there any nonprinting characters in this file? = 这个文件中是否包含任何不可打印字符？
Does the program support nonprinting characters? = 该程序是否支持不可打印字符？


否定句:
The document does not contain any nonprinting characters. = 该文档不包含任何不可打印字符。
The software does not display nonprinting characters by default. = 该软件默认不显示不可打印字符。


PHRASE->
nonprinting characters = 不可打印字符
nonprinting control characters = 不可打印控制字符
nonprinting ASCII characters = 不可打印的ASCII字符
handle nonprinting characters = 处理不可打印字符
detect nonprinting characters = 检测不可打印字符

