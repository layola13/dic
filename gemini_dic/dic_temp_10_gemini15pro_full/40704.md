convertend:/ˈkɒnvəˌtɛnd/ | n. | 被转换数，转换对象 |The convertend is the number being converted from one base to another. 被转换数是指从一个基数转换为另一个基数的数字。| The convertend in this binary to decimal conversion is 101101.  在这个二进制到十进制的转换中，被转换数是 101101。 | synonyms:  input number, original number | antonyms:  result, output | noun

USAGE->

一般现在时 (Simple Present):
The convertend represents the initial value. 被转换数代表初始值。
The system uses the convertend to calculate the output. 系统使用被转换数来计算输出。

现在进行时 (Present Continuous):
The program is converting the convertend to a decimal value.  程序正在将被转换数转换为十进制值。
The calculator is processing the convertend. 计算器正在处理被转换数。

现在完成时 (Present Perfect):
The algorithm has converted the convertend successfully. 该算法已成功转换了被转换数。
The system has identified the convertend. 系统已识别被转换数。

现在完成进行时 (Present Perfect Continuous):
The computer has been processing the convertend for the last hour.  计算机过去一小时一直在处理被转换数。
The program has been converting the convertend to different bases. 程序一直在将被转换数转换为不同的基数。

一般过去时 (Simple Past):
The function converted the convertend to hexadecimal. 该函数将被转换数转换为十六进制。
The user entered the convertend into the system. 用户将被转换数输入到系统中。

过去进行时 (Past Continuous):
The program was converting the convertend when the power went out.  断电时程序正在转换被转换数。
The system was processing the convertend before the error occurred. 在发生错误之前，系统正在处理被转换数。

过去完成时 (Past Perfect):
The system had converted the convertend before displaying the result. 系统在显示结果之前已转换了被转换数。
The user had already entered the convertend. 用户已经输入了被转换数。

过去完成进行时 (Past Perfect Continuous):
The program had been converting the convertend for several minutes before it crashed. 程序崩溃前几分钟一直在转换被转换数。
The system had been processing the convertend for a long time. 系统一直在处理被转换数很长时间。

一般将来时 (Simple Future):
The algorithm will convert the convertend to a different base. 该算法会将被转换数转换为不同的基数。
The system will process the convertend automatically. 系统将自动处理被转换数。


将来进行时 (Future Continuous):
The program will be converting the convertend at that time. 程序将在那个时间转换被转换数。
The computer will be processing the convertend during the simulation. 计算机将在模拟期间处理被转换数。

将来完成时 (Future Perfect):
The system will have converted the convertend by the end of the process.  在流程结束时，系统将已经转换了被转换数。
The program will have finished processing the convertend. 程序将已经完成被转换数的处理。

将来完成进行时 (Future Perfect Continuous):
The computer will have been converting the convertend for hours by then. 到那时，计算机将已经转换被转换数数小时了。
The system will have been processing the convertend continuously. 系统将一直在持续处理被转换数。


PHRASE->
convertend value = 被转换数值
decimal convertend = 十进制被转换数
binary convertend = 二进制被转换数
hexadecimal convertend = 十六进制被转换数
input convertend = 输入被转换数
