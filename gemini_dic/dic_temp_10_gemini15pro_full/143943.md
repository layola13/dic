outputter:/ˈaʊtpʊtər/| n.|输出器，输出程序|A device or program that outputs data.|The outputter sent the processed data to the printer.|近义词：output device, output program|反义词: input device, input program|noun

USAGE->

一般现在时 (Simple Present):
The outputter sends data to the monitor.
输出器将数据发送到显示器。

The program outputter displays the results.
程序输出器显示结果。


现在进行时 (Present Continuous):
The outputter is currently transmitting the data.
输出器正在传输数据。

The system outputter is processing a large file.
系统输出器正在处理一个大文件。


现在完成时 (Present Perfect):
The outputter has successfully sent the information.
输出器已成功发送信息。

The outputter has experienced a malfunction.
输出器发生了故障。


现在完成进行时 (Present Perfect Continuous):
The outputter has been working continuously for hours.
输出器已连续工作数小时。

The system has been using this outputter for a week.
系统已经使用这个输出器一个星期了。


一般过去时 (Simple Past):
The outputter displayed the results on the screen.
输出器在屏幕上显示结果。

The outputter failed to send the data.
输出器未能发送数据。


过去进行时 (Past Continuous):
The outputter was processing the data when the power went out.
断电时，输出器正在处理数据。

The technician was checking the outputter when the error occurred.
当错误发生时，技术人员正在检查输出器。


过去完成时 (Past Perfect):
The outputter had already sent the data before the system crashed.
在系统崩溃之前，输出器已经发送了数据。

The technician had checked the outputter before the error occurred.
在错误发生之前，技术人员已经检查了输出器。


过去完成进行时 (Past Perfect Continuous):
The outputter had been working perfectly for months before the malfunction.
在故障发生之前，输出器已经完美地工作了几个月。

The team had been using that outputter for a year before switching to a new one.
在切换到新的输出器之前，团队已经使用那个输出器一年了。


一般将来时 (Simple Future):
The outputter will send the data tomorrow.
输出器明天将发送数据。

The new outputter will improve the efficiency of the system.
新的输出器将提高系统的效率。


将来进行时 (Future Continuous):
The outputter will be transmitting data at 8 AM tomorrow.
输出器将于明天上午8点传输数据。

The team will be testing the new outputter next week.
团队下周将测试新的输出器。


将来完成时 (Future Perfect):
By next week, the outputter will have processed all the data.
到下周，输出器将处理完所有数据。

By the end of the month, we will have installed the new outputter.
到月底，我们将安装新的输出器。


将来完成进行时 (Future Perfect Continuous):
By next year, the outputter will have been operating continuously for five years.
到明年，输出器将已经连续运行五年了。

By the time the project is finished, the team will have been using this outputter for over two years.
到项目完成时，团队将已经使用这个输出器超过两年了。


虚拟语气:
If the outputter were working correctly, we would have the results by now.
如果输出器工作正常，我们现在应该已经得到结果了。

I wish the outputter would process the data faster.
我希望输出器能更快地处理数据。



被动语态:
The data was sent by the outputter.
数据由输出器发送。

The results are displayed by the outputter on the screen.
结果由输出器显示在屏幕上。


疑问句:
What type of outputter is this?
这是什么类型的输出器？

How does the outputter work?
输出器是如何工作的？


否定句:
The outputter didn't send the data correctly.
输出器没有正确发送数据。

The outputter isn't working properly.
输出器工作不正常。



PHRASE->
video outputter = 视频输出器
audio outputter = 音频输出器
data outputter = 数据输出器
high-speed outputter = 高速输出器
network outputter = 网络输出器
