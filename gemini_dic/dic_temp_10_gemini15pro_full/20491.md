bitwise:/ˈbɪtwaɪz/| adj.|按位；位；逐位的|Relating to or involving individual bits of data.  Operating on each bit individually.|The bitwise AND operator compares the corresponding bits of two integers. 按位与运算符比较两个整数的对应位。|bit-by-bit, per bit|N/A|adjective
bitwise:/ˈbɪtwaɪz/| adv.|按位地；位地；逐位地|In a way that relates to or involves individual bits of data.  Operating on each bit individually.|The function performs a bitwise comparison of the two values. 该函数执行两个值的按位比较。|bit-by-bit, per bit|N/A|adverb


USAGE->

USAGE->
一般现在时 (Simple Present):
The program bitwise shifts the value by two positions.  该程序将值按位移动两位。

Bitwise operations are common in low-level programming.  位操作在低级编程中很常见。


现在进行时 (Present Continuous):
The system is currently performing a bitwise XOR operation. 系统目前正在执行按位异或运算。

He is learning how to use bitwise operators in C++. 他正在学习如何在 C++ 中使用位运算符。


现在完成时 (Present Perfect):
We have implemented a bitwise solution for this problem. 我们已经为这个问题实现了一个按位解决方案。

The engineer has optimized the code using bitwise manipulation. 工程师使用位操作优化了代码。


现在完成进行时 (Present Perfect Continuous):
They have been working on a bitwise algorithm for days. 他们一直在研究一种按位算法好几天了。

She has been studying bitwise operators in preparation for the exam.  她一直在学习位运算符，为考试做准备。


一般过去时 (Simple Past):
The function bitwise inverted the input value.  该函数按位反转了输入值。

We used a bitwise approach to solve the puzzle. 我们使用按位方法解决了这个难题。


过去进行时 (Past Continuous):
He was explaining bitwise operations when the power went out.  断电时他正在解释位操作。

They were implementing a bitwise AND function when I arrived.  我到达时，他们正在实现一个按位与函数。


过去完成时 (Past Perfect):
She had already learned about bitwise operators before taking the course.  在参加课程之前，她已经学习了位运算符。

The team had successfully implemented a bitwise solution before the deadline.  团队在截止日期之前成功地实现了一个按位解决方案。


过去完成进行时 (Past Perfect Continuous):
He had been researching bitwise techniques for months before finding a solution. 他在找到解决方案之前已经研究位技术好几个月了。

They had been debugging the bitwise code for hours before discovering the error. 他们在发现错误之前已经调试了位代码几个小时。


一般将来时 (Simple Future):
We will use a bitwise approach to improve performance. 我们将使用按位方法来提高性能。

The new algorithm will incorporate bitwise operations. 新算法将包含位操作。


将来进行时 (Future Continuous):
Next week, we will be learning about bitwise operations in assembly language. 下周，我们将学习汇编语言中的位操作。

He will be implementing the bitwise functions next month. 他下个月将实现位函数。


将来完成时 (Future Perfect):
By the end of the project, we will have implemented all the necessary bitwise functionalities. 到项目结束时，我们将实现所有必要的按位功能。

She will have mastered bitwise operations by the time she graduates. 到她毕业时，她将掌握位操作。


将来完成进行时 (Future Perfect Continuous):
By next year, they will have been using bitwise techniques for over a decade. 到明年，他们将使用位技术超过十年。

He will have been studying bitwise algorithms for several years by the time he completes his PhD. 到他完成博士学位时，他将已经学习位算法好几年了。


虚拟语气:
If I were to optimize this code, I would use bitwise operations. 如果要优化此代码，我会使用位操作。

If we had used a bitwise approach, we could have solved the problem more efficiently. 如果我们使用按位方法，我们就可以更有效地解决问题。


被动语态:
Bitwise operations are frequently used in embedded systems. 位操作经常用于嵌入式系统。

The data was processed using a bitwise algorithm. 数据使用按位算法处理。


疑问句:
Are bitwise operations faster than arithmetic operations? 位操作比算术运算快吗？

How do bitwise operators work? 位运算符是如何工作的？


否定句:
This function does not perform any bitwise operations. 此函数不执行任何按位操作。

We did not use a bitwise approach for this project. 我们没有在这个项目中使用按位方法。



PHRASE->
bitwise AND 按位与
bitwise OR 按位或
bitwise XOR 按位异或
bitwise NOT 按位非
bitwise shift 按位移位
