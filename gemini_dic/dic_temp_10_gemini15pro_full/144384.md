outstreams:/ˈaʊtˌstriːmz/| n.|输出流，输出数据流|A sequence of data being transferred from a source to a destination, typically from a computer or other device.|Data outstreams from the server to client computers. 数据从服务器输出到客户端计算机。|The outstreams of the sensor are processed in real-time. 传感器的输出流被实时处理。|近义词：outputs, effluence, outflow|反义词:instreams, inflow|noun


USAGE->

USAGE->
一般现在时 (Simple Present):
The system outstreams data continuously. 系统持续输出数据。
The sensor outstreams readings every second. 传感器每秒输出读数。

现在进行时 (Present Continuous):
The server is currently outstreaming video to multiple clients. 服务器当前正在向多个客户端输出视频。
The program is outstreaming the results to a file. 程序正在将结果输出到一个文件。

现在完成时 (Present Perfect):
The device has outstreamed a large amount of data. 该设备已经输出了大量数据。
The system has outstreamed all the necessary information. 系统已经输出了所有必要的信息。

现在完成进行时 (Present Perfect Continuous):
The computer has been outstreaming data for several hours. 计算机已经输出了几个小时的数据。
The server has been outstreaming video to the client since the connection began. 自从连接开始以来，服务器一直在向客户端输出视频。

一般过去时 (Simple Past):
The system outstreamed the data successfully. 系统成功地输出了数据。
The sensor outstreamed a series of readings. 传感器输出了一系列读数。

过去进行时 (Past Continuous):
The server was outstreaming video when the connection dropped. 连接断开时，服务器正在输出视频。
The program was outstreaming the results when the power went out. 断电时，程序正在输出结果。

过去完成时 (Past Perfect):
The system had outstreamed the data before the error occurred. 在错误发生之前，系统已经输出了数据。
The sensor had outstreamed several readings before it malfunctioned. 传感器在发生故障之前已经输出了一些读数。

过去完成进行时 (Past Perfect Continuous):
The computer had been outstreaming data for hours before it crashed. 计算机崩溃前已经输出了几个小时的数据。
The server had been outstreaming video for a long time before the connection was interrupted.  在连接中断之前，服务器已经输出了很长时间的视频。


一般将来时 (Simple Future):
The system will outstream the data tomorrow. 系统明天将输出数据。
The sensor will outstream readings at regular intervals. 传感器将定期输出读数。


将来进行时 (Future Continuous):
The server will be outstreaming video during the conference. 会议期间，服务器将输出视频。
The program will be outstreaming the results as they become available. 程序将在结果可用时输出结果。


将来完成时 (Future Perfect):
By next week, the system will have outstreamed all the required data. 到下周，系统将输出所有必需的数据。
By the end of the day, the sensor will have outstreamed hundreds of readings. 到今天结束时，传感器将输出数百个读数。


将来完成进行时 (Future Perfect Continuous):
By next year, the computer will have been outstreaming data for a decade. 到明年，计算机将已经输出了十年的数据。
By the time the project is complete, the system will have been outstreaming data continuously for months. 到项目完成时，系统将已经连续数月输出数据。


虚拟语气:
If the system were working properly, it would outstream the data correctly. 如果系统正常工作，它将正确地输出数据。
I wish the sensor would outstream more frequent readings. 我希望传感器能输出更频繁的读数。


被动语态:
The data is outstreamed by the system. 数据由系统输出。
The video is being outstreamed to multiple clients. 视频正在输出到多个客户端。


疑问句:
What data is being outstreamed? 正在输出什么数据？
Where are the outstreams being directed? 输出流被定向到哪里？


否定句:
The system is not outstreaming any data. 系统没有输出任何数据。
The sensor is not outstreaming readings regularly. 传感器没有定期输出读数。


PHRASE->
data outstreams = 数据输出流
real-time outstreams = 实时输出流
continuous outstreams = 连续输出流
high-volume outstreams = 大容量输出流
video outstreams = 视频输出流
