parsable:/ˈpɑːrsəbl/ | adj. | 可解析的，可分析的 | Able to be parsed; capable of being broken down into component parts for analysis or interpretation, especially by a computer program. | The data is parsable by our software.  这些数据可以用我们的软件解析。| analyzable, interpretable | unparsable, indecipherable | adjective

parsable:/ˈpɑːrsəbl/ | adj. | 可解析的，可分析的 | Able to be parsed; capable of being broken down into component parts for analysis or interpretation, especially by a computer program. | The log files are parsable using regular expressions.  这些日志文件可以使用正则表达式解析。|  decodable, readable | illegible, unreadable | adjective

USAGE->

USAGE->
一般现在时 (Simple Present):
The program parses the data efficiently.  这个程序高效地解析数据。

The system parses user input in real time. 系统实时解析用户输入。


现在进行时 (Present Continuous):
The software is parsing the large file now.  软件正在解析这个大文件。

They are parsing the text to extract keywords.  他们正在解析文本以提取关键词。


现在完成时 (Present Perfect):
The parser has successfully parsed the document.  解析器已经成功地解析了文档。

We have parsed the data and extracted the relevant information.  我们已经解析了数据并提取了相关信息。


现在完成进行时 (Present Perfect Continuous):
The computer has been parsing the data for hours.  计算机已经解析数据好几个小时了。

They have been parsing the logs to identify the error.  他们一直在解析日志以识别错误。


一般过去时 (Simple Past):
The program parsed the input correctly.  程序正确地解析了输入。

We parsed the data yesterday. 我们昨天解析了数据。


过去进行时 (Past Continuous):
The system was parsing the file when the power went out.  断电时系统正在解析文件。

The software was parsing the data when we discovered the bug.  当我们发现这个bug时，软件正在解析数据。


过去完成时 (Past Perfect):
The program had parsed the data before displaying the results.  程序在显示结果之前已经解析了数据。

We had parsed the log files before we realized the problem. 在我们意识到问题之前，我们已经解析了日志文件。


过去完成进行时 (Past Perfect Continuous):
The system had been parsing the data for days before it finished.  系统解析数据好多天后才完成。

They had been parsing the data for weeks before finding the solution.  他们在找到解决方案之前已经解析数据好几周了。


一般将来时 (Simple Future):
The software will parse the data automatically.  软件将自动解析数据。

The system will parse the input and generate a report. 系统将解析输入并生成报告。


将来进行时 (Future Continuous):
The program will be parsing the data tomorrow.  程序明天将解析数据。

The team will be parsing the data next week. 团队下周将解析数据。


将来完成时 (Future Perfect):
By next week, the system will have parsed all the data.  到下周，系统将解析所有数据。

By tomorrow, the program will have parsed the entire file. 到明天，程序将解析整个文件。


将来完成进行时 (Future Perfect Continuous):
By the end of the month, the program will have been parsing data for three months.  到月底，程序将已经解析数据三个月了。

By next year, the system will have been parsing data for a full year.  到明年，系统将已经解析数据一整年了。


PHRASE->
easily parsable  容易解析的
machine-parsable  机器可解析的
human-parsable  人类可解析的
parsable format  可解析格式
parsable data  可解析数据
