logarithmically:/ˌlɔːɡərɪθmɪkli/ | adv. | 对数地 | In a manner relating to or expressed in terms of logarithms. | The graph shows the data plotted logarithmically.  这张图表以对数方式绘制数据。|  exponentially, geometrically | linearly, arithmetically | adverb

USAGE->

USAGE->
一般现在时 (Simple Present):
The algorithm logarithmically reduces the search space.  该算法以对数方式减少搜索空间。

The function grows logarithmically with input size. 该函数随着输入大小的对数增长。

现在进行时 (Present Continuous):
The system is logarithmically scaling its resources. 系统正在按对数比例扩展其资源。

He is plotting the data logarithmically to visualize the trend. 他正在以对数方式绘制数据以可视化趋势。

现在完成时 (Present Perfect):
The complexity of the algorithm has been logarithmically reduced. 该算法的复杂度已对数级降低。

The data has been scaled logarithmically for better analysis.  数据已按对数比例缩放以便更好地分析。


现在完成进行时 (Present Perfect Continuous):
Researchers have been logarithmically analyzing data for years. 多年来，研究人员一直在对数据进行对数分析。

They have been logarithmically adjusting the parameters to optimize performance. 他们一直在以对数方式调整参数以优化性能。


一般过去时 (Simple Past):
The engineer logarithmically transformed the variables. 工程师对变量进行了对数变换。

The data was analyzed logarithmically to reveal hidden patterns.  数据以对数方式进行分析以揭示隐藏的模式。


过去进行时 (Past Continuous):
The program was logarithmically processing the input data. 程序正在对数地处理输入数据。

They were logarithmically compressing the image to reduce file size. 他们正在以对数方式压缩图像以减小文件大小。


过去完成时 (Past Perfect):
The scientist had logarithmically plotted the data before presenting it.  科学家在展示数据之前已将其绘制成对数图。

The system had logarithmically scaled its capacity in anticipation of increased demand.  该系统已按对数比例扩展其容量以应对不断增长的需求。


过去完成进行时 (Past Perfect Continuous):
They had been logarithmically adjusting the algorithm for several weeks. 他们已经以对数方式调整算法好几个星期了。

The team had been logarithmically analyzing the results for months before a breakthrough. 在取得突破之前，团队已经对结果进行了数月的对数分析。


一般将来时 (Simple Future):
We will logarithmically scale the infrastructure to accommodate growth. 我们将对数地扩展基础设施以适应增长。

The algorithm will logarithmically reduce the processing time.  该算法将以对数方式减少处理时间。


将来进行时 (Future Continuous):
The system will be logarithmically processing the data stream in real-time. 该系统将实时地对数据流进行对数处理。

They will be logarithmically adjusting the model parameters as new data becomes available.  随着新数据的出现，他们将以对数方式调整模型参数。


将来完成时 (Future Perfect):
By next year, we will have logarithmically expanded our server capacity. 到明年，我们将按对数比例扩展我们的服务器容量。

The team will have logarithmically analyzed all the data by the end of the project. 到项目结束时，团队将对所有数据进行对数分析。


将来完成进行时 (Future Perfect Continuous):
By then, they will have been logarithmically refining the model for over a year. 到那时，他们将已经以对数方式改进了模型一年多了。

The system will have been logarithmically collecting data for a decade. 该系统将已经对数地收集数据十年了。


PHRASE->
plot logarithmically =  以对数方式绘制
scale logarithmically = 按对数比例缩放
analyze logarithmically =  对数地分析
adjust logarithmically =  对数地调整
transform logarithmically = 对数地转换
