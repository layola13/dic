non-interactive:/ˌnɒnɪntərˈæktɪv/ | adj. | 非交互式的 | Not designed for or allowing direct communication or exchange between a user and a system.  |  This is a non-interactive program; it just runs and produces output.  这是一个非交互式程序；它只是运行并产生输出。 | The lecture was non-interactive; the professor just spoke and the students listened.  讲座是非交互式的；教授只是讲，学生只是听。 | automatic, passive, one-way | interactive | adjective


USAGE->

USAGE->
一般现在时 (Simple Present):
The system non-interactively processes the data. 系统非交互式地处理数据。
This software non-interactively generates reports.  这个软件非交互式地生成报告。

现在进行时 (Present Continuous):
The program is non-interactively compiling the code.  程序正在非交互式地编译代码。
The server is non-interactively sending data. 服务器正在非交互式地发送数据。

现在完成时 (Present Perfect):
The script has non-interactively completed the task. 脚本已经非交互式地完成了任务。
The system has non-interactively processed millions of transactions.  系统已经非交互式地处理了数百万笔交易。

现在完成进行时 (Present Perfect Continuous):
The program has been non-interactively running for hours.  程序已经非交互式地运行了几个小时。
The server has been non-interactively sending data all day. 服务器一整天都在非交互式地发送数据。


一般过去时 (Simple Past):
The program non-interactively executed the command.  程序非交互式地执行了命令。
The system non-interactively generated the report yesterday. 系统昨天非交互式地生成了报告。


过去进行时 (Past Continuous):
The system was non-interactively processing data when the power went out.  断电时，系统正在非交互式地处理数据。
The computer was non-interactively running the simulation overnight.  计算机整夜都在非交互式地运行模拟。


过去完成时 (Past Perfect):
The program had non-interactively finished before I checked. 在我检查之前，程序已经非交互式地完成了。
The script had non-interactively generated the files before the error occurred.  在错误发生之前，脚本已经非交互式地生成了文件。


过去完成进行时 (Past Perfect Continuous):
The system had been non-interactively running for days before the crash.  在崩溃之前，系统已经非交互式地运行了好几天。
The server had been non-interactively transferring data for hours before the connection was lost. 在连接丢失之前，服务器已经非交互式地传输数据数小时。


一般将来时 (Simple Future):
The program will non-interactively process the data tomorrow.  程序明天将非交互式地处理数据。
The system will non-interactively generate the report next week.  系统下周将非交互式地生成报告。


将来进行时 (Future Continuous):
The system will be non-interactively processing the data overnight. 系统将在夜间非交互式地处理数据。
The server will be non-interactively sending the files tomorrow morning. 服务器明天早上将非交互式地发送文件。


将来完成时 (Future Perfect):
By next week, the system will have non-interactively processed all the data.  到下周，系统将已经非交互式地处理了所有数据。
By the end of the month, the program will have non-interactively generated all the reports. 到月底，程序将已经非交互式地生成了所有报告。


将来完成进行时 (Future Perfect Continuous):
By next year, the system will have been non-interactively running for a decade.  到明年，该系统将已经非交互式地运行了十年。
By the time you arrive, the program will have been non-interactively processing data for hours.  到你到达时，程序将已经非交互式地处理数据数小时。



PHRASE->
non-interactive mode  非交互模式
non-interactive process  非交互式进程
non-interactive script  非交互式脚本
non-interactive command  非交互式命令
non-interactive installation  非交互式安装


词源:  "non-" (not) + "interactive" (involving reciprocal action or influence).  "Interactive" ultimately derives from Latin "inter" (between) + "agere" (to do, to drive, to act).
