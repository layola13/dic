concatenates:/kənˈkætɪneɪts/ | v. | 连接，串联 | links or joins together in a series or chain. | The program concatenates the two files.  这个程序把两个文件连接起来。| join, combine, link, append, merge | separate, divide, split | verb

concatenate:/kənˈkætɪneɪt/ | v. | 连接，串联 | links or joins together in a series or chain. |  She concatenated the two strings of code. 她把两串代码连接在一起。| join, combine, link, append, merge | separate, divide, split | verb

concatenation:/kənˌkætɪˈneɪʃn/ | n. | 连接，串联 | the action of linking things together in a series. | The concatenation of the two data sets created a more comprehensive view. 这两个数据集的连接创建了一个更全面的视图。| combination, linking, joining, merging, union | separation, division, splitting | noun


USAGE->

一般现在时 (Simple Present):
The system concatenates the files automatically. 系统自动连接文件。
He often concatenates different data sources for his research. 他经常为他的研究连接不同的数据源。

现在进行时 (Present Continuous):
The program is concatenating the files now. 程序现在正在连接文件。
She is concatenating the strings of text as we speak.  在我们说话的时候，她正在连接文本字符串。

现在完成时 (Present Perfect):
The software has concatenated all the necessary files. 软件已经连接了所有必要的文件。
He has concatenated the data from multiple sources. 他已经连接了来自多个来源的数据。

现在完成进行时 (Present Perfect Continuous):
The system has been concatenating files for the past hour. 系统过去一小时一直在连接文件。
She has been concatenating data strings all morning. 她整个上午都在连接数据字符串。

一般过去时 (Simple Past):
The program concatenated the two files successfully. 程序成功地连接了两个文件。
He concatenated the data before analyzing it. 他在分析数据之前先连接了数据。

过去进行时 (Past Continuous):
The computer was concatenating the files when the power went out.  断电时，计算机正在连接文件。
She was concatenating the strings when she was interrupted.  她正在连接字符串时被打断了。

过去完成时 (Past Perfect):
He had concatenated the data before he realized there was an error. 他连接数据之后才意识到有一个错误。
The system had concatenated the files before the crash. 系统在崩溃之前已经连接了文件。

过去完成进行时 (Past Perfect Continuous):
She had been concatenating data for hours before she took a break. 她连续连接数据几个小时后才休息了一下。
The program had been concatenating files all night. 程序整晚都在连接文件。

一般将来时 (Simple Future):
The software will concatenate the files tomorrow. 软件明天会连接文件。
The script will concatenate the strings in the next step. 脚本将在下一步连接字符串。

将来进行时 (Future Continuous):
The system will be concatenating files during the maintenance window. 系统将在维护窗口期间连接文件。
She will be concatenating the data strings tomorrow morning. 她明天早上将连接数据字符串。

将来完成时 (Future Perfect):
By next week, the program will have concatenated all the required data. 到下周，程序将连接所有必需的数据。
He will have concatenated the files by the end of the day. 他将在今天结束之前连接文件。

将来完成进行时 (Future Perfect Continuous):
By next year, the system will have been concatenating data for five years. 到明年，系统将已经连接数据五年了。
She will have been concatenating strings for several hours by the time the process completes. 到这个过程完成时，她将已经连接字符串几个小时了。



PHRASE->
concatenate strings  连接字符串
concatenate files 连接文件
concatenate data 连接数据
concatenate lists 连接列表
concatenate arrays 连接数组
