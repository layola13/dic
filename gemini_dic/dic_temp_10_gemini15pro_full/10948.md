arrays:/əˈreɪz/| n.|数组，阵列|A data structure that stores a collection of elements of the same data type.|Example: The program stores the data in arrays.  程序将数据存储在数组中。|Example:  He used arrays to organize the experimental data. 他使用数组来组织实验数据。|近义词：matrix, table, list, vector|反义词: (No direct antonym for a data structure)|词性:noun


USAGE->

一般现在时 (Simple Present):
What type of arrays do you usually use? = 你通常使用什么类型的数组？
An array stores elements of the same data type. = 数组存储相同数据类型的元素。

现在进行时 (Present Continuous):
He is currently working with multi-dimensional arrays. = 他目前正在使用多维数组。
They are accessing the elements of the array using an index. = 他们正在使用索引访问数组的元素。

现在完成时 (Present Perfect):
We have already initialized the array with default values. = 我们已经用默认值初始化了数组。
She has successfully implemented a sorting algorithm for the array. = 她已经成功地为数组实现了一个排序算法。

现在完成进行时 (Present Perfect Continuous):
They have been studying different types of arrays for weeks. = 他们已经学习不同类型的数组好几个星期了。
He has been debugging the array manipulation code all day. = 他一整天都在调试数组操作代码。

一般过去时 (Simple Past):
The program crashed because the array index was out of bounds. = 程序崩溃是因为数组索引超出了界限。
She created a new array to store the processed data. = 她创建了一个新数组来存储处理后的数据。

过去进行时 (Past Continuous):
I was trying to access an element outside the array's bounds. = 我试图访问数组边界之外的元素。
They were discussing the advantages of using dynamic arrays. = 他们正在讨论使用动态数组的优点。

过去完成时 (Past Perfect):
Before I realized the error, the program had already overwritten the array. = 在我意识到错误之前，程序已经覆盖了数组。
He had initialized the array with incorrect values, causing the program to malfunction. = 他用不正确的值初始化了数组，导致程序出现故障。

过去完成进行时 (Past Perfect Continuous):
We had been working with arrays for months before we encountered this problem. = 在遇到这个问题之前，我们已经使用数组好几个月了。
She had been trying to optimize the array access code for several days. = 她一直试图优化数组访问代码好几天了。

一般将来时 (Simple Future):
We will use a two-dimensional array to represent the game board. = 我们将使用二维数组来表示游戏面板。
The function will return an array containing the sorted values. = 该函数将返回一个包含排序值的数组。


将来进行时 (Future Continuous):
Tomorrow at this time, I will be working on optimizing the array search algorithm. = 明天这个时候，我将致力于优化数组搜索算法。
They will be implementing a new data structure using arrays next week. = 下周他们将使用数组实现一个新的数据结构。

将来完成时 (Future Perfect):
By the end of the project, we will have implemented all the necessary array operations. = 到项目结束时，我们将实现所有必要的数组操作。
He will have finished debugging the array code by tomorrow morning. = 到明天早上，他将完成数组代码的调试。


将来完成进行时 (Future Perfect Continuous):
By next year, they will have been using arrays in their projects for five years. = 到明年，他们将在项目中使用数组五年了。
She will have been studying data structures and algorithms, including arrays, for two semesters. = 到那时，她将学习数据结构和算法（包括数组）两个学期了。



虚拟语气:
If we had used a larger array, we wouldn't have encountered this overflow error. = 如果我们使用更大的数组，就不会遇到这个溢出错误。
I wish I knew a more efficient way to sort this array. = 我希望我知道一种更有效的方法来排序这个数组。


被动语态:
The array was initialized with random values. = 数组被初始化为随机值。
The elements of the array are being accessed sequentially. = 数组的元素正在被顺序访问。

疑问句:
What is the size of the array? = 数组的大小是多少？
How can I access the last element of the array? = 如何访问数组的最后一个元素？

否定句:
The array does not contain the value you are searching for. = 数组不包含您要搜索的值。
We did not allocate enough memory for the array. = 我们没有为数组分配足够的内存。


PHRASE->
array of = 一系列/大量的
access an array = 访问数组
create an array = 创建数组
initialize an array = 初始化数组
sort an array = 排序数组
