initialised:/ɪˈnɪʃəlaɪzd/ |v. |初始化，设定初始值|To set up for the first time; to give a starting value to a variable or system.|The program initialised the variables to zero. 程序将变量初始化为零。|The system was initialised with the default settings. 系统使用默认设置初始化。|近义词：initialized (US spelling), set up, configure, prepare, boot, start, launch|反义词：reset, clear, terminate|Verb

USAGE->

一般现在时 (Simple Present):
The program initializes the variables automatically. 程序自动初始化变量。
He initializes the system every morning. 他每天早上初始化系统。

现在进行时 (Present Continuous):
The system is initializing now, please wait. 系统正在初始化，请稍候。
They are initializing the new software. 他们正在初始化新软件。

现在完成时 (Present Perfect):
The program has initialized successfully. 程序已成功初始化。
Have you initialized the database yet? 你初始化数据库了吗？

现在完成进行时 (Present Perfect Continuous):
The system has been initializing for a few minutes. 系统已经初始化几分钟了。
They have been initializing the servers all day. 他们一整天都在初始化服务器。

一般过去时 (Simple Past):
The technician initialized the system yesterday. 技术员昨天初始化了系统。
We initialized the program with the correct parameters. 我们使用正确的参数初始化了程序。

过去进行时 (Past Continuous):
The system was initializing when the power went out. 断电时系统正在初始化。
While we were initializing the system, we found a bug. 当我们初始化系统时，发现了一个错误。

过去完成时 (Past Perfect):
After he had initialized the program, he started testing. 初始化程序后，他开始测试。
The system had initialized before the error occurred. 错误发生之前系统已经初始化。

过去完成进行时 (Past Perfect Continuous):
They had been initializing the server for hours before they found the problem. 他们在发现问题之前已经初始化服务器几个小时了。
She had been initializing the database all morning before the meeting. 开会前她整个早上都在初始化数据库。

一般将来时 (Simple Future):
The program will initialize automatically when you start it. 启动时程序将自动初始化。
We will initialize the system tomorrow. 我们明天将初始化系统。

将来进行时 (Future Continuous):
The system will be initializing during the installation process. 系统将在安装过程中初始化。
We will be initializing the new servers next week. 我们下周将初始化新的服务器。

将来完成时 (Future Perfect):
By tomorrow, the system will have initialized completely. 到明天，系统将完全初始化。
We will have initialized all the servers by the end of the week. 到本周末，我们将初始化所有服务器。

将来完成进行时 (Future Perfect Continuous):
By the time you arrive, the system will have been initializing for two hours. 你到达时，系统将已经初始化两个小时了。
They will have been initializing the database for a week by next Friday. 到下周五，他们将已经初始化数据库一周了。



PHRASE->
initialise with (用...初始化):  The variables were initialised with default values. 变量用默认值初始化。
fully initialised (完全初始化):  The system is fully initialised and ready to use. 系统已完全初始化并可以使用。
initialise to (初始化为): The counter was initialised to zero. 计数器被初始化为零。
re-initialise (重新初始化):  We had to re-initialise the system after the crash. 崩溃后我们不得不重新初始化系统。
initialization process (初始化过程): The initialization process took several minutes. 初始化过程花费了几分钟。
