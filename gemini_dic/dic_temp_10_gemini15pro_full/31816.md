checksum:/ˈtʃɛksʌm/| n.|校验和|A number produced by a checksum algorithm that is used to check the integrity of a file or data transfer.  It's like a digital fingerprint for data.|例句：The checksum didn't match, indicating a corrupted download. 校验和不匹配，表明下载的文件已损坏。

例句：Before installing the software, verify the checksum to ensure it hasn't been tampered with. 在安装软件之前，请验证校验和以确保它没有被篡改。|近义词：hash, hash value, CRC (Cyclic Redundancy Check)|反义词: N/A|词性:noun


USAGE->

USAGE->
一般现在时 (Simple Present):
The checksum verifies the integrity of the data. 校验和验证数据的完整性。
This program calculates the checksum automatically.  这个程序自动计算校验和。


现在进行时 (Present Continuous):
The system is calculating the checksum now. 系统正在计算校验和。
We are verifying the checksum of the downloaded file. 我们正在验证下载文件的校验和。


现在完成时 (Present Perfect):
The download has finished, and the checksum has been calculated. 下载已完成，校验和已计算。
Have you verified the checksum yet? 你验证了校验和了吗？


现在完成进行时 (Present Perfect Continuous):
The computer has been calculating the checksum for the past hour. 计算机过去一个小时一直在计算校验和。
They have been verifying the checksums of all the files.  他们一直在验证所有文件的校验和。


一般过去时 (Simple Past):
The checksum didn't match, so I downloaded the file again. 校验和不匹配，所以我重新下载了文件。
I calculated the checksum before installing the software.  我在安装软件之前计算了校验和。


过去进行时 (Past Continuous):
I was verifying the checksum when the power went out.  我正在验证校验和时断电了。
He was calculating the checksum of the large file. 他正在计算大文件的校验和。


过去完成时 (Past Perfect):
After the download had finished, I verified the checksum. 下载完成后，我验证了校验和。
I had already calculated the checksum before he asked me to. 在他让我计算之前，我已经计算了校验和。


过去完成进行时 (Past Perfect Continuous):
I had been verifying checksums for hours before I found the error. 在我发现错误之前，我已经验证了几个小时的校验和。
They had been calculating checksums all day. 他们一整天都在计算校验和。


一般将来时 (Simple Future):
I will verify the checksum after the download completes. 下载完成后，我将验证校验和。
The program will calculate the checksum automatically. 程序将自动计算校验和。


将来进行时 (Future Continuous):
I will be verifying the checksum tomorrow morning. 我明天早上将验证校验和。
The system will be calculating the checksum during the installation. 系统将在安装过程中计算校验和。


将来完成时 (Future Perfect):
By the end of the day, I will have verified all the checksums. 到今天结束时，我将已经验证了所有校验和。
The program will have calculated the checksum by then. 到那时，程序将已经计算了校验和。


将来完成进行时 (Future Perfect Continuous):
By next week, I will have been verifying checksums for a month. 到下周，我将已经验证校验和一个月了。
The system will have been calculating the checksum for several hours. 系统将已经计算校验和几个小时了。


虚拟语气:
If the checksum were different, I would know the file was corrupted. 如果校验和不同，我就知道文件已损坏。
If I had verified the checksum, I would have avoided the problem. 如果我验证了校验和，我就避免了这个问题。


被动语态:
The checksum is calculated by the program. 校验和由程序计算。
The checksum was verified after the download. 校验和在下载后被验证。


疑问句:
What is the checksum of this file? 这个文件的校验和是多少？
Did you verify the checksum? 你验证了校验和吗？


否定句:
The checksum doesn't match. 校验和不匹配。
I didn't verify the checksum. 我没有验证校验和。



PHRASE->
generate a checksum 生成校验和
verify a checksum 验证校验和
checksum algorithm 校验和算法
match the checksum 匹配校验和
calculate the checksum 计算校验和
