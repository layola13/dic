outputted:/ˈaʊtpʊtɪd/ | v. | 输出的；已输出的 |  produced or delivered as output. | The data was outputted to a file. 数据被输出到一个文件。 | emitted, produced, delivered | inputted | verb

ETYMOLOGY->
From output + -ed. Output itself comes from "out" + "put".

USAGE->

一般现在时 (Simple Present):
The system outputs data every hour. 系统每小时输出数据。
The printer outputs high-quality images. 这台打印机输出高质量的图像。

现在进行时 (Present Continuous):
The computer is outputting the results now. 计算机正在输出结果。
The program is outputting a series of error messages. 程序正在输出一系列错误消息。

现在完成时 (Present Perfect):
The system has outputted all the necessary information. 系统已经输出了所有必要的信息。
The program has outputted the results to a file. 程序已将结果输出到文件。

现在完成进行时 (Present Perfect Continuous):
The machine has been outputting data continuously for three hours. 机器已经连续输出数据三个小时了。
The software has been outputting error messages all morning.  软件整个早上都在输出错误消息。


一般过去时 (Simple Past):
The system outputted the data yesterday. 系统昨天输出了数据。
The program outputted an error message. 程序输出了一个错误消息。

过去进行时 (Past Continuous):
The computer was outputting the results when the power went out.  断电时，计算机正在输出结果。
The printer was outputting the document when it ran out of ink. 打印机在墨水用完时正在输出文档。

过去完成时 (Past Perfect):
The system had already outputted the data before I arrived. 在我到达之前，系统已经输出了数据。
The program had outputted an error message before crashing. 程序在崩溃之前输出了一条错误消息。

过去完成进行时 (Past Perfect Continuous):
The machine had been outputting data for hours before it malfunctioned. 机器故障前已经输出了好几个小时的数据。
The program had been outputting error messages for a while before we noticed the problem. 在我们注意到问题之前，程序已经输出了一段时间的错误消息。


一般将来时 (Simple Future):
The system will output the data tomorrow. 系统明天将输出数据。
The program will output the results to a screen. 程序将把结果输出到屏幕上。

将来进行时 (Future Continuous):
The computer will be outputting the results this time tomorrow. 明天这个时候，计算机将正在输出结果。
The printer will be outputting the report later this afternoon. 今天下午晚些时候，打印机将正在输出报告。

将来完成时 (Future Perfect):
By next week, the system will have outputted all the data. 到下周，系统将已经输出了所有数据。
By the end of the day, the program will have outputted the results. 到今天结束时，程序将已经输出了结果。

将来完成进行时 (Future Perfect Continuous):
By next month, the system will have been outputting data for a year. 到下个月，系统将已经输出数据一年了。
By the end of the project, the team will have been outputting reports weekly for six months. 到项目结束时，团队将已经每周输出报告六个月了。


PHRASE->
outputted to a file = 输出到文件
outputted to a printer = 输出到打印机
outputted to a screen = 输出到屏幕
outputted as text = 输出为文本
outputted as an image = 输出为图像
