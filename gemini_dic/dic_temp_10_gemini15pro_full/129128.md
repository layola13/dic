nlink:/ˈenlɪŋk/ | n. | 文件链接数 | The number of hard links to a file.  This count keeps track of how many directories have entries for this file. |  The `nlink` value of the file is 3.  该文件的`nlink`值为 3. | synonyms: link count | antonyms: N/A | noun


USAGE->

一般现在时 (Simple Present):
The file has an nlink of 2.  该文件有一个 nlink 值为 2。
Every file starts with an nlink of 1. 每个文件都以 nlink 值 1 开始。

现在进行时 (Present Continuous):
The system is updating the nlink as we create new hard links.  当我们创建新的硬链接时，系统正在更新 nlink 值。
The nlink is increasing as more directories reference the file.  随着更多目录引用该文件，nlink 值正在增加。

现在完成时 (Present Perfect):
The nlink has changed since we created the hard link. 自从我们创建了硬链接以来，nlink 值已经改变了。
The system has updated the nlink automatically. 系统已自动更新 nlink 值。

现在完成进行时 (Present Perfect Continuous):
The nlink has been increasing steadily as we add more links. 随着我们添加更多链接，nlink 值一直在稳步增加。
The system has been tracking the nlink for this file since its creation. 自创建以来，系统一直在跟踪此文件的 nlink 值。

一般过去时 (Simple Past):
The nlink was initially 1.  最初 nlink 值为 1。
We checked the nlink before deleting the file.  在删除文件之前，我们检查了 nlink 值。

过去进行时 (Past Continuous):
The nlink was increasing while we were creating the hard links.  当我们创建硬链接时，nlink 值正在增加。
The system was updating the nlink as we added more directories.  当我们添加更多目录时，系统正在更新 nlink 值。

过去完成时 (Past Perfect):
The nlink had already changed before we noticed.  在我们注意到之前，nlink 值已经改变了。
The system had updated the nlink before we deleted the directory.  在我们删除目录之前，系统已经更新了 nlink 值。

过去完成进行时 (Past Perfect Continuous):
The nlink had been decreasing as we removed hard links.  当我们删除硬链接时，nlink 值一直在减少。
The system had been tracking the nlink changes for several days.  系统已经跟踪 nlink 值的变化好几天了。

一般将来时 (Simple Future):
The nlink will decrease if we delete the original file.  如果我们删除原始文件，nlink 值将会减少。
The nlink will update when we create a new hard link.  当我们创建一个新的硬链接时，nlink 值将会更新。

将来进行时 (Future Continuous):
The system will be updating the nlink automatically as we add more links. 当我们添加更多链接时，系统将自动更新 nlink 值。
The nlink will be changing as we modify the directory structure. 当我们修改目录结构时，nlink 值将会改变。

将来完成时 (Future Perfect):
By tomorrow, the nlink will have changed several times. 到明天，nlink 值将已经改变了好几次。
The system will have updated the nlink by the time we check it again. 在我们再次检查之前，系统将已经更新了 nlink 值。

将来完成进行时 (Future Perfect Continuous):
By next week, the system will have been tracking the nlink changes for a month. 到下周，系统将已经跟踪 nlink 值的变化一个月了。
The nlink will have been fluctuating as we modify the file system. 当我们修改文件系统时，nlink 值将一直在波动。


PHRASE->
check the nlink  检查链接数
increase the nlink 增加链接数
decrease the nlink 减少链接数
update the nlink 更新链接数
the nlink of a file 文件的链接数
