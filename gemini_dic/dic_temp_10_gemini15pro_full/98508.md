instantiated:/ɪnˈstænʃieɪtɪd/ | v. | 实例化；具体化 | To represent (an abstract concept) by a concrete or particular instance. | The class 'Animal' is instantiated to create individual animals like 'dog' and 'cat'.  动物类“Animal”被实例化以创建像“狗”和“猫”这样的个体动物。| synonyms: exemplified, represented, materialized, embodied | antonyms: abstract, generalized | verb

instantiates:/ɪnˈstænʃieɪeɪts/ | v. | 实例化；具体化 | Third-person singular present tense form of "instantiate". | He instantiates the theory with a real-world example. 他用一个真实的例子来实例化这个理论。 | synonyms: exemplifies, represents, materializes, embodies | antonyms: abstracts, generalizes | verb

instantiate:/ɪnˈstænʃieɪt/ | v. | 实例化；具体化 | To represent (an abstract concept) by a concrete or particular instance. | You can instantiate this class to create a new object. 你可以实例化这个类来创建一个新的对象。 | synonyms: exemplify, represent, materialize, embody | antonyms: abstract, generalize | verb

instantiation:/ɪnˌstænʃiˈeɪʃən/ | n. | 实例化；具体化 | The act of instantiating; an instance of something. |  The instantiation of this object requires several parameters.  这个对象的实例化需要几个参数。| synonyms: example, illustration, embodiment | antonyms: abstraction, generalization | noun


USAGE->

一般现在时 (Simple Present):
The program instantiates a new object every time it runs.  每次运行程序都会实例化一个新对象。
He instantiates the concept of bravery with his actions. 他用他的行动诠释了勇敢的概念。


现在进行时 (Present Continuous):
The system is instantiating a new virtual machine. 系统正在实例化一个新的虚拟机。
He is instantiating the principles he learned in class. 他正在实践他在课堂上学到的原则。


现在完成时 (Present Perfect):
The developer has instantiated several objects in the code.  开发人员已经在代码中实例化了几个对象。
She has instantiated a new design pattern in her project. 她已经在她的项目中实例化了一个新的设计模式。


现在完成进行时 (Present Perfect Continuous):
They have been instantiating different versions of the software. 他们一直在实例化不同版本的软件。
We have been instantiating new user profiles all day.  我们一整天都在实例化新的用户配置文件。


一般过去时 (Simple Past):
The programmer instantiated the class to create an object. 程序员实例化了该类以创建一个对象。
She instantiated a new variable for the calculation.  她为计算实例化了一个新变量。


过去进行时 (Past Continuous):
The system was instantiating a new database when the power went out.  断电时，系统正在实例化一个新的数据库。
He was instantiating the example from the textbook. 他正在实例化课本中的例子。


过去完成时 (Past Perfect):
The program had instantiated the object before the error occurred.  在错误发生之前，程序已经实例化了该对象。
They had instantiated a test environment before deploying the code.  在部署代码之前，他们已经实例化了一个测试环境。


过去完成进行时 (Past Perfect Continuous):
We had been instantiating virtual machines for hours before we found the issue.  我们一直在实例化虚拟机好几个小时才发现问题。
She had been instantiating different algorithms before settling on the best one.  在确定最佳算法之前，她一直在实例化不同的算法。


一般将来时 (Simple Future):
The software will instantiate a new window upon launch. 软件启动时将实例化一个新窗口。
The system will instantiate a new user profile for every registration. 每次注册，系统都会实例化一个新的用户配置文件。


将来进行时 (Future Continuous):
The program will be instantiating new objects in the background. 程序将在后台实例化新的对象。
The server will be instantiating new connections as needed. 服务器将根据需要实例化新的连接。


将来完成时 (Future Perfect):
By the end of the week, we will have instantiated all the necessary components.  到本周末，我们将实例化所有必要的组件。
By tomorrow, she will have instantiated a working prototype. 到明天，她将实例化一个工作原型。


将来完成进行时 (Future Perfect Continuous):
By next year, they will have been instantiating these objects for over a decade.  到明年，他们将已经实例化这些对象十多年了。
By the time you arrive, we will have been instantiating the server for several hours.  到你到达时，我们将已经实例化服务器几个小时了。


PHRASE->
instantiate an object  实例化一个对象
instantiate a class 实例化一个类
instantiate a variable 实例化一个变量
instantiate a process 实例化一个进程
instantiate a model 实例化一个模型
