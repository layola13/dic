initialising:/ɪˈnɪʃəlaɪzɪŋ/| v.|初始化，设定初始值|The program is initialising. 程序正在初始化。|例句：The system is initialising, please wait. 系统正在初始化，请稍候。|近义词：setting up, booting up, starting, configuring|反义词：terminating, shutting down, closing, ending|Verb - present participle

USAGE->

USAGE->
一般现在时 (Simple Present):
The program initialises automatically.  程序自动初始化。
He initialises the variables before each run. 他在每次运行前初始化变量。

现在进行时 (Present Continuous):
The system is initialising right now. 系统正在初始化。
The computer is initialising the operating system. 电脑正在初始化操作系统。

现在完成时 (Present Perfect):
The program has initialised successfully. 程序已成功初始化。
The system has initialised all the necessary components. 系统已经初始化了所有必要的组件。

现在完成进行时 (Present Perfect Continuous):
The server has been initialising for several minutes. 服务器已经初始化几分钟了。
The application has been initialising slowly due to network issues. 由于网络问题，应用程序一直在缓慢初始化。

一般过去时 (Simple Past):
The system initialised without any errors. 系统初始化没有出现任何错误。
The technician initialised the new hardware. 技术人员初始化了新的硬件。

过去进行时 (Past Continuous):
The computer was initialising when the power went out. 断电时，计算机正在初始化。
While the program was initialising, I checked my email. 程序初始化的时候，我查看了我的电子邮件。

过去完成时 (Past Perfect):
The system had initialised before I started working. 在我开始工作之前，系统已经初始化了。
The program had initialised all the variables before executing the main function. 在执行主函数之前，程序已经初始化了所有变量。

过去完成进行时 (Past Perfect Continuous):
The server had been initialising for an hour before the error occurred.  在错误发生之前，服务器已经初始化了一个小时。
The software had been initialising for quite some time when it finally finished. 软件初始化了很长一段时间后终于完成了。

一般将来时 (Simple Future):
The system will initialise automatically upon startup. 系统将在启动时自动初始化。
The program will initialise the database connection first. 程序将首先初始化数据库连接。

将来进行时 (Future Continuous):
The computer will be initialising the new settings tomorrow morning. 明天早上，计算机将初始化新的设置。
The server will be initialising during the maintenance window. 服务器将在维护窗口期间初始化。

将来完成时 (Future Perfect):
By the time you arrive, the system will have initialised. 等你到达时，系统将已经初始化完成。
The program will have initialised all the required modules by the end of the process. 在进程结束时，程序将已经初始化了所有必需的模块。

将来完成进行时 (Future Perfect Continuous):
By next week, the server will have been initialising for a full week. 到下周，服务器将已经初始化整整一周了。
The application will have been initialising for several hours by the time you get back. 等你回来的时候，应用程序将已经初始化几个小时了。


虚拟语气:
If the system were initialising correctly, we wouldn't have this problem. 如果系统正确初始化，我们就不会有这个问题。
I wish the program would initialise faster. 我希望程序初始化更快一些。


被动语态:
The system was initialised by the administrator. 系统由管理员初始化。
The variables are being initialised by the program. 变量正在被程序初始化。


疑问句:
Is the system initialising properly? 系统是否正在正确初始化？
Why is the program taking so long to initialise? 为什么程序初始化需要这么长时间？


否定句:
The program did not initialise correctly. 程序没有正确初始化。
The system is not initialising at all. 系统根本没有初始化。


PHRASE->
initialising sequence  初始化顺序
initialising process  初始化过程
initialising parameters 初始化参数
initialising routine  初始化例程
initialising stage 初始化阶段
