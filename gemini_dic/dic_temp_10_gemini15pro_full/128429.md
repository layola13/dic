newline:/ˈnjuːlaɪn/ |n.|换行符；新行|A newline character is a control character or sequence of control characters that starts a new line of text.|The code inserts a newline after each entry.| 近义词: line break, carriage return | 反义词: N/A |词性:名词


USAGE->

一般现在时 (Simple Present):
The system adds a newline after each command. = 系统在每个命令后添加一个换行符。
The program automatically inserts a newline. = 程序自动插入换行符。

现在进行时 (Present Continuous):
The software is inserting a newline after each paragraph. =  该软件正在每个段落后插入换行符。
The printer is adding a newline at the end of every line. = 打印机正在每行末尾添加换行符。

现在完成时 (Present Perfect):
The editor has inserted a newline at the wrong place. = 编辑器在错误的地方插入了一个换行符。
The program has automatically added a newline. = 程序自动添加了一个换行符。

现在完成进行时 (Present Perfect Continuous):
The system has been adding newlines after every sentence. = 系统一直在每个句子后添加换行符。
The software has been inserting newlines inconsistently. = 该软件一直在不一致地插入换行符。

一般过去时 (Simple Past):
The code added a newline after each data entry. = 代码在每个数据条目后添加了一个换行符。
The script inserted a newline at the end of the file. = 脚本在文件末尾插入了一个换行符。

过去进行时 (Past Continuous):
The program was adding newlines after every record. = 程序正在每个记录后添加换行符。
The system was inserting a newline whenever it encountered a specific character. =  系统遇到特定字符时都会插入换行符。

过去完成时 (Past Perfect):
The code had added a newline before I realized the error. = 代码已经添加了一个换行符，然后我才意识到错误。
The script had inserted unnecessary newlines throughout the document. = 脚本在整个文档中插入了不必要的换行符。

过去完成进行时 (Past Perfect Continuous):
The program had been adding newlines incorrectly for hours. = 程序几个小时以来一直在错误地添加换行符。
The system had been inserting newlines at random intervals. = 系统一直在随机间隔插入换行符。

一般将来时 (Simple Future):
The update will add a newline after every header. = 更新将在每个标题后添加一个换行符。
The new version will insert a newline automatically. = 新版本将自动插入换行符。

将来进行时 (Future Continuous):
The system will be adding newlines after each section. = 系统将在每个部分后添加换行符。
The program will be inserting newlines according to the specified format. = 程序将根据指定的格式插入换行符。


将来完成时 (Future Perfect):
By tomorrow, the script will have added all the necessary newlines. = 到明天，脚本将添加所有必要的换行符。
The program will have inserted the newline before the file is saved. = 程序将在保存文件之前插入换行符。


将来完成进行时 (Future Perfect Continuous):
By the end of the week, the system will have been adding newlines consistently for several days. = 到本周末，系统将连续几天持续添加换行符。
The program will have been inserting newlines according to the user’s preferences for a long time. = 程序将根据用户的偏好长时间插入换行符。


PHRASE->
insert a newline = 插入换行符
add a newline = 添加换行符
newline character = 换行符
remove a newline = 删除换行符
a newline break = 换行符中断
