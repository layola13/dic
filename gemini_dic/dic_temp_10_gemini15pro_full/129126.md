nle:/ɛn ɛl iː/ |abbr.| negative log-likelihood|负对数似然|NLE is a common metric used in machine learning.  负对数似然是机器学习中常用的一个指标。|Synonym: negative log likelihood|Antonym: None|Noun

USAGE->

一般现在时 (Simple Present):
The NLE measures the goodness of fit of a statistical model.  负对数似然衡量统计模型的拟合优度。

The NLE is often used in training machine learning models. 负对数似然常用于训练机器学习模型。


现在进行时 (Present Continuous):
The researchers are using NLE to evaluate their model's performance. 研究人员正在使用负对数似然来评估他们的模型性能。

We are minimizing the NLE during the training process.  我们在训练过程中正在最小化负对数似然。


现在完成时 (Present Perfect):
The team has successfully minimized the NLE of their model.  团队已经成功地最小化了他们模型的负对数似然。

She has used NLE in several of her research projects. 她已经在她的几个研究项目中使用了负对数似然。


现在完成进行时 (Present Perfect Continuous):
They have been working on reducing the NLE for weeks.  他们已经花了数周时间来降低负对数似然。

He has been studying the use of NLE in deep learning. 他一直在研究负对数似然在深度学习中的应用。


一般过去时 (Simple Past):
We used NLE to compare different models. 我们使用负对数似然来比较不同的模型。

The researchers calculated the NLE of the model. 研究人员计算了模型的负对数似然。


过去进行时 (Past Continuous):
They were analyzing the NLE when the power went out.  断电时，他们正在分析负对数似然。

She was calculating the NLE of the model yesterday. 她昨天正在计算模型的负对数似然。


过去完成时 (Past Perfect):
We had already calculated the NLE before we realized the error. 在我们意识到错误之前，我们已经计算了负对数似然。

They had implemented NLE in their system before the update. 他们在更新之前已经在他们的系统中实现了负对数似然。


过去完成进行时 (Past Perfect Continuous):
They had been trying to minimize the NLE for months before they found a solution.  在找到解决方案之前，他们已经尝试了几个月来最小化负对数似然。

She had been studying NLE for years before she started using it in her research.  在她开始在研究中使用负对数似然之前，她已经研究了多年。


一般将来时 (Simple Future):
We will use NLE to evaluate the new model.  我们将使用负对数似然来评估新的模型。

The researchers will compare the NLE of different algorithms. 研究人员将比较不同算法的负对数似然。


将来进行时 (Future Continuous):
We will be using NLE to monitor the model's performance. 我们将使用负对数似然来监控模型的性能。

They will be analyzing the NLE during the testing phase.  他们将在测试阶段分析负对数似然。


将来完成时 (Future Perfect):
By next week, we will have implemented NLE in our system. 到下周，我们将在我们的系统中实现负对数似然。

By the end of the month, they will have finished calculating the NLE.  到月底，他们将完成负对数似然的计算。


将来完成进行时 (Future Perfect Continuous):
By next year, we will have been using NLE for over a decade. 到明年，我们将已经使用负对数似然超过十年了。

By the time the project is finished, they will have been studying NLE for several years.  到项目完成时，他们将已经研究负对数似然好几年了。


PHRASE->
minimize NLE  最小化负对数似然
calculate NLE  计算负对数似然
reduce NLE  降低负对数似然
NLE loss  负对数似然损失
NLE metric  负对数似然指标
