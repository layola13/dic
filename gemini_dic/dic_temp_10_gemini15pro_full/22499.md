boolean:/ˈbuːliən/| adj. n.|布尔型的，布尔值|A data type having two possible values (usually denoted true and false), intended to represent the truth values of logic and Boolean algebra.  Relating to or involving the use of Boolean algebra. |Example: The boolean variable 'isValid' is set to true. 布尔变量“isValid”设置为真。|Synonym: logical, binary|Antonym: N/A|Adjective, Noun

USAGE->

一般现在时 (Simple Present):
The function returns a boolean value.  这个函数返回一个布尔值。
Boolean logic is fundamental to computer science. 布尔逻辑是计算机科学的基础。

现在进行时 (Present Continuous):
The program is checking a boolean condition. 程序正在检查一个布尔条件。
We are using boolean operators to filter the data. 我们正在使用布尔运算符来过滤数据。

现在完成时 (Present Perfect):
The system has evaluated the boolean expression. 系统已经计算了布尔表达式。
He has worked with boolean variables before. 他以前使用过布尔变量。


现在完成进行时 (Present Perfect Continuous):
They have been studying boolean algebra for weeks. 他们学习布尔代数已经好几个星期了。
The computer has been processing boolean data all day.  计算机一整天都在处理布尔数据。

一般过去时 (Simple Past):
The function returned a boolean value. 函数返回了一个布尔值。
We used boolean operators in the query. 我们在查询中使用了布尔运算符。


过去进行时 (Past Continuous):
The system was evaluating a boolean expression when it crashed. 系统崩溃时正在计算一个布尔表达式。
He was working with boolean variables when I interrupted him. 当我打断他时，他正在使用布尔变量。


过去完成时 (Past Perfect):
The program had checked the boolean condition before proceeding. 程序在继续之前已经检查了布尔条件。
She had studied boolean algebra in college.  她在大学里学习过布尔代数。


过去完成进行时 (Past Perfect Continuous):
They had been debugging the boolean logic for hours. 他们已经调试布尔逻辑好几个小时了。
The computer had been processing boolean data for a long time before it finished. 计算机在完成之前已经处理了很长时间的布尔数据。


一般将来时 (Simple Future):
The function will return a boolean result.  函数将返回一个布尔结果。
We will use boolean algebra to simplify the logic. 我们将使用布尔代数来简化逻辑。


将来进行时 (Future Continuous):
The program will be checking a boolean condition at that time. 程序将在那时检查一个布尔条件。
They will be working with boolean variables tomorrow. 他们明天将使用布尔变量。

将来完成时 (Future Perfect):
By next week, the system will have evaluated all boolean expressions. 到下周，系统将计算所有布尔表达式。
He will have finished the boolean logic module by then. 到那时，他将完成布尔逻辑模块。

将来完成进行时 (Future Perfect Continuous):
By the end of the month, they will have been studying boolean algebra for a semester. 到月底，他们将学习布尔代数一个学期了。
The computer will have been processing boolean data for days by the time it completes. 计算机在完成之前将已经处理了数天的布尔数据。



PHRASE->
boolean value 布尔值
boolean expression 布尔表达式
boolean logic 布尔逻辑
boolean operator 布尔运算符
boolean algebra 布尔代数
