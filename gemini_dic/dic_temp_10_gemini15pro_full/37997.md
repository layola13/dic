compiler:/kəmˈpaɪlər/| n.|编译器|A program that translates source code written in a high-level language (like Java or C++) into a set of machine-language instructions that can be understood by a digital computer's CPU.|The compiler transformed the code into an executable file.|近义词:translator|反义词:interpreter|词性:noun


USAGE->

一般现在时 (Simple Present):
The compiler translates source code into machine code.
编译器将源代码翻译成机器代码。

The compiler checks for syntax errors.
编译器检查语法错误。


现在进行时 (Present Continuous):
The compiler is processing the code right now.
编译器正在处理代码。

The compiler is generating an executable file.
编译器正在生成可执行文件。


现在完成时 (Present Perfect):
The compiler has finished compiling the program.
编译器已经完成了程序的编译。

The compiler has detected several errors.
编译器检测到了几个错误。


现在完成进行时 (Present Perfect Continuous):
The compiler has been working on this large project for hours.
编译器已经在这个大型项目上工作了几个小时。

The compiler has been optimizing the code for better performance.
编译器一直在优化代码以获得更好的性能。


一般过去时 (Simple Past):
The compiler generated the executable file successfully.
编译器成功生成了可执行文件。

The compiler reported a syntax error.
编译器报告了一个语法错误。


过去进行时 (Past Continuous):
The compiler was processing the code when the power went out.
断电时，编译器正在处理代码。

The compiler was optimizing the code for speed.
编译器正在优化代码的速度。


过去完成时 (Past Perfect):
The compiler had finished compiling the program before I ran it.
在我运行程序之前，编译器已经完成了编译。

The compiler had detected the error before I noticed it.
在我注意到错误之前，编译器已经检测到了它。


过去完成进行时 (Past Perfect Continuous):
The compiler had been working on the code for hours before it finally finished.
编译器在最终完成之前已经处理代码数小时了。

The compiler had been optimizing the code for days.
编译器已经优化代码好几天了。


一般将来时 (Simple Future):
The compiler will translate the code into machine instructions.
编译器会将代码翻译成机器指令。

The new compiler will be much faster.
新的编译器会快得多。


将来进行时 (Future Continuous):
The compiler will be processing the code tomorrow.
编译器明天将处理代码。

The compiler will be optimizing the code for memory usage.
编译器将优化代码的内存使用。


将来完成时 (Future Perfect):
By next week, the compiler will have finished compiling the entire project.
到下周，编译器将完成整个项目的编译。

The compiler will have optimized the code by the time we release the software.
到我们发布软件时，编译器将已经优化了代码。


将来完成进行时 (Future Perfect Continuous):
By the end of the month, the compiler will have been processing data for three weeks straight.
到月底，编译器将连续处理数据三周。

The compiler will have been optimizing the code for a month by the time we ship the product.
到我们交付产品时，编译器将已经优化代码一个月了。


PHRASE->
compiler optimization = 编译器优化
compiler error = 编译器错误
compiler construction = 编译器构造
just-in-time compiler (JIT compiler) = 即时编译器
ahead-of-time compiler (AOT compiler) = 预先编译器
