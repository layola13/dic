dereferences:/diːˈrɛfərənsɪz/ | n. | 解引用 | The act of accessing the value stored at a memory location pointed to by a pointer. |  The program dereferences the pointer to access the data. 程序解引用指针来访问数据。| synonyms: accesses, retrieves | antonyms: assigns, stores | noun

dereference:/ˌdiːˈrɛfərəns/ | v. | 解引用 | To access the value stored at a memory location pointed to by a pointer.  | The code dereferences the pointer to get the value of the variable. 代码解引用指针以获取变量的值。| synonyms: access, retrieve | antonyms: assign, store | verb


USAGE->

一般现在时 (Simple Present):
The program dereferences the pointer every time it loops.  程序每次循环都解引用指针。

He dereferences the null pointer, causing a crash. 他解引用空指针，导致程序崩溃。


现在进行时 (Present Continuous):
The code is dereferencing the pointer to access the data. 代码正在解引用指针以访问数据。

The system is dereferencing multiple pointers simultaneously. 系统正在同时解引用多个指针。


现在完成时 (Present Perfect):
The program has dereferenced the pointer several times. 程序已经多次解引用指针。

The function has dereferenced the invalid pointer, leading to an error. 函数解引用了无效指针，导致错误。


现在完成进行时 (Present Perfect Continuous):
The system has been dereferencing the pointer for a while now. 系统已经解引用该指针一段时间了。

The debugger has been dereferencing the pointer to track its value. 调试器一直在解引用指针以跟踪其值。


一般过去时 (Simple Past):
The program dereferenced the pointer and retrieved the data. 程序解引用了指针并检索了数据。

The function dereferenced a null pointer, causing the program to terminate. 函数解引用了一个空指针，导致程序终止。


过去进行时 (Past Continuous):
The program was dereferencing the pointer when the error occurred. 错误发生时，程序正在解引用指针。

The system was dereferencing a series of pointers to access a linked list. 系统正在解引用一系列指针以访问链表。


过去完成时 (Past Perfect):
The program had dereferenced the pointer before the crash. 程序在崩溃之前已经解引用了指针。

The function had dereferenced the pointer multiple times before returning a value. 函数在返回值之前多次解引用了指针。


过去完成进行时 (Past Perfect Continuous):
The system had been dereferencing the pointer for hours before the issue was discovered.  在发现问题之前，系统已经解引用该指针数小时了。

The debugger had been dereferencing the pointer to monitor its changes over time. 调试器一直在解引用指针以监视其随时间的变化。


一般将来时 (Simple Future):
The program will dereference the pointer to access the data. 程序将解引用指针以访问数据。

The function will dereference the pointer only if it is valid. 仅当指针有效时，函数才会解引用该指针。


将来进行时 (Future Continuous):
The system will be dereferencing the pointer at regular intervals. 系统将定期解引用指针。

The program will be dereferencing the pointer to update the display. 程序将解引用指针以更新显示。


将来完成时 (Future Perfect):
By the end of the process, the program will have dereferenced the pointer numerous times. 到进程结束时，程序将已经多次解引用指针。

The function will have dereferenced the pointer and returned the value before the timeout occurs. 在超时发生之前，函数将已经解引用指针并返回了值。


将来完成进行时 (Future Perfect Continuous):
By next week, the system will have been dereferencing the pointer continuously for a month. 到下周，系统将已经连续解引用该指针一个月了。

The program will have been dereferencing the pointer to track its changes over an extended period. 程序将一直在解引用指针以跟踪其在较长时间内的变化。


PHRASE->
dereference a pointer  解引用指针
dereference the null pointer  解引用空指针
dereference operator 解引用运算符 (e.g., * in C/C++)
multiple dereferences 多次解引用
invalid dereference 无效解引用
