macro-instruction:/ˈmækroʊ ɪnˈstrʌkʃən/| n.|宏指令|A single instruction that expands automatically into a sequence of elementary instructions. |The macro-instruction simplifies the programming process.|Synonyms: macro, macro-operation, macrocommand|Antonyms: microinstruction, micro-operation|Noun

USAGE->

一般现在时 (Simple Present):
A macro-instruction simplifies the programming process. = 宏指令简化了编程过程。
The macro-instruction represents a sequence of lower-level instructions. = 宏指令代表一系列低级指令。

现在进行时 (Present Continuous):
The programmer is using a macro-instruction to automate a repetitive task. = 程序员正在使用宏指令来自动化重复性任务。
The compiler is expanding the macro-instruction into its constituent microinstructions. = 编译器正在将宏指令扩展为其组成的微指令。

现在完成时 (Present Perfect):
The use of macro-instructions has significantly reduced development time. = 宏指令的使用大大减少了开发时间。
The programmer has defined a new macro-instruction for frequently used operations. = 程序员已经为常用操作定义了一个新的宏指令。

现在完成进行时 (Present Perfect Continuous):
The team has been utilizing macro-instructions to improve code efficiency. = 团队一直在利用宏指令来提高代码效率。
The compiler has been processing macro-instructions for the past hour. = 编译器过去一个小时一直在处理宏指令。


一般过去时 (Simple Past):
The programmer used a macro-instruction to simplify the code. = 程序员使用宏指令简化了代码。
The macro-instruction saved a lot of time and effort. = 宏指令节省了大量时间和精力。

过去进行时 (Past Continuous):
The compiler was expanding the macro-instruction when the error occurred. = 编译器在扩展宏指令时发生了错误。
The programmer was defining a new set of macro-instructions. = 程序员正在定义一组新的宏指令。


过去完成时 (Past Perfect):
The programmer had already defined the macro-instruction before writing the main program. = 程序员在编写主程序之前已经定义了宏指令。
The compiler had processed all the macro-instructions before the execution began. = 编译器在执行开始之前已经处理了所有宏指令。


过去完成进行时 (Past Perfect Continuous):
The team had been working on a new set of macro-instructions for several weeks. = 团队已经为一组新的宏指令工作了几个星期。
The compiler had been expanding macro-instructions for hours before it finished. = 编译器在完成之前已经扩展宏指令好几个小时了。


一般将来时 (Simple Future):
The new macro-instruction will streamline the development process. = 新的宏指令将简化开发流程。
The use of macro-instructions will reduce the risk of errors. = 宏指令的使用将减少错误的风险。

将来进行时 (Future Continuous):
The compiler will be expanding the macro-instructions during the next compilation phase. = 编译器将在下一个编译阶段扩展宏指令。
The team will be implementing new macro-instructions to improve code readability. = 团队将实施新的宏指令来提高代码可读性。


将来完成时 (Future Perfect):
By the end of the project, the team will have implemented all the necessary macro-instructions. = 到项目结束时，团队将已经实施了所有必要的宏指令。
The compiler will have expanded all the macro-instructions before the program starts. = 编译器将在程序启动之前扩展所有宏指令。


将来完成进行时 (Future Perfect Continuous):
By next year, the team will have been using macro-instructions for over a decade. = 到明年，团队将已经使用宏指令超过十年。
The compiler will have been processing macro-instructions for hours by the time the compilation is complete. = 到编译完成时，编译器将已经处理宏指令几个小时。


PHRASE->
define a macro-instruction = 定义一个宏指令
expand a macro-instruction = 展开一个宏指令
use a macro-instruction = 使用一个宏指令
macro-instruction library = 宏指令库
macro-instruction set = 宏指令集
