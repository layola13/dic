bootd:/ˈbuːtd/| n.| 引导程序，引导守护进程 | A program or daemon that manages the booting process of a computer system. | The bootd process failed and the server couldn't start.  引导程序进程失败，服务器无法启动。| Synonym: bootloader | Antonym: N/A | noun

USAGE->

一般现在时 (Simple Present):
The bootd loads the operating system. 引导程序加载操作系统。
The server uses a custom bootd.  服务器使用自定义引导程序。

现在进行时 (Present Continuous):
The system is currently running the bootd. 系统当前正在运行引导程序。
The bootd is loading the kernel. 引导程序正在加载内核。

现在完成时 (Present Perfect):
The bootd has successfully loaded the operating system. 引导程序已成功加载操作系统。
The server has experienced issues with its bootd. 服务器的引导程序出现问题。

现在完成进行时 (Present Perfect Continuous):
The system has been running the bootd for several minutes. 系统已经运行引导程序几分钟了。
The bootd has been loading the kernel for a while. 引导程序一直在加载内核一段时间了。

一般过去时 (Simple Past):
The bootd loaded the operating system quickly. 引导程序快速加载了操作系统。
The bootd failed to start. 引导程序启动失败。

过去进行时 (Past Continuous):
The system was running the bootd when the power went out.  断电时系统正在运行引导程序。
The bootd was loading the kernel when the error occurred.  发生错误时，引导程序正在加载内核。

过去完成时 (Past Perfect):
The bootd had loaded the kernel before the error occurred.  错误发生之前，引导程序已经加载了内核。
The system had already run the bootd once before restarting. 系统在重启之前已经运行过一次引导程序。

过去完成进行时 (Past Perfect Continuous):
The system had been running the bootd for an hour before it crashed. 系统崩溃前已经运行引导程序一个小时了。
The bootd had been loading the kernel for several minutes before it timed out. 引导程序加载内核几分钟后超时了。

一般将来时 (Simple Future):
The bootd will load the new operating system tomorrow. 引导程序明天将加载新的操作系统。
The system will use a different bootd next time. 系统下次将使用不同的引导程序。

将来进行时 (Future Continuous):
The system will be running the bootd during the update. 系统将在更新期间运行引导程序。
The bootd will be loading the new kernel at that time. 届时引导程序将加载新的内核。

将来完成时 (Future Perfect):
The bootd will have loaded the operating system by the time you log in.  在你登录之前，引导程序将已经加载了操作系统。
The system will have finished using the bootd by then. 到那时系统将已经完成使用引导程序。

将来完成进行时 (Future Perfect Continuous):
The system will have been running the bootd for several hours by the time the installation completes. 安装完成时，系统将已经运行引导程序几个小时了。
The bootd will have been loading the kernel for a long time if there's a problem. 如果有问题，引导程序将已经加载内核很长时间了。


PHRASE->
configure the bootd  配置引导程序
bootd configuration 引导程序配置
bootd failure 引导程序故障
bootd process 引导程序进程
troubleshoot bootd  排除引导程序故障
