endline:/ˈɛndlaɪn/| n.|结束行，终止行|A character or sequence of characters signifying the end of a line of text. |The endline character is usually a newline. 结束行字符通常是换行符。| synonym: terminator, delimiter | antonym: startline | noun

USAGE->

一般现在时 (Simple Present):
The endline character marks the end of each line. 结束行字符标记每一行的结尾。
The system uses a specific endline sequence. 系统使用特定的结束行序列。


现在进行时 (Present Continuous):
The program is reading the file line by line, looking for the endline. 程序正在逐行读取文件，查找结束行。
The editor is inserting the correct endline characters. 编辑器正在插入正确的结束行字符。


现在完成时 (Present Perfect):
The program has reached the endline of the file. 程序已到达文件的结束行。
The user has typed the command after the endline. 用户在结束行之后键入了命令。


现在完成进行时 (Present Perfect Continuous):
The system has been processing the data up to the endline. 系统一直在处理直到结束行的数据。
The program has been searching for the specific endline sequence. 程序一直在搜索特定的结束行序列。


一般过去时 (Simple Past):
The program detected the endline and moved to the next line. 程序检测到结束行并移动到下一行。
The system wrote the data until the endline. 系统将数据写入直到结束行。


过去进行时 (Past Continuous):
The program was processing the line when it encountered the endline. 程序在处理行时遇到了结束行。
The system was writing the data when it reached the endline. 系统在写入数据时到达了结束行。


过去完成时 (Past Perfect):
The program had processed the entire line before it reached the endline. 程序在到达结束行之前已处理了整行。
The system had written all the data before it encountered the endline. 系统在遇到结束行之前已写入了所有数据。


过去完成进行时 (Past Perfect Continuous):
The program had been processing the data for hours before it reached the endline. 程序在到达结束行之前已经处理数据数小时了。
The system had been writing the data continuously before it encountered the endline. 系统在遇到结束行之前一直在连续写入数据。


一般将来时 (Simple Future):
The program will read until it reaches the endline. 程序将读取直到到达结束行。
The system will write the data followed by an endline character. 系统将写入数据，后跟一个结束行字符。


将来进行时 (Future Continuous):
The program will be processing the data line by line, looking for the endline. 程序将逐行处理数据，查找结束行。
The system will be writing the data until it reaches the endline. 系统将写入数据直到到达结束行。


将来完成时 (Future Perfect):
By the time the program finishes, it will have processed all the lines up to the endline.  当程序完成时，它将已经处理了直到结束行的所有行。
The system will have written all the data up to the endline. 系统将已经写入了直到结束行的所有数据。


将来完成进行时 (Future Perfect Continuous):
The program will have been processing the data for hours by the time it reaches the endline.  当程序到达结束行时，它将已经处理数据数小时了。
The system will have been writing the data continuously until it reaches the endline. 系统将一直连续写入数据直到到达结束行。



PHRASE->
reach the endline  到达结束行
insert an endline  插入结束行
detect the endline  检测结束行
process until the endline  处理直到结束行
write to the endline  写入到结束行
