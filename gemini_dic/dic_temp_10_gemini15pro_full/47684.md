debugger: /diːˈbʌɡər/ | n. | 调试器 | A program used to test and debug other programs. |  The debugger helped me find the error in my code.  调试器帮助我找到了代码中的错误。 |  synonyms: debugger tool, debugging tool | antonyms: N/A | noun

USAGE->

一般现在时 (Simple Present):

The debugger identifies errors in the code.  调试器识别代码中的错误。

The debugger helps programmers find bugs. 调试器帮助程序员找到错误。


现在进行时 (Present Continuous):

I am using the debugger to step through the code. 我正在使用调试器单步执行代码。

He is debugging the program to find the cause of the crash. 他正在调试程序以查找崩溃的原因。


现在完成时 (Present Perfect):

The debugger has identified several memory leaks. 调试器已经识别出几个内存泄漏。

We have used the debugger to fix the performance issues. 我们已经使用调试器修复了性能问题。


现在完成进行时 (Present Perfect Continuous):

I have been debugging this code for hours. 我一直在调试这段代码好几个小时了。

She has been using the debugger to track down the bug all morning. 她整个上午一直在使用调试器追踪这个错误。


一般过去时 (Simple Past):

The debugger showed me the value of the variable. 调试器向我显示了变量的值。

I debugged the program yesterday. 我昨天调试了程序。


过去进行时 (Past Continuous):

I was debugging the code when the power went out.  断电时我正在调试代码。

He was using the debugger to step through the code when he found the error. 当他发现错误时，他正在使用调试器单步执行代码。


过去完成时 (Past Perfect):

I had debugged the code before releasing the software. 在发布软件之前，我已经调试了代码。

She had used the debugger to find the error before I arrived. 在我到达之前，她已经使用调试器找到了错误。


过去完成进行时 (Past Perfect Continuous):

I had been debugging the code for hours before I found the error. 在我找到错误之前，我已经调试代码好几个小时了。

He had been using the debugger all day before he finally fixed the bug. 在他最终修复错误之前，他一整天都在使用调试器。


一般将来时 (Simple Future):

I will use the debugger to check the code tomorrow.  我明天将使用调试器检查代码。

The debugger will help us find the bug. 调试器将帮助我们找到错误。


将来进行时 (Future Continuous):

I will be debugging the code tomorrow morning. 明天早上我将调试代码。

He will be using the debugger to test the new features. 他将使用调试器测试新功能。


将来完成时 (Future Perfect):

By the end of the week, I will have debugged the entire program. 到本周末，我将调试完整个程序。

By next month, we will have used the debugger to fix all known bugs. 到下个月，我们将使用调试器修复所有已知的错误。


将来完成进行时 (Future Perfect Continuous):

By next week, I will have been debugging this code for a month. 到下周，我将调试这段代码一个月了。

By the end of the year, she will have been using the debugger to improve the performance of the application for six months. 到年底，她将使用调试器改进应用程序的性能六个月了。


PHRASE->

run the debugger  运行调试器
set a breakpoint in the debugger 在调试器中设置断点
step through the code with the debugger 使用调试器单步执行代码
inspect variables in the debugger  在调试器中检查变量
use the debugger to find bugs  使用调试器查找错误
