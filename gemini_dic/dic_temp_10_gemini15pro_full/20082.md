bios:/ˈbaɪɒs/ (UK), /ˈbaɪoʊs/ (US)| n. |基本输入/输出系统|Basic Input/Output System.  It's firmware used to perform hardware initialization during the booting process (power-on startup), and to provide runtime services for operating systems and programs.|The computer's BIOS needed to be updated. 这台电脑的BIOS需要更新。|近义词:firmware|反义词:N/A|名词

USAGE->

一般现在时 (Simple Present):
The BIOS controls the startup process. BIOS控制启动过程。
The BIOS checks the hardware. BIOS检查硬件。

现在进行时 (Present Continuous):
The BIOS is initializing the hardware. BIOS正在初始化硬件。
The system is loading the BIOS. 系统正在加载BIOS。

现在完成时 (Present Perfect):
The BIOS has completed the POST. BIOS已完成开机自检。
The system has loaded the BIOS successfully. 系统已成功加载BIOS。

现在完成进行时 (Present Perfect Continuous):
The BIOS has been running diagnostics. BIOS一直在运行诊断程序。
The system has been loading the BIOS for a while. 系统加载BIOS已经有一段时间了。

一般过去时 (Simple Past):
The BIOS detected an error. BIOS检测到一个错误。
The BIOS initialized the hardware successfully. BIOS成功初始化了硬件。

过去进行时 (Past Continuous):
The BIOS was checking the hardware when the power failed. 断电时，BIOS正在检查硬件。
The system was loading the BIOS when I interrupted it. 我打断它时，系统正在加载BIOS。

过去完成时 (Past Perfect):
The BIOS had already initialized the hardware before the operating system loaded. 在操作系统加载之前，BIOS已经初始化了硬件。
The system had loaded the BIOS before the error occurred. 在错误发生之前，系统已经加载了BIOS。

过去完成进行时 (Past Perfect Continuous):
The BIOS had been running diagnostics for a few minutes before the error occurred.  在错误发生前，BIOS已经运行了几分钟的诊断程序。
The system had been loading the BIOS for a while before it finally started. 系统加载BIOS有一段时间后才最终启动。

一般将来时 (Simple Future):
The BIOS will check the hardware during startup. BIOS将在启动期间检查硬件。
The system will load the BIOS automatically. 系统将自动加载BIOS。

将来进行时 (Future Continuous):
The BIOS will be initializing the hardware during the boot process. BIOS将在启动过程中初始化硬件。
The system will be loading the BIOS when you turn on the computer. 当你打开电脑时，系统将加载BIOS。


将来完成时 (Future Perfect):
The BIOS will have initialized the hardware by the time the operating system loads. 在操作系统加载之前，BIOS将已经初始化了硬件。
The system will have loaded the BIOS before the user sees the login screen. 在用户看到登录屏幕之前，系统将已经加载了BIOS。

将来完成进行时 (Future Perfect Continuous):
By the time you see the desktop, the BIOS will have been running for several seconds. 当你看到桌面时，BIOS将已经运行了几秒钟。
The system will have been loading the BIOS for a short time before it starts the operating system. 系统将在启动操作系统之前加载BIOS一小段时间。

虚拟语气:
If the BIOS were corrupted, the computer wouldn't boot. 如果BIOS损坏，电脑将无法启动。
I wish I knew how to update the BIOS. 我希望我知道如何更新BIOS。

被动语态:
The BIOS is updated regularly. BIOS定期更新。
The hardware is initialized by the BIOS. 硬件由BIOS初始化。

疑问句:
What version of the BIOS is installed? 安装的是哪个版本的BIOS？
How do I access the BIOS settings? 如何访问BIOS设置？

否定句:
The BIOS isn't working correctly. BIOS工作不正常。
The system didn't load the BIOS. 系统没有加载BIOS。


PHRASE->
update the BIOS 更新BIOS
access the BIOS settings 访问BIOS设置
configure the BIOS 配置BIOS
flash the BIOS  刷新BIOS
check the BIOS version 检查BIOS版本
