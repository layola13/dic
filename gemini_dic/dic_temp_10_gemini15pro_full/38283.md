concatenated:/kənˈkætɪneɪtɪd/ /kɑːnˈkætɪneɪtɪd/ | adj. | 连接的，串联的 | Linked together; formed by joining two or more things end to end. | The files were concatenated into a single large file.  这些文件被连接成一个单独的大文件。| joined, combined, linked, attached | separate, detached | adjective

concatenated:/kənˈkætɪneɪtɪd/ /kɑːnˈkætɪneɪtɪd/ | v. | 连接，串联 |  (past participle of concatenate) To link together in a series or chain. | The program concatenated the two strings. 程序将两个字符串连接起来。| join, combine, link, attach | separate, detach | verb


USAGE->

一般现在时 (Simple Present):
The system concatenates the data fields.
系统连接数据字段。

New information is concatenated to the existing file.
新的信息被连接到现有文件上。


现在进行时 (Present Continuous):
The program is concatenating the files as we speak.
程序正在连接文件。

He is concatenating the strings to create a longer sequence.
他正在连接字符串以创建一个更长的序列。


现在完成时 (Present Perfect):
The software has concatenated all the individual files.
软件已连接所有单个文件。

She has concatenated the data from multiple sources.
她已经连接了来自多个来源的数据。


现在完成进行时 (Present Perfect Continuous):
The computer has been concatenating the files for hours.
计算机已连接文件数小时了。

They have been concatenating the data streams all day.
他们整天都在连接数据流。


一般过去时 (Simple Past):
The script concatenated the text files automatically.
脚本自动连接文本文件。

We concatenated the separate reports into one document.
我们将单独的报告连接成一个文档。


过去进行时 (Past Continuous):
The system was concatenating the log files when the error occurred.
发生错误时，系统正在连接日志文件。

He was concatenating the video clips to make a movie.
他正在连接视频剪辑来制作电影。


过去完成时 (Past Perfect):
He had concatenated the strings before running the program.
在运行程序之前，他已经连接了字符串。

She had concatenated the data before analyzing it.
在分析数据之前，她已经连接了数据。


过去完成进行时 (Past Perfect Continuous):
The program had been concatenating the data for several minutes before it crashed.
程序在崩溃前几分钟一直在连接数据。

They had been concatenating the files for hours before they realized the error.
他们在意识到错误之前已经连接了几个小时的文件。


一般将来时 (Simple Future):
The program will concatenate the files tomorrow.
程序明天将连接文件。

The system will automatically concatenate the new data.
系统将自动连接新数据。


将来进行时 (Future Continuous):
The computer will be concatenating the data overnight.
计算机将在夜间连接数据。

The system will be concatenating the logs in the background.
系统将在后台连接日志。


将来完成时 (Future Perfect):
By tomorrow, the system will have concatenated all the files.
到明天，系统将连接所有文件。

By next week, they will have concatenated all the data.
到下周，他们将连接所有数据。


将来完成进行时 (Future Perfect Continuous):
By the end of the month, the system will have been concatenating data for three months straight.
到月底，系统将连续三个月连接数据。

By the time the project is finished, they will have been concatenating data for over a year.
到项目完成时，他们将连接数据超过一年。


虚拟语气:
If I were to concatenate these strings, the result would be a long sentence.
如果我要连接这些字符串，结果将是一个长句子。

If the files were concatenated correctly, the program would run smoothly.
如果文件连接正确，程序将运行流畅。


被动语态:
The files were concatenated by the system administrator.
文件由系统管理员连接。

The data is being concatenated as we speak.
数据正在被连接。



疑问句:
Were the files concatenated properly?
文件是否正确连接？

How are the strings concatenated in this program?
在这个程序中字符串是如何连接的？



否定句:
The files were not concatenated in the correct order.
文件未按正确顺序连接。

The strings have not been concatenated yet.
字符串尚未连接。


PHRASE->
concatenate strings  连接字符串
concatenate files  连接文件
concatenate data  连接数据
concatenate lists  连接列表
concatenate arrays  连接数组
