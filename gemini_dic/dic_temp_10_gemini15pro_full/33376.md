chunked:/tʃʌŋkt/ | adj. | 分块的，成块的 | Divided into chunks; segmented. | The data was chunked for easier processing.  数据被分块以便于处理。| The presentation was chunked into smaller sections to make it easier to understand.  演示文稿被分成更小的部分，以便更容易理解。| segmented, divided, partitioned | continuous, whole | adjective

USAGE->

一般现在时 (Simple Present):
The system chunked the data into smaller pieces. 系统将数据分成更小的块。
The program always chunks large files for faster uploads.  程序总是将大文件分块以便更快地上传。

现在进行时 (Present Continuous):
The software is chunking the video file as we speak.  软件正在对视频文件进行分块处理。
They are chunking the project into manageable phases.  他们正在将项目分成可管理的阶段。

现在完成时 (Present Perfect):
The algorithm has chunked the text into individual words.  该算法已将文本分块成单个单词。
He has chunked the wood for the fire.  他把木头劈成块准备生火。

现在完成进行时 (Present Perfect Continuous):
The computer has been chunking the data for the past hour.  计算机过去一小时一直在对数据进行分块处理。
They have been chunking the workload to improve efficiency.  他们一直在对工作量进行分块以提高效率。

一般过去时 (Simple Past):
We chunked the information into smaller sections for clarity.  为了清晰起见，我们将信息分成了更小的部分。
The program chunked the audio file into separate tracks.  程序将音频文件分成了单独的音轨。

过去进行时 (Past Continuous):
They were chunking the data when the power went out.  断电时，他们正在对数据进行分块处理。
The system was chunking the files for backup.  系统正在分块文件进行备份。

过去完成时 (Past Perfect):
He had chunked the wood before it started raining.  在开始下雨之前，他已经把木头劈成块了。
They had chunked the project into phases before starting work.  在开始工作之前，他们已经将项目分成了几个阶段。

过去完成进行时 (Past Perfect Continuous):
She had been chunking the data for hours before finding the error.  在发现错误之前，她已经对数据进行分块处理好几个小时了。
They had been chunking the text for analysis all day. 他们一整天都在对文本进行分块分析。


一般将来时 (Simple Future):
The program will chunk the data automatically.  程序将自动对数据进行分块。
We will chunk the project into smaller tasks. 我们将把项目分解成更小的任务。

将来进行时 (Future Continuous):
The system will be chunking the data overnight.  系统将在夜间对数据进行分块。
They will be chunking the work to distribute it evenly.  他们将把工作分块，以便均匀分配。

将来完成时 (Future Perfect):
By tomorrow, the program will have chunked all the data.  到明天，程序将完成所有数据的块化。
By the end of the week, we will have chunked the entire project.  到本周末，我们将完成整个项目的块化。

将来完成进行时 (Future Perfect Continuous):
By next month, the system will have been chunking data for a year.  到下个月，系统将已经对数据进行分块一年了。
By the time the project is finished, they will have been chunking the work for months.  到项目完成时，他们将已经对工作进行分块数月了。

虚拟语气:
If the data were chunked, it would be easier to process.  如果数据被分块，处理起来会更容易。
I wish the program chunked the files automatically.  我希望程序自动对文件进行分块。

被动语态:
The data was chunked into smaller segments.  数据被分块成更小的片段。
The files are being chunked for faster upload.  文件正在被分块以加快上传速度。

疑问句:
Is the data chunked properly?  数据是否正确分块？
How is the information chunked? 信息是如何分块的？

否定句:
The data isn't chunked efficiently.  数据分块效率不高。
The files weren't chunked before the upload.  上传前文件没有分块。



PHRASE->
chunked encoding | 分块编码
chunked transfer encoding | 分块传输编码
chunked data stream | 分块数据流
chunked file upload | 分块文件上传
chunked message | 分块消息
