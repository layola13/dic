arrays:/əˈreɪz/ | n. | 数组，阵列 |  An ordered series or arrangement of things, often in rows and columns.  A data structure that stores a fixed-size sequential collection of elements of the same type. |  The program stores the data in arrays.  这个程序将数据存储在数组中。

The telescope uses arrays of sensors to detect faint light.  这台望远镜使用传感器阵列来探测微弱的光线。 | synonyms: matrix, table, grid, vector, series, sequence | antonyms:  disorder, chaos | noun


USAGE->

一般现在时 (Simple Present):
The computer uses arrays to store information.  计算机使用数组来存储信息。

These arrays contain important data.  这些数组包含重要数据。


现在进行时 (Present Continuous):
The program is accessing the elements of the array. 程序正在访问数组的元素。

He is creating arrays to organize the data. 他正在创建数组来组织数据。


现在完成时 (Present Perfect):
The scientist has analyzed the data in the arrays. 科学家已经分析了数组中的数据。

We have used arrays in many of our programs. 我们在许多程序中都使用了数组。


现在完成进行时 (Present Perfect Continuous):
They have been working with multi-dimensional arrays. 他们一直在使用多维数组。

She has been studying the use of arrays in data analysis. 她一直在研究数组在数据分析中的应用。


一般过去时 (Simple Past):
The programmer created an array to store the results. 程序员创建了一个数组来存储结果。

The system used arrays to manage the large dataset. 系统使用数组来管理大型数据集。


过去进行时 (Past Continuous):
We were processing the data in the arrays when the power went out.  断电时我们正在处理数组中的数据。

She was studying the properties of arrays. 她当时正在研究数组的属性。


过去完成时 (Past Perfect):
They had already filled the array with data before the error occurred.  在错误发生之前，他们已经用数据填充了数组。

He had studied arrays before taking the programming course. 他在参加编程课程之前学习过数组。


过去完成进行时 (Past Perfect Continuous):
We had been working with arrays for weeks before finding a solution. 我们用数组工作了几个星期才找到解决方案。

She had been researching the use of arrays in image processing. 她一直在研究数组在图像处理中的应用。


一般将来时 (Simple Future):
We will use arrays to store the sensor readings. 我们将使用数组来存储传感器读数。

The program will create a new array for each user. 该程序将为每个用户创建一个新的数组。


将来进行时 (Future Continuous):
The computer will be processing the arrays in the background. 计算机将在后台处理数组。

We will be using arrays to analyze the data next week. 我们下周将使用数组来分析数据。


将来完成时 (Future Perfect):
By next month, we will have implemented the array-based solution. 到下个月，我们将已经实施了基于数组的解决方案。

The program will have processed all the arrays by the end of the day.  到今天结束时，程序将已经处理了所有数组。


将来完成进行时 (Future Perfect Continuous):
By the end of the year, they will have been working with arrays for over a decade. 到年底，他们使用数组的时间将超过十年。

She will have been studying array manipulation techniques for several years. 她将已经学习数组操作技术好几年了.


虚拟语气:
If I knew more about arrays, I would be able to write a more efficient program.  如果我更了解数组，我就能写出更高效的程序。

If the array were larger, we could store more data. 如果数组更大，我们就能存储更多数据。


被动语态:
The arrays are accessed by the program. 数组由程序访问。

The data in the arrays was analyzed. 数组中的数据被分析了。


疑问句:
What type of data are stored in these arrays? 这些数组中存储的是什么类型的数据？

How many elements are in the array? 数组中有多少个元素？


否定句:
The program does not use arrays to store this type of data. 程序不使用数组来存储此类型的数据。

We did not create the arrays ourselves.  我们没有自己创建数组。




PHRASE->
array of options 许多选择
an array of problems  一系列问题
an array of colors  各种颜色
two-dimensional array 二维数组
multi-dimensional array 多维数组
