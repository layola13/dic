misencoded:/ˌmɪsɪnˈkəʊdɪd/ | adj. | 错误编码的，编码错误的 | Incorrectly encoded; containing errors in encoding. | The misencoded data caused the program to crash.  错误编码的数据导致程序崩溃。

The misencoded message was unintelligible.  编码错误的信息难以理解。 | 近义词: corrupted, garbled, scrambled | 反义词: correctly encoded, properly encoded | 词性:形容词

词源:  "mis-" (prefix meaning "wrongly" or "badly") + "encode" (verb meaning "to convert into a coded form").  "mis-"（表示“错误地”或“糟糕地”的前缀）+ "encode"（动词，意思是“转换为编码形式”）。


USAGE->

一般现在时 (Simple Present):
The system sometimes misencodes data.  该系统有时会错误地编码数据。

Misencoded characters can cause problems. 错误编码的字符会导致问题。


现在进行时 (Present Continuous):
The software is misencoding the file. 该软件正在错误地编码文件。

He is misencoding the information as we speak. 他正在错误地编码我们正在说的信息。


现在完成时 (Present Perfect):
The program has misencoded several files. 该程序已经错误地编码了几个文件。

The technician has misencoded the settings. 技术人员错误地编码了设置。


现在完成进行时 (Present Perfect Continuous):
The system has been misencoding data all day. 系统一整天都在错误地编码数据。

They have been misencoding the video signal for hours. 他们已经错误地编码视频信号好几个小时了。


一般过去时 (Simple Past):
The device misencoded the signal yesterday. 该设备昨天错误地编码了信号。

The programmer misencoded the algorithm. 程序员错误地编码了算法。


过去进行时 (Past Continuous):
The computer was misencoding the data when the power went out.  断电时，计算机正在错误地编码数据。

He was misencoding the files while trying to compress them. 他在尝试压缩文件时错误地编码了文件。


过去完成时 (Past Perfect):
The system had misencoded the data before we noticed the problem. 在我们注意到问题之前，系统已经错误地编码了数据。

The software had misencoded the file before it was saved.  该软件在保存文件之前就已经错误地编码了它。


过去完成进行时 (Past Perfect Continuous):
The program had been misencoding data for weeks before it was discovered. 在发现之前，该程序已经错误地编码数据数周了。

The encoder had been misencoding the video stream for hours before anyone realized. 编码器在有人意识到之前已经错误地编码视频流数小时了。


一般将来时 (Simple Future):
If we don't fix this bug, the system will misencode the data again. 如果我们不修复这个错误，系统将再次错误地编码数据。

The new software will not misencode the files. 新软件不会错误地编码文件。


将来进行时 (Future Continuous):
The computer will be misencoding the data if we don't update the drivers. 如果我们不更新驱动程序，计算机将错误地编码数据。

The system will be misencoding information unless we fix the issue. 除非我们解决这个问题，否则系统将错误地编码信息。


将来完成时 (Future Perfect):
By tomorrow, the system will have misencoded thousands of files. 到明天，系统将错误地编码数千个文件。

By next week, we will have fixed the misencoded data problem. 到下周，我们将解决错误编码的数据问题。


将来完成进行时 (Future Perfect Continuous):
By the end of the month, the system will have been misencoding data for a year. 到月底，系统将已经错误地编码数据一年了。

By the time we finish, the program will have been misencoding the video for days. 到我们完成时，该程序将已经错误地编码视频好几天了。



PHRASE->
misencoded data  错误编码的数据
misencoded characters 错误编码的字符
misencoded file  错误编码的文件
misencoded information 错误编码的信息
misencoded signal 错误编码的信号
