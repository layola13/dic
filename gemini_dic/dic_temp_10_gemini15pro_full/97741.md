initializations:/ɪˌnɪʃələˈzeɪʃənz/ | n. |初始化，初始设置|The process of setting the initial values of variables or data structures. This is often done at the beginning of a program or when a new object is created.|The system requires several initializations before it can be used. 系统在使用前需要进行多次初始化。|近义词: setup, configuration, boot, startup|反义词: termination, conclusion | noun

initialisation:/ɪˌnɪʃəlaɪˈzeɪʃən/ | n. |初始化，初始设置|The process of setting the initial values of variables or data structures. This is often done at the beginning of a program or when a new object is created.|The program crashed due to faulty initialisation. 程序由于初始化错误而崩溃。|近义词: setup, configuration, boot, startup|反义词: termination, conclusion | noun


USAGE->

一般现在时 (Simple Present):
The program initializes the variables at startup.  程序在启动时初始化变量。
The system initializes the settings every time it's turned on. 系统每次启动时都会初始化设置。

现在进行时 (Present Continuous):
The system is initializing the database right now.  系统正在初始化数据库。
The program is initializing its components as we speak.  程序正在初始化它的组件。


现在完成时 (Present Perfect):
The system has initialized the network connection. 系统已经初始化了网络连接。
The program has initialized all necessary variables. 程序已经初始化了所有必要的变量。

现在完成进行时 (Present Perfect Continuous):
The system has been initializing for several minutes. 系统已经初始化了几分钟。
The server has been initializing the database since this morning.  服务器从今天早上开始就一直在初始化数据库。


一般过去时 (Simple Past):
The program initialized the display successfully. 程序成功初始化了显示器。
The system initialized the hardware without any errors. 系统正确地初始化了硬件。

过去进行时 (Past Continuous):
The system was initializing the hard drive when the power went out.  断电时，系统正在初始化硬盘。
The program was initializing the graphics card when it crashed. 程序崩溃时正在初始化显卡。

过去完成时 (Past Perfect):
The system had initialized the network before the user logged in.  用户登录之前，系统已经初始化了网络。
The program had initialized all the required modules before starting the main process. 在启动主进程之前，程序已经初始化了所有必需的模块。


过去完成进行时 (Past Perfect Continuous):
The server had been initializing the database for hours before the error occurred.  在错误发生之前，服务器已经初始化数据库好几个小时了。
The program had been initializing the components for a while when I cancelled the process. 当我取消进程时，程序已经初始化组件一段时间了。


一般将来时 (Simple Future):
The system will initialize the settings automatically. 系统将自动初始化设置。
The program will initialize the memory when it starts. 程序启动时将初始化内存。


将来进行时 (Future Continuous):
The system will be initializing the database at midnight.  系统将在午夜初始化数据库。
The program will be initializing the graphics card during the installation process. 程序将在安装过程中初始化显卡。


将来完成时 (Future Perfect):
By the time you log in, the system will have initialized all the necessary components.  到你登录时，系统将已经初始化了所有必要的组件。
The program will have initialized the memory by the time the operating system loads.  在操作系统加载之前，程序将已经初始化了内存。


将来完成进行时 (Future Perfect Continuous):
The server will have been initializing the database for 12 hours by tomorrow morning. 到明天早上，服务器将已经初始化数据库 12 个小时了。
The program will have been initializing the components for several minutes before the user interface appears. 在用户界面出现之前，程序将已经初始化组件几分钟了。



PHRASE->
system initializations 系统初始化
network initializations 网络初始化
database initializations 数据库初始化
hardware initializations 硬件初始化
software initializations 软件初始化
