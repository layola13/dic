ascians:/ˈæskiənz/| n.|ASCII字符|Characters that are part of the ASCII standard, representing letters, numbers, punctuation, and control codes.|The website only accepts asciians in the form data.|近义词：ASCII characters, 7-bit characters|反义词: Unicode characters|Noun


USAGE->

一般现在时 (Simple Present):
The system only processes asciians. =  该系统只处理ASCII字符。
Many older systems rely on asciians. = 许多旧系统依赖于ASCII字符。

现在进行时 (Present Continuous):
The program is converting the text to asciians. = 程序正在将文本转换为ASCII字符。
She is checking the document for non-asciians. = 她正在检查文档中是否有非ASCII字符。

现在完成时 (Present Perfect):
We have removed all non-asciians from the database. = 我们已经从数据库中删除了所有非ASCII字符。
The software has processed millions of asciians. = 该软件已经处理了数百万个ASCII字符。

现在完成进行时 (Present Perfect Continuous):
They have been working on filtering out non-asciians. = 他们一直在致力于过滤掉非ASCII字符。
He has been studying the use of asciians in different systems. = 他一直在研究ASCII字符在不同系统中的使用。


一般过去时 (Simple Past):
The old computer only used asciians. = 旧的电脑只使用ASCII字符。
They limited the input to asciians. = 他们将输入限制为ASCII字符。

过去进行时 (Past Continuous):
We were processing asciians when the system crashed. = 系统崩溃时我们正在处理ASCII字符。
She was explaining the importance of asciians. = 她当时正在解释ASCII字符的重要性。

过去完成时 (Past Perfect):
Before Unicode, most systems had only used asciians. = 在Unicode之前，大多数系统只使用ASCII字符。
They had converted the file to asciians before uploading it. = 他们在上传之前将文件转换成了ASCII字符。

过去完成进行时 (Past Perfect Continuous):
We had been working with asciians for years before Unicode became popular. = 在Unicode流行之前，我们已经使用ASCII字符多年了。
They had been studying asciians for a long time. = 他们一直在研究ASCII字符很长时间了。

一般将来时 (Simple Future):
The new system will support both asciians and Unicode. = 新系统将同时支持ASCII字符和Unicode。
We will convert the data to asciians. = 我们将把数据转换为ASCII字符。

将来进行时 (Future Continuous):
We will be processing asciians throughout the night. = 我们将在整个晚上处理ASCII字符。
The system will be checking for non-asciians in real time. = 系统将实时检查非ASCII字符。

将来完成时 (Future Perfect):
By next week, we will have converted all the files to asciians. = 到下周，我们将把所有文件转换为ASCII字符。
They will have finished processing the asciians by tomorrow. = 他们将在明天之前完成ASCII字符的处理。

将来完成进行时 (Future Perfect Continuous):
By the end of the project, we will have been working with asciians for over a year. = 到项目结束时，我们将已经使用ASCII字符一年多了。
They will have been studying asciians for several months. = 他们将已经学习ASCII字符好几个月了。

虚拟语气:
If the system only accepted asciians, it would be simpler. = 如果系统只接受ASCII字符，它会更简单。
I wish all systems used only asciians. = 我希望所有系统都只使用ASCII字符。

被动语态:
Asciians are used in many legacy systems. = ASCII字符在许多遗留系统中使用。
The data was converted to asciians. = 数据被转换成了ASCII字符。

疑问句:
Are these characters all asciians? = 这些字符都是ASCII字符吗？
How many asciians are in this file? = 这个文件中包含多少个ASCII字符？

否定句:
These characters are not all asciians. = 这些字符并非都是ASCII字符。
The system does not support non-asciians. = 该系统不支持非ASCII字符。


PHRASE->
converting to asciians = 转换为ASCII字符
filtering out non-asciians = 过滤掉非ASCII字符
working with asciians = 使用ASCII字符
processing asciians = 处理ASCII字符
ASCII character set = ASCII字符集
