builtins:/ˈbɪltɪnz/| n.|内置函数/组件/模块|Built-in functions or components are those that are included as a standard part of a software or programming language. They are readily available for use without needing any external libraries or installations.|Python's `builtins` module provides access to essential functions like `print()` and `len()`. Python的`builtins`模块提供了对基本函数（如`print()`和`len()`）的访问。|synonyms: integrated, inherent, intrinsic|antonyms: external, add-on|noun

USAGE->

一般现在时 (Simple Present):
The `builtins` module provides many useful functions.  `builtins`模块提供许多有用的函数。

Python uses the `builtins` module for core operations. Python使用`builtins`模块进行核心操作。


现在进行时 (Present Continuous):
The programmer is using the `builtins` function `len()` to calculate string length. 程序员正在使用`builtins`函数`len()`来计算字符串长度。

The system is accessing the `builtins` module to execute the `print()` function. 系统正在访问`builtins`模块来执行`print()`函数。


现在完成时 (Present Perfect):
Python has always included a `builtins` module. Python一直包含一个`builtins`模块。

The developer has used several functions from the `builtins` module. 开发人员已经使用了`builtins`模块中的几个函数。


现在完成进行时 (Present Perfect Continuous):
The interpreter has been using the `builtins` module throughout the execution of the script. 解释器在整个脚本执行过程中一直在使用`builtins`模块。

The program has been relying on the `builtins` module for basic functionality. 程序一直依赖`builtins`模块来实现基本功能。


一般过去时 (Simple Past):
The original version of Python included a smaller set of `builtins`. Python的原始版本包含一组较小的`builtins`。

The programmer accessed the `builtins` module to use the `int()` function. 程序员访问了`builtins`模块以使用`int()`函数。


过去进行时 (Past Continuous):
The script was using the `builtins` module when the error occurred.  脚本在发生错误时正在使用`builtins`模块。

The interpreter was accessing the `builtins` module to execute a function. 解释器正在访问`builtins`模块来执行一个函数。


过去完成时 (Past Perfect):
Before the update, the `builtins` module had included a different set of functions. 在更新之前，`builtins`模块包含一组不同的函数。

The programmer had already used several `builtins` functions before encountering the issue. 程序员在遇到问题之前已经使用了几个`builtins`函数。


过去完成进行时 (Past Perfect Continuous):
The program had been relying on the `builtins` module for several hours before crashing. 程序在崩溃前已经依赖`builtins`模块几个小时了。

The interpreter had been using the `builtins` module extensively before the memory leak. 解释器在内存泄漏之前一直在广泛使用`builtins`模块。


一般将来时 (Simple Future):
Future versions of Python will likely include more functions in the `builtins` module. Python的未来版本可能会在`builtins`模块中包含更多函数。

The new library will utilize several functions from the `builtins` module. 新库将利用`builtins`模块中的几个函数。


将来进行时 (Future Continuous):
The interpreter will be using the `builtins` module to execute the code. 解释器将使用`builtins`模块来执行代码。

The program will be accessing the `builtins` module frequently during runtime. 程序将在运行时频繁访问`builtins`模块。


将来完成时 (Future Perfect):
By the time the program finishes, it will have used several `builtins` functions. 当程序完成时，它将已经使用了几种`builtins`函数。

The interpreter will have accessed the `builtins` module numerous times. 解释器将已经访问了`builtins`模块很多次。


将来完成进行时 (Future Perfect Continuous):
By next year, Python will have been using the `builtins` module for decades. 到明年，Python将已经使用`builtins`模块几十年了。

The program will have been relying on the `builtins` module for its core functionality for years. 多年来，该程序一直依赖`builtins`模块的核心功能。


PHRASE->
access builtins  访问内置函数
modify builtins  修改内置函数 (Generally not recommended)
extend builtins 扩展内置函数
builtin functions 内置函数
builtin modules 内置模块
