misencodes:/ˌmɪsɪnˈkoʊdz/ | v. | 错误编码，编码错误 | To encode incorrectly, resulting in errors in the resulting data. | The software misencoded the file, causing it to be unreadable.  该软件对文件进行了错误编码，导致文件无法读取。 | miscodes, encodes incorrectly | encodes correctly | verb
misencodes:/ˌmɪsɪnˈkoʊdz/ | n. | 错误编码（的结果）| An instance of incorrect encoding. |  The misencodes in the database corrupted the information. 数据库中的编码错误损坏了信息。| errors, glitches | correct encoding | noun


USAGE->

一般现在时 (Simple Present):
The system sometimes misencodes data. 系统有时会错误编码数据。
He often misencodes the files when he's tired.  他累了的时候经常把文件编码错误。

现在进行时 (Present Continuous):
The software is misencoding the video stream. 该软件正在对视频流进行错误编码。
He is misencoding the data as we speak.  他正在错误地编码数据。

现在完成时 (Present Perfect):
The program has misencoded several files.  该程序已经错误编码了几个文件。
She has misencoded the information, leading to errors.  她错误地编码了信息，导致了错误。

现在完成进行时 (Present Perfect Continuous):
The system has been misencoding data all day.  系统一整天都在错误地编码数据。
He has been misencoding the files for hours. 他已经错误地编码文件好几个小时了。

一般过去时 (Simple Past):
The encoder misencoded the audio track. 编码器错误地编码了音轨。
The program misencoded the data yesterday. 程序昨天错误地编码了数据。

过去进行时 (Past Continuous):
The system was misencoding the data when the power went out.  断电时，系统正在错误地编码数据。
He was misencoding the files when I interrupted him. 我打断他时，他正在错误地编码文件。

过去完成时 (Past Perfect):
The system had misencoded the data before we noticed the problem. 在我们注意到问题之前，系统已经错误地编码了数据。
She had misencoded the file before sending it.  她在发送文件之前错误地编码了它。

过去完成进行时 (Past Perfect Continuous):
The system had been misencoding data for weeks before the issue was resolved. 在问题解决之前，系统已经错误地编码数据数周了。
They had been misencoding the files for days before they realized the mistake. 他们在意识到错误之前，已经错误地编码文件好几天了。

一般将来时 (Simple Future):
The system will misencode the data if we don't fix the bug. 如果我们不修复这个错误，系统将会错误地编码数据。
He will misencode the file if he's not careful. 如果他不小心，他会错误地编码文件。

将来进行时 (Future Continuous):
The system will be misencoding the data if we don't intervene. 如果我们不干预，系统将会一直在错误地编码数据。
He will be misencoding the files if we don't give him proper training. 如果我们不给他适当的培训，他将会一直在错误地编码文件。

将来完成时 (Future Perfect):
By next week, the system will have misencoded thousands of files. 到下周，系统将已经错误地编码了数千个文件。
By tomorrow, he will have misencoded all the data. 到明天，他将已经错误地编码了所有数据。

将来完成进行时 (Future Perfect Continuous):
By next month, the system will have been misencoding data for a year. 到下个月，系统将已经错误地编码数据一年了。
By the end of the project, he will have been misencoding files for months. 到项目结束时，他将已经错误地编码文件好几个月了。



PHRASE->
what misencodes are causing the problem? = 什么编码错误导致了这个问题？
check for misencodes in the file = 检查文件中的编码错误
the misencodes corrupted the data = 编码错误损坏了数据
fix the misencodes in the system = 修复系统中的编码错误
identify and correct the misencodes = 识别并纠正编码错误
