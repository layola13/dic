csv:/ˌsiːɛsˈviː/ /ˌsiːɛsˈviː/| n.|逗号分隔值（Comma-Separated Values）|A file format used to store tabular data (numbers and text) in plain text. Each line of the file is a data record. Each record consists of one or more fields, separated by commas.  A CSV file typically stores tabular data (numbers and text) in plain text, in which each line is a data record. Each record consists of one or more fields, separated by commas. The use of the comma as a field separator is the source of the name for this file format.|I saved the data in a CSV file. 我把数据保存为CSV文件。|近义词：TSV (Tab-Separated Values), delimited text file|反义词:|名词


USAGE->

一般现在时 (Simple Present):
The CSV file contains all the necessary data.  这个CSV文件包含所有必要的数据。

This program reads CSV files and converts them to Excel spreadsheets.  这个程序读取CSV文件并将其转换为Excel电子表格。


现在进行时 (Present Continuous):
I am currently analyzing the data from the CSV file. 我目前正在分析来自CSV文件的数据。

He is importing the CSV file into the database. 他正在将CSV文件导入数据库。


现在完成时 (Present Perfect):
I have already processed the CSV file. 我已经处理了CSV文件。

She has converted the spreadsheet into a CSV file. 她已将电子表格转换为CSV文件。


现在完成进行时 (Present Perfect Continuous):
We have been working with CSV files all day. 我们一整天都在处理CSV文件。

They have been trying to import the CSV file for hours. 他们已经尝试导入CSV文件好几个小时了。


一般过去时 (Simple Past):
I downloaded the CSV file yesterday. 我昨天下载了CSV文件。

We analyzed the data from the CSV file last week.  我们上周分析了来自CSV文件的数据。


过去进行时 (Past Continuous):
I was editing the CSV file when the power went out.  我正在编辑CSV文件时停电了。

She was uploading the CSV file when the internet connection failed. 她正在上传CSV文件时，网络连接失败了。


过去完成时 (Past Perfect):
I had already downloaded the CSV file before you sent it. 在你发送之前，我已经下载了CSV文件。

He had converted the data to CSV before importing it into the database. 在导入数据库之前，他已经将数据转换为CSV格式。


过去完成进行时 (Past Perfect Continuous):
I had been working on the CSV file for hours before I finally finished. 在我最终完成之前，我已经处理CSV文件好几个小时了。

They had been trying to fix the errors in the CSV file all morning. 他们整个上午都在尝试修复CSV文件中的错误。


一般将来时 (Simple Future):
I will download the CSV file tomorrow. 我明天会下载CSV文件。

We will analyze the data in the CSV file next week. 我们下周将分析CSV文件中的数据。


将来进行时 (Future Continuous):
I will be processing the CSV file tomorrow morning. 明天早上我将处理CSV文件。

They will be importing the CSV file into the database next week. 下周他们将把CSV文件导入数据库。


将来完成时 (Future Perfect):
By next week, I will have finished analyzing the CSV file. 到下周，我将完成对CSV文件的分析。

By the end of the month, we will have converted all the data to CSV format. 到月底，我们将把所有数据转换为CSV格式。


将来完成进行时 (Future Perfect Continuous):
By next year, I will have been working with CSV files for five years. 到明年，我将使用CSV文件五年了。

By the time the project is finished, they will have been analyzing CSV data for months. 到项目完成时，他们将分析CSV数据好几个月了。


虚拟语气:
If I had the CSV file, I could analyze the data. 如果我有CSV文件，我就可以分析数据。

I wish I knew how to create a CSV file. 我希望我知道如何创建CSV文件。


被动语态:
The CSV file was created by the program. CSV文件是由程序创建的。

The data is being exported as a CSV file. 数据正在导出为CSV文件。


疑问句:
Where is the CSV file located? CSV文件在哪里？

How do I open a CSV file? 如何打开CSV文件？


否定句:
I don't have the CSV file. 我没有CSV文件。

The program doesn't support CSV files.  该程序不支持CSV文件。



PHRASE->
export to CSV = 导出到CSV
import from CSV = 从CSV导入
CSV file format = CSV文件格式
parse a CSV file = 解析CSV文件
generate a CSV file = 生成CSV文件
