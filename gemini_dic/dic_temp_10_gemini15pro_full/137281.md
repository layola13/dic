nonsynchronically:/ˌnɒnˌsɪŋkrəˈnɪkli/ | adv. | 非同步地，不同步地 | Not occurring at the same time; asynchronously.  | The two processes operate nonsynchronically, meaning they don't have to start or finish at the same time. 这两个进程非同步运行，意味着它们不必同时开始或结束。 |  The data was updated nonsynchronically across the different servers.  数据在不同的服务器上非同步更新。 | asynchronously, independently | synchronously | adverb


USAGE->

USAGE->
一般现在时 (Simple Present):
The system nonsynchronically updates the database. 系统非同步更新数据库。
The two devices communicate nonsynchronically. 这两个设备非同步通信。


现在进行时 (Present Continuous):
The servers are nonsynchronically processing the requests. 服务器正在非同步处理请求。
The program is nonsynchronically downloading the files. 程序正在非同步下载文件。


现在完成时 (Present Perfect):
The data has been updated nonsynchronically. 数据已非同步更新。
The system has nonsynchronically processed the transactions. 系统已非同步处理了交易。


现在完成进行时 (Present Perfect Continuous):
The program has been nonsynchronically downloading the files for hours. 程序已经非同步下载文件好几个小时了。
The servers have been nonsynchronically processing requests all day. 服务器一整天都在非同步处理请求。


一般过去时 (Simple Past):
The system nonsynchronically updated the database last night. 系统昨晚非同步更新了数据库。
The two devices communicated nonsynchronically during the test. 在测试期间，这两个设备非同步通信。


过去进行时 (Past Continuous):
The servers were nonsynchronically processing the requests when the error occurred.  错误发生时，服务器正在非同步处理请求。
The program was nonsynchronically downloading the files while I was working. 我工作的时候，程序正在非同步下载文件。


过去完成时 (Past Perfect):
The data had been updated nonsynchronically before the system crashed. 系统崩溃前，数据已经非同步更新了。
The system had nonsynchronically processed the transactions before the report was generated. 在生成报告之前，系统已经非同步处理了交易。


过去完成进行时 (Past Perfect Continuous):
The program had been nonsynchronically downloading the files for hours before it finished. 程序非同步下载文件好几个小时才完成。
The servers had been nonsynchronically processing requests all day before the outage.  断电之前，服务器一整天都在非同步处理请求。


一般将来时 (Simple Future):
The system will nonsynchronically update the database tomorrow. 系统明天将非同步更新数据库。
The two devices will communicate nonsynchronically during the next test. 在下一次测试中，这两个设备将非同步通信。


将来进行时 (Future Continuous):
The servers will be nonsynchronically processing the requests during peak hours.  高峰时段，服务器将非同步处理请求。
The program will be nonsynchronically downloading the files overnight. 程序将通宵非同步下载文件。


将来完成时 (Future Perfect):
The data will have been updated nonsynchronically by next week.  到下周，数据将已经非同步更新。
The system will have nonsynchronically processed all the transactions by the end of the month. 到月底，系统将已经非同步处理了所有交易。


将来完成进行时 (Future Perfect Continuous):
The program will have been nonsynchronically downloading the files for a week by then. 到那时，程序将已经非同步下载文件一周了。
The servers will have been nonsynchronically processing requests for days by the time we fix the issue.  到我们解决问题的时候，服务器将已经非同步处理请求好几天了。



PHRASE->
operate nonsynchronically  非同步运行
communicate nonsynchronically 非同步通信
update nonsynchronically  非同步更新
process nonsynchronically 非同步处理
run nonsynchronically 非同步运行
