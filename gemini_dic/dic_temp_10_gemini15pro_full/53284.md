dirnt:/dɜːrnt/ | n. | 目录条目 | A directory entry.  It represents a file or a directory within a file system, as returned by the readdir() function. |  The `dirnt` structure contains information about each file in the directory. | 近义词: entry, item | 反义词:  (No direct antonym) | 词性: noun

USAGE->

一般现在时 (Simple Present):
The dirnt structure contains the file name. = dirnt 结构包含文件名。
The readdir() function returns a pointer to a dirnt structure. = readdir() 函数返回指向 dirnt 结构的指针。

现在进行时 (Present Continuous):
The program is reading the directory entries using dirnt. = 程序正在使用 dirnt 读取目录条目。
The system is processing each dirnt structure. = 系统正在处理每个 dirnt 结构。

现在完成时 (Present Perfect):
The function has read all the dirnt entries in the directory. = 函数已读取目录中的所有 dirnt 条目。
The loop has processed each dirnt structure. = 循环已处理每个 dirnt 结构。

现在完成进行时 (Present Perfect Continuous):
The program has been reading dirnt entries for the last hour. = 程序在过去一个小时里一直在读取 dirnt 条目。
The system has been processing dirnt structures continuously. = 系统一直在连续处理 dirnt 结构。


一般过去时 (Simple Past):
The function returned a dirnt structure for each file. = 函数为每个文件返回一个 dirnt 结构。
The program read the directory entries using dirnt. = 程序使用 dirnt 读取了目录条目。


过去进行时 (Past Continuous):
The program was reading the directory entries using dirnt when the error occurred. = 发生错误时，程序正在使用 dirnt 读取目录条目。
The system was processing each dirnt structure when it crashed. = 系统崩溃时，正在处理每个 dirnt 结构。


过去完成时 (Past Perfect):
The program had read all the dirnt entries before the error occurred. = 程序在发生错误之前已读取所有 dirnt 条目。
The system had processed all the dirnt structures before it restarted. = 系统在重新启动之前已处理所有 dirnt 结构。


过去完成进行时 (Past Perfect Continuous):
The program had been reading dirnt entries for an hour before the error occurred. = 程序在发生错误之前已读取 dirnt 条目一个小时。
The system had been processing dirnt structures for a long time before it crashed. = 系统在崩溃之前已长时间处理 dirnt 结构。


一般将来时 (Simple Future):
The program will read the directory entries using dirnt. = 程序将使用 dirnt 读取目录条目。
The function will return a dirnt structure for each file. = 函数将为每个文件返回一个 dirnt 结构。


将来进行时 (Future Continuous):
The program will be reading the directory entries using dirnt at that time. = 程序届时将使用 dirnt 读取目录条目。
The system will be processing dirnt structures during the operation. = 系统将在操作期间处理 dirnt 结构。


将来完成时 (Future Perfect):
The program will have read all the dirnt entries by tomorrow. = 程序到明天将已读取所有 dirnt 条目。
The system will have processed all the dirnt structures by the end of the day. = 系统到今天结束时将已处理所有 dirnt 结构。


将来完成进行时 (Future Perfect Continuous):
The program will have been reading dirnt entries for 24 hours by tomorrow. = 程序到明天将已读取 dirnt 条目 24 小时。
The system will have been processing dirnt structures continuously for a week by then. = 到那时，系统将已连续处理 dirnt 结构一周。



PHRASE->
loop through dirnt structures = 循环遍历 dirnt 结构
access dirnt members = 访问 dirnt 成员
get file information from dirnt = 从 dirnt 获取文件信息
process each dirnt entry = 处理每个 dirnt 条目
retrieve file name from dirnt = 从 dirnt 检索文件名


词源:  Derived from "directory entry". It's a contraction formed for convenience in programming contexts.
