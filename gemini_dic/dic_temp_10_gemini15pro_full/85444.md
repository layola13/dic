hashed:/hæʃt/ /ˈhæʃt/ | adj. |  散列的，哈希的 | Converted to a hash code or hash value, especially for security purposes.  It's often used in computer science when talking about data storage and retrieval. |  The password was hashed before being stored in the database.  密码在存储到数据库之前已被散列。 | synonyms: encrypted, encoded, ciphered | antonyms: decrypted, decoded, deciphered | adjective

hashed:/hæʃt/ /ˈhæʃt/ | v. | 散列，哈希 | To convert (data) into a hash code or hash value, especially for security purposes. | The system hashes user passwords to protect them.  系统对用户密码进行哈希处理以保护它们。 | synonyms: encrypt, encode, cipher | antonyms: decrypt, decode, decipher | verb


USAGE->

USAGE->
Simple Present:
The system hashes all passwords. = 系统会对所有密码进行哈希处理。
She hashes the data before sending it. = 她先将数据哈希，然后再发送。

Present Continuous:
The system is hashing the password now. = 系统正在对密码进行哈希处理。
He is hashing the file to ensure its integrity. = 他正在对文件进行哈希处理以确保其完整性。

Present Perfect:
The system has hashed the password successfully. = 系统已成功对密码进行哈希处理。
They have hashed all the sensitive data. = 他们已经对所有敏感数据进行了哈希处理。

Present Perfect Continuous:
The system has been hashing data all day. = 系统一整天都在进行数据哈希处理。
She has been hashing the files for several hours. = 她已经对这些文件进行了几个小时的哈希处理。

Simple Past:
The system hashed the password yesterday. = 系统昨天对密码进行了哈希处理。
We hashed the data before storing it. = 我们在存储数据之前对其进行了哈希处理。

Past Continuous:
The system was hashing the password when the power went out. = 断电时，系统正在对密码进行哈希处理。
He was hashing the file while we waited. = 我们等待的时候，他正在对文件进行哈希处理。

Past Perfect:
The system had hashed the password before the breach occurred. = 在漏洞发生之前，系统已经对密码进行了哈希处理。
They had hashed the data before sending it to the server. = 他们在将数据发送到服务器之前对其进行了哈希处理。

Past Perfect Continuous:
The system had been hashing data for hours before it crashed. = 系统崩溃前已经进行数据哈希处理好几个小时了。
She had been hashing the files all night. = 她整晚都在对文件进行哈希处理。

Simple Future:
The system will hash the new password automatically. = 系统将自动对新密码进行哈希处理。
We will hash the data before uploading it. = 我们将在上传数据之前对其进行哈希处理。

Future Continuous:
The system will be hashing the data tomorrow. = 系统明天将对数据进行哈希处理。
They will be hashing the files during the maintenance window. = 他们将在维护窗口期间对文件进行哈希处理。

Future Perfect:
By next week, the system will have hashed all user passwords. = 到下周，系统将对所有用户密码进行哈希处理。
We will have hashed all the data by the end of the month. = 到月底，我们将对所有数据进行哈希处理。

Future Perfect Continuous:
By next year, the system will have been hashing data for five years. = 到明年，系统将已经进行数据哈希处理五年了。
They will have been hashing files continuously for a week. = 他们将连续一周对文件进行哈希处理。


PHRASE->
hashed password = 哈希密码
hashed value = 哈希值
hashed data = 哈希数据
get hashed = 被哈希
cryptographically hashed =  经过加密哈希处理的
