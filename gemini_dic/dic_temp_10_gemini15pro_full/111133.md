literator:/ˈlɪtəreɪtər/| n.|迭代器|An object that allows you to traverse a collection of data, one element at a time.|The iterator made it easy to process each item in the list.|近义词：enumerator, traverser|反义词:|noun


USAGE->

一般现在时 (Simple Present):
The iterator returns the next element in the sequence.
迭代器返回序列中的下一个元素。

The iterator provides a way to access elements without exposing the underlying representation.
迭代器提供了一种访问元素而不暴露底层表示的方法。

现在进行时 (Present Continuous):
The program is using an iterator to process the data.
程序正在使用迭代器来处理数据。

She is learning how to use iterators in Python.
她正在学习如何在 Python 中使用迭代器。


现在完成时 (Present Perfect):
The iterator has processed all the elements in the list.
迭代器已经处理了列表中的所有元素。

He has used iterators in his previous projects.
他在以前的项目中使用过迭代器。


现在完成进行时 (Present Perfect Continuous):
The program has been using an iterator to process the large dataset for hours.
程序已经使用迭代器处理大型数据集好几个小时了。

She has been studying iterators and generators in Python for a week.
她学习 Python 中的迭代器和生成器已经一个星期了。


一般过去时 (Simple Past):
The iterator returned the first element of the array.
迭代器返回了数组的第一个元素。

We used an iterator to loop through the collection.
我们使用迭代器循环遍历集合。


过去进行时 (Past Continuous):
The program was using an iterator to process the data when the error occurred.
程序正在使用迭代器处理数据时发生了错误。

He was explaining how iterators work when I interrupted him.
他正在解释迭代器如何工作时，我打断了他。


过去完成时 (Past Perfect):
The iterator had already processed half of the data before the program crashed.
在程序崩溃之前，迭代器已经处理了一半的数据。

She had learned about iterators before she started the project.
在她开始这个项目之前，她已经学习了迭代器。


过去完成进行时 (Past Perfect Continuous):
The program had been using an iterator for several hours before it finished processing the data.
程序使用迭代器几个小时后才完成了数据的处理。

They had been discussing the use of iterators for a while before they reached a decision.
他们在做出决定之前已经讨论了迭代器的使用一段时间。


一般将来时 (Simple Future):
The iterator will return the next element in the sequence.
迭代器将返回序列中的下一个元素。

We will use an iterator to efficiently process the data.
我们将使用迭代器来有效地处理数据。


将来进行时 (Future Continuous):
The program will be using an iterator to process the data tomorrow.
程序明天将使用迭代器来处理数据。

She will be explaining the concept of iterators in the next lecture.
她将在下一节课中解释迭代器的概念。


将来完成时 (Future Perfect):
By the end of the day, the iterator will have processed all the data.
到今天结束时，迭代器将处理完所有数据。

He will have implemented the iterator pattern by next week.
他将在下周之前实现迭代器模式。


将来完成进行时 (Future Perfect Continuous):
By next month, the program will have been using the iterator for over a year.
到下个月，程序将已经使用迭代器一年多了。

She will have been studying iterators for several months by the time she graduates.
到她毕业时，她将已经学习迭代器好几个月了。



PHRASE->
create an iterator = 创建一个迭代器
use an iterator = 使用一个迭代器
iterator pattern = 迭代器模式
iterator object = 迭代器对象
loop through an iterator = 循环遍历一个迭代器

