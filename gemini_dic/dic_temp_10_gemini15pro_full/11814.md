array:/əˈreɪ/| n.|数组，阵列，排列，一系列|An ordered series or arrangement of something.|The telescope picked up a vast array of signals from space. 望远镜接收到了来自太空的大量信号。|synonyms: collection, series, range, assembly, group|antonyms: disorder, disorganization|noun

USAGE->

一般现在时 (Simple Present):
The array contains five elements. =  这个数组包含五个元素。
The array stores data efficiently. = 这个数组有效地存储数据。

现在进行时 (Present Continuous):
The program is accessing the array. = 程序正在访问数组。
He is creating a new array for the data. = 他正在为数据创建一个新的数组。

现在完成时 (Present Perfect):
The program has accessed the array multiple times. = 程序已经多次访问了数组。
We have created a dynamic array. = 我们已经创建了一个动态数组。

现在完成进行时 (Present Perfect Continuous):
The system has been processing the array for hours. = 系统已经处理这个数组好几个小时了。
They have been working on optimizing the array. = 他们一直在致力于优化数组。

一般过去时 (Simple Past):
The function returned an array of values. = 函数返回了一个值数组。
We initialized the array with zeros. = 我们用零初始化了数组。

过去进行时 (Past Continuous):
The program was sorting the array when the error occurred. = 程序在排序数组时发生了错误。
They were discussing the array's structure. = 他们当时正在讨论数组的结构。

过去完成时 (Past Perfect):
The program had processed the array before the shutdown. = 程序在关机之前已经处理了数组。
He had initialized the array before calling the function. = 他在调用函数之前已经初始化了数组。

过去完成进行时 (Past Perfect Continuous):
The system had been processing the array for hours before the crash. = 系统在崩溃前已经处理这个数组好几个小时了。
They had been working on the array for weeks. = 他们已经在这个数组上工作了几个星期。

一般将来时 (Simple Future):
The program will process the array tomorrow. = 程序明天将处理这个数组。
The function will return an array of results. = 函数将返回一个结果数组。

将来进行时 (Future Continuous):
The program will be processing the array tomorrow morning. = 程序明天早上将正在处理这个数组。
They will be analyzing the array's contents. = 他们将分析数组的内容。

将来完成时 (Future Perfect):
The program will have processed the array by tomorrow evening. = 到明天晚上，程序将已经处理完这个数组。
They will have finished sorting the array by then. = 到那时，他们将已经完成了数组的排序。

将来完成进行时 (Future Perfect Continuous):
By next week, the system will have been processing the array for a month. = 到下周，系统将已经处理这个数组一个月了。
They will have been working on the array's optimization for several weeks. = 他们将已经在这个数组的优化上工作好几个星期了。


PHRASE->
an array of = 一系列的/各种各样的
two-dimensional array = 二维数组
three-dimensional array = 三维数组
element of an array = 数组元素
access an array = 访问数组


