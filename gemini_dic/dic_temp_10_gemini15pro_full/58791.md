dunter:/ˈdʌntər/| n. |双下划线方法/魔法方法|A method in Python with double underscores at the beginning and end of its name, indicating special behavior.|Example: The `__init__` dunter is called when a new object is created.  `__init__`双下划线方法会在创建新对象时被调用。|Synonym: magic method, special method|Antonym: N/A|noun
USAGE->

一般现在时 (Simple Present):
Python uses dunters to define special methods. = Python使用双下划线方法来定义特殊方法。
The `__str__` dunter controls how an object is represented as a string. = `__str__` 双下划线方法控制对象如何表示为字符串。

现在进行时 (Present Continuous):
The interpreter is calling the `__add__` dunter to perform addition. = 解释器正在调用`__add__` 双下划线方法来执行加法。
We are overriding the `__len__` dunter to customize the length calculation. = 我们正在重写`__len__` 双下划线方法来自定义长度计算。

现在完成时 (Present Perfect):
The `__init__` dunter has initialized the object's attributes. = `__init__` 双下划线方法已经初始化了对象的属性。
The programmer has implemented the `__eq__` dunter for custom equality checks. = 程序员已经实现了`__eq__` 双下划线方法用于自定义相等性检查。

现在完成进行时 (Present Perfect Continuous):
The program has been using the `__getitem__` dunter to access list elements. = 程序一直在使用`__getitem__` 双下划线方法来访问列表元素。
The developer has been working on implementing the `__call__` dunter. = 开发人员一直在致力于实现`__call__` 双下划线方法。

一般过去时 (Simple Past):
The `__del__` dunter was called when the object was destroyed. = 当对象被销毁时，调用了`__del__` 双下划线方法。
We used the `__repr__` dunter to get a string representation of the object. = 我们使用`__repr__` 双下划线方法来获取对象的字符串表示形式。

过去进行时 (Past Continuous):
The program was utilizing the `__iter__` dunter to iterate through a sequence. = 程序正在利用`__iter__` 双下划线方法来迭代序列。
The developer was debugging the `__next__` dunter. = 开发人员正在调试`__next__` 双下划线方法。

过去完成时 (Past Perfect):
The `__enter__` dunter had already been called before the code block was executed. = 在代码块执行之前，已经调用了`__enter__` 双下划线方法。
The `__exit__` dunter had handled the exception before the program terminated. = 在程序终止之前，`__exit__` 双下划线方法已经处理了异常。

过去完成进行时 (Past Perfect Continuous):
The program had been relying on the `__getattr__` dunter for dynamic attribute access. = 程序一直依赖`__getattr__` 双下划线方法进行动态属性访问。
The developer had been debugging the `__setattr__` dunter for hours. = 开发人员已经调试`__setattr__` 双下划线方法好几个小时了。


一般将来时 (Simple Future):
We will use the `__lt__` dunter to compare two objects. = 我们将使用`__lt__` 双下划线方法来比较两个对象。
The `__ge__` dunter will define the greater than or equal to behavior. = `__ge__` 双下划线方法将定义大于或等于的行为。

将来进行时 (Future Continuous):
The program will be utilizing the `__contains__` dunter for membership testing. = 程序将利用`__contains__` 双下划线方法进行成员测试。
The developer will be implementing the `__format__` dunter for custom formatting. = 开发人员将实现`__format__` 双下划线方法以进行自定义格式化。

将来完成时 (Future Perfect):
By the end of the project, we will have implemented several custom dunters. = 到项目结束时，我们将实现几个自定义的双下划线方法。
The programmer will have defined the `__hash__` dunter for use in hash tables. = 程序员将定义`__hash__` 双下划线方法以用于哈希表。


将来完成进行时 (Future Perfect Continuous):
By next year, we will have been using dunters extensively in our Python code. = 到明年，我们将在 Python 代码中广泛使用双下划线方法。
The team will have been working with various dunters for optimal performance. = 团队将一直使用各种双下划线方法来优化性能。



PHRASE->
`__init__` dunter = 构造函数/初始化方法
`__str__` dunter = 字符串表示方法
`__len__` dunter = 长度方法
`__add__` dunter = 加法方法
`__eq__` dunter = 等于方法


注意事项:
词源: "dunder" is a contraction of "double underscore".  "dunder" 是 "double underscore" 的缩写。


