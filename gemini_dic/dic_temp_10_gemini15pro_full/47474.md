deallocating:/diːˈæləkeɪtɪŋ/
verb
释放，解除分配
The system is deallocating memory that is no longer needed. 系统正在释放不再需要的内存。
The program is deallocating the resources. 程序正在释放资源。
releasing, freeing
allocating, reserving
v.

USAGE->

USAGE->
一般现在时 (Simple Present):
The system deallocates memory when it's no longer in use. = 当内存不再使用时，系统会释放它。
The program deallocates resources after they are finished. = 程序在资源使用完毕后释放它们。

现在进行时 (Present Continuous):
The operating system is deallocating memory now. = 操作系统现在正在释放内存。
The application is deallocating unused resources. = 应用程序正在释放未使用的资源。

现在完成时 (Present Perfect):
The program has deallocated the memory successfully. = 程序已成功释放内存。
The system has deallocated all unnecessary resources. = 系统已释放所有不必要的资源。

现在完成进行时 (Present Perfect Continuous):
The system has been deallocating memory for the past hour. = 系统过去一个小时一直在释放内存。
The program has been deallocating resources since it finished its task. = 自从程序完成任务以来，它一直在释放资源。

一般过去时 (Simple Past):
The system deallocated the memory after the program closed. = 程序关闭后，系统释放了内存。
The function deallocated the resources it had used. = 函数释放了它使用的资源。

过去进行时 (Past Continuous):
The system was deallocating memory when the power went out. = 断电时，系统正在释放内存。
The program was deallocating resources while the user was waiting. = 用户等待时，程序正在释放资源。

过去完成时 (Past Perfect):
The system had deallocated the memory before the error occurred. = 在错误发生之前，系统已经释放了内存。
The program had deallocated all resources before it terminated. = 程序终止之前，已经释放了所有资源。

过去完成进行时 (Past Perfect Continuous):
The system had been deallocating memory for several minutes before the crash. = 在崩溃前的几分钟内，系统一直在释放内存。
The program had been deallocating resources for a while before it finished. = 在完成之前，程序已经释放资源一段时间了。

一般将来时 (Simple Future):
The system will deallocate the memory when the program closes. = 程序关闭时，系统将释放内存。
The garbage collector will deallocate unused objects. = 垃圾收集器将释放未使用的对象。

将来进行时 (Future Continuous):
The system will be deallocating memory during the shutdown process. = 在关机过程中，系统将释放内存。
The program will be deallocating resources while it performs other tasks. = 程序在执行其他任务时将释放资源。

将来完成时 (Future Perfect):
By the time the program finishes, the system will have deallocated all unnecessary memory. = 到程序完成时，系统将已经释放所有不必要的内存。
The application will have deallocated the resources by the end of the process. = 到进程结束时，应用程序将已经释放资源。

将来完成进行时 (Future Perfect Continuous):
The system will have been deallocating memory for several hours by the time the server restarts. = 到服务器重新启动时，系统将已经释放内存几个小时了。
The program will have been deallocating resources for a while by the time it completes its task. = 到程序完成任务时，它将已经释放资源一段时间了。


PHRASE->
deallocating memory = 释放内存
deallocating resources = 释放资源
system is deallocating = 系统正在释放
program is deallocating = 程序正在释放
start deallocating = 开始释放

