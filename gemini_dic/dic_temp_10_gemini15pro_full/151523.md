parseInt:/ˌpɑːrsˈɪnt/| function | 解析整数 | A function that converts a string to an integer. | Example: parseInt("10") returns 10.  parseInt("10.5") returns 10. | 近义词: Number, parseFloat | 反义词: String, toString | 词性: function

USAGE->

USAGE->
一般现在时 (Simple Present):
parseInt("123") returns 123. = parseInt("123") 返回 123。

He uses parseInt to convert strings to numbers. = 他使用 parseInt 将字符串转换为数字。

现在进行时 (Present Continuous):
The program is parsing the string using parseInt. = 程序正在使用 parseInt 解析字符串。

She is converting the input to a number using parseInt right now. = 她现在正在使用 parseInt 将输入转换为数字。

现在完成时 (Present Perfect):
We have parsed the string using parseInt. = 我们已经使用 parseInt 解析了字符串。

The function has successfully converted the string to an integer using parseInt. = 该函数已使用 parseInt 成功将字符串转换为整数。

现在完成进行时 (Present Perfect Continuous):
The system has been parsing data using parseInt all day. = 系统一整天都在使用 parseInt 解析数据。

He has been using parseInt to process the input for hours. = 他使用 parseInt 处理输入已经好几个小时了。


一般过去时 (Simple Past):
I parsed the string using parseInt. = 我使用 parseInt 解析了字符串。

The function returned an integer after parsing the string with parseInt. =  该函数使用 parseInt 解析字符串后返回了一个整数。

过去进行时 (Past Continuous):
I was parsing the data with parseInt when the error occurred. = 当错误发生时，我正在使用 parseInt 解析数据。

She was using parseInt to convert the input when the system crashed. = 系统崩溃时，她正在使用 parseInt 转换输入。

过去完成时 (Past Perfect):
We had parsed the string with parseInt before processing it further. = 在进一步处理之前，我们已经使用 parseInt 解析了字符串。

He had used parseInt to convert the string before storing it in the database. = 在将字符串存储到数据库之前，他已经使用 parseInt 转换了它。

过去完成进行时 (Past Perfect Continuous):
The program had been parsing data with parseInt for hours before it finished. = 程序使用 parseInt 解析数据数小时后才完成。

They had been using parseInt to process the input for days before finding the bug. = 他们在发现错误之前已经使用 parseInt 处理输入好几天了。


一般将来时 (Simple Future):
I will parse the string using parseInt. = 我将使用 parseInt 解析字符串。

The program will convert the input to an integer using parseInt. = 程序将使用 parseInt 将输入转换为整数。


将来进行时 (Future Continuous):
I will be parsing the data with parseInt tomorrow. = 我明天将使用 parseInt 解析数据。

The system will be using parseInt to process the input next week. = 系统下周将使用 parseInt 处理输入。


将来完成时 (Future Perfect):
By next week, I will have parsed all the strings using parseInt. = 到下周，我将使用 parseInt 解析所有字符串。

The program will have converted all the inputs to integers using parseInt by the end of the month. = 到月底，程序将使用 parseInt 将所有输入转换为整数。


将来完成进行时 (Future Perfect Continuous):
By next year, I will have been parsing data with parseInt for five years. = 到明年，我将使用 parseInt 解析数据五年了。

The system will have been using parseInt to process input for a decade by 2030. = 到 2030 年，该系统将使用 parseInt 处理输入十年。


虚拟语气:
If I needed to convert a string to an integer, I would use parseInt. = 如果我需要将字符串转换为整数，我会使用 parseInt。

If she had used parseInt, she would have gotten the correct result. = 如果她使用了 parseInt，她会得到正确的结果。


被动语态:
The string was parsed using parseInt. = 字符串已使用 parseInt 解析。

The input is being parsed with parseInt by the program. = 输入正由程序使用 parseInt 解析。


疑问句:
What does parseInt return when given a non-numeric string? = 当给定一个非数字字符串时，parseInt 返回什么？

How do you use parseInt to convert a string to an integer in JavaScript? = 你如何在 JavaScript 中使用 parseInt 将字符串转换为整数？



否定句:
The string was not parsed using parseInt. = 字符串未使用 parseInt 解析。

He did not use parseInt to convert the string. = 他没有使用 parseInt 来转换字符串。



PHRASE->
parseInt with radix =  parseInt(string, radix)  带有基数的 parseInt
parseInt and parseFloat = parseInt 和 parseFloat
using parseInt in JavaScript = 在 JavaScript 中使用 parseInt
parseInt for string conversion = 用于字符串转换的 parseInt
parseInt to convert to number = parseInt 转换为数字

PHRASE->
parseInt("10", 10); // Parses "10" as a decimal number (base 10). = parseInt("10", 10); // 将“10”解析为十进制数（基数为 10）。
parseInt("1A", 16); // Parses "1A" as a hexadecimal number (base 16). = parseInt("1A", 16); // 将“1A”解析为十六进制数（基数为 16）。
parseInt("11", 2); // Parses "11" as a binary number (base 2). = parseInt("11", 2); // 将“11”解析为二进制数（基数为 2）。
parseInt("010"); // Parses "010" as an octal number in older versions of JavaScript. Be careful! = parseInt("010"); // 在旧版本的 JavaScript 中将“010”解析为八进制数。小心！
parseInt("FF", 16); // Parses "FF" as a hexadecimal number (base 16). = parseInt("FF", 16); // 将“FF”解析为十六进制数（基数为 16）。
