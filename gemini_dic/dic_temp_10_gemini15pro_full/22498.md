boolean:/ˈbuːliən/| adj.|布尔的，逻辑的|A boolean variable can have only two possible values: true or false.|Example: The result of the comparison is a boolean value. 比较的结果是一个布尔值。|Example: In programming, boolean logic is fundamental. 在编程中，布尔逻辑是基础。|Synonym: logical, binary|Antonym: non-boolean|Word type: adjective

ETYMOLOGY->
Named after George Boole, a 19th-century English mathematician who invented Boolean algebra.

USAGE->

一般现在时 (Simple Present):
The function returns a boolean value.  这个函数返回一个布尔值。
This variable stores a boolean.  这个变量存储一个布尔值。

现在进行时 (Present Continuous):
The program is checking a boolean condition.  程序正在检查一个布尔条件。
The system is evaluating a boolean expression. 系统正在评估一个布尔表达式。

现在完成时 (Present Perfect):
The code has used a boolean variable to control the flow.  代码已经使用了一个布尔变量来控制流程。
The test has returned a boolean result. 测试已经返回了一个布尔结果。

现在完成进行时 (Present Perfect Continuous):
The program has been using boolean logic for several iterations. 程序已经使用布尔逻辑进行了几次迭代。
The system has been processing boolean data all day. 系统一整天都在处理布尔数据。

一般过去时 (Simple Past):
The function returned a boolean value. 函数返回了一个布尔值。
The variable stored a boolean.  变量存储了一个布尔值。

过去进行时 (Past Continuous):
The program was checking a boolean condition when it crashed. 程序崩溃时正在检查一个布尔条件。
The system was processing boolean data when the power went out.  断电时系统正在处理布尔数据。

过去完成时 (Past Perfect):
The code had already used a boolean variable before the error occurred.  错误发生之前，代码已经使用了一个布尔变量。
The test had returned a boolean result before the program terminated. 程序终止之前，测试已经返回了一个布尔结果。

过去完成进行时 (Past Perfect Continuous):
The program had been using boolean logic for hours before it finished. 程序在完成之前已经使用了布尔逻辑数小时。
The system had been processing boolean data all night before the issue was discovered. 在发现问题之前，系统整晚都在处理布尔数据。


一般将来时 (Simple Future):
The function will return a boolean value.  函数将返回一个布尔值。
This variable will store a boolean.  这个变量将存储一个布尔值。

将来进行时 (Future Continuous):
The program will be checking a boolean condition at that time. 到那时程序将正在检查一个布尔条件。
The system will be processing boolean data tomorrow.  系统明天将处理布尔数据。


将来完成时 (Future Perfect):
By next week, the code will have implemented boolean logic. 到下周，代码将已经实现了布尔逻辑。
By tomorrow, the system will have processed all the boolean data. 到明天，系统将已经处理了所有的布尔数据。

将来完成进行时 (Future Perfect Continuous):
By next year, the program will have been using boolean logic for a decade. 到明年，程序将已经使用布尔逻辑十年了。
By the end of the project, the system will have been processing boolean data for months. 到项目结束时，系统将已经处理布尔数据数月了。



PHRASE->
boolean value 布尔值
boolean expression 布尔表达式
boolean logic 布尔逻辑
boolean variable 布尔变量
boolean data type 布尔数据类型


