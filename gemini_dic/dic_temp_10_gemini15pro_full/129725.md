non-empty:/ˌnɒn ˈemti/ /ˌnɑːn ˈempti/ | adj. | 非空的 | Containing something; not empty. |  This function requires a non-empty list as input.  这个函数需要一个非空列表作为输入。

| Example 1:  Please ensure the text field is non-empty before submitting the form. 请确保在提交表单之前文本字段不是空的。

| Example 2: The set of natural numbers is non-empty. 自然数的集合是非空的。

| Synonym: full, occupied, filled | Antonym: empty, vacant, void | Adjective


USAGE->

一般现在时 (Simple Present):
Is the box non-empty?  这个盒子是非空的吗？
The list is non-empty.  这个列表是非空的。


现在进行时 (Present Continuous):
The database is becoming non-empty as we add data. 当我们添加数据时，数据库正在变得非空。
He is checking if the string is non-empty. 他正在检查字符串是否非空。


现在完成时 (Present Perfect):
The queue has become non-empty since we last checked. 自从我们上次检查以来，队列已经变为非空。
She has ensured the field is non-empty. 她已确保该字段为非空。


现在完成进行时 (Present Perfect Continuous):
The system has been adding data, making the table non-empty. 系统一直在添加数据，使表非空。
They have been working to make the set non-empty.  他们一直在努力使集合非空。



一般过去时 (Simple Past):
The file was non-empty when I last opened it.  我上次打开文件时，它不是空的。
He confirmed that the variable was non-empty. 他确认该变量非空。


过去进行时 (Past Continuous):
I was checking if the array was non-empty when the error occurred. 当错误发生时，我正在检查数组是否非空。
She was filling the container, making it non-empty. 她正在填充容器，使其非空。


过去完成时 (Past Perfect):
The list had become non-empty before the function was called.  在调用函数之前，列表已经变为非空。
He had verified that the string was non-empty.  他已验证该字符串非空。


过去完成进行时 (Past Perfect Continuous):
The program had been adding elements, making the list non-empty. 程序一直在添加元素，使列表非空。
They had been working on the project, ensuring the database was non-empty. 他们一直在进行这个项目，确保数据库是非空的。


一般将来时 (Simple Future):
The directory will be non-empty after the files are uploaded. 文件上传后，目录将非空。
The variable will become non-empty when the user inputs data. 当用户输入数据时，变量将变为非空。


将来进行时 (Future Continuous):
The system will be checking if the field is non-empty before proceeding. 系统将在继续之前检查字段是否非空。
We will be adding data to the table, making it non-empty. 我们将向表中添加数据，使其非空。


将来完成时 (Future Perfect):
By tomorrow, the database will have become non-empty. 到明天，数据库将变为非空。
By the end of the week, they will have made the set non-empty. 到本周末，他们将使集合非空。



将来完成进行时 (Future Perfect Continuous):
By next month, the system will have been storing data, making the drive non-empty. 到下个月，系统将一直在存储数据，使驱动器非空。
By the time the project is finished, they will have been working on ensuring the database remains non-empty. 到项目完成时，他们将一直在努力确保数据库保持非空。


PHRASE->
a non-empty set  一个非空集合
a non-empty string  一个非空字符串
a non-empty list 一个非空列表
ensure non-empty  确保非空
check for non-empty 检查是否非空
