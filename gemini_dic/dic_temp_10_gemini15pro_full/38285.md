concatenating:/kənˈkætɪneɪtɪŋ/ | v. | 连接，串联 |  The process of linking things together in a series or chain. |  The program is concatenating the two files.  程序正在连接这两个文件。 | join, combine, link, append | separate, divide | verb

concatenation:/kənˌkætɪˈneɪʃn/ | n. | 连接，串联 | The action of linking things together in a series or chain. | The concatenation of these events led to the crisis. 这些事件的串联导致了这场危机。 | combination, union, linking | separation, division | noun


USAGE->

一般现在时 (Simple Present):
The system concatenates the data files automatically. 系统自动连接数据文件。
He concatenates the strings using a special character. 他使用特殊字符连接字符串。

现在进行时 (Present Continuous):
The software is concatenating the video clips now.  该软件正在连接视频剪辑。
She is concatenating the different sections of the report. 她正在连接报告的不同部分。

现在完成时 (Present Perfect):
The program has concatenated all the necessary files. 程序已经连接了所有必要的文件。
He has concatenated the strings to create a longer one. 他连接了字符串以创建一个更长的字符串。


现在完成进行时 (Present Perfect Continuous):
The computer has been concatenating the data for hours.  计算机已经连接数据好几个小时了。
They have been concatenating the audio files to create a single track.  他们一直在连接音频文件以创建一个单一的音轨。


一般过去时 (Simple Past):
The script concatenated the log files. 脚本连接了日志文件。
She concatenated the two lists of names. 她连接了两个姓名列表。


过去进行时 (Past Continuous):
The program was concatenating the files when the power went out.  断电时，程序正在连接文件。
He was concatenating the strings when he noticed an error.  当他注意到一个错误时，他正在连接字符串。


过去完成时 (Past Perfect):
The system had concatenated the data before the error occurred.  在错误发生之前，系统已经连接了数据。
She had concatenated the files before sending them.  在发送文件之前，她已经连接了这些文件。

过去完成进行时 (Past Perfect Continuous):
The server had been concatenating the data for days before it crashed. 服务器在崩溃前已经连接数据好几天了。
He had been concatenating the strings for hours before he realized there was a simpler way. 在意识到有更简单的方法之前，他连接字符串已经好几个小时了。

一般将来时 (Simple Future):
The program will concatenate the files automatically tomorrow.  程序明天将自动连接文件。
She will concatenate the strings using a specific function. 她将使用特定函数连接字符串。

将来进行时 (Future Continuous):
The system will be concatenating the data overnight.  系统将在一夜之间连接数据。
He will be concatenating the video clips during the rendering process. 他将在渲染过程中连接视频剪辑。

将来完成时 (Future Perfect):
By next week, the software will have concatenated all the data.  到下周，软件将连接所有数据。
By the end of the day, she will have concatenated all the necessary files.  到今天结束时，她将连接所有必要的文件。

将来完成进行时 (Future Perfect Continuous):
By next year, the system will have been concatenating data for a decade. 到明年，系统将已经连接数据十年了。
By the time the project is finished, she will have been concatenating video clips for months.  到项目完成时，她将已经连接视频剪辑好几个月了。


PHRASE->
concatenate strings  连接字符串
concatenate files  连接文件
concatenate data  连接数据
concatenate lists  连接列表
concatenate arrays 连接数组
