byte:/baɪt/| n.|字节|A unit of digital information in computer science, usually consisting of eight bits.|This file is 2 megabytes in size.  这个文件大小为 2 兆字节。|Synonyms: octet (in networking)|Antonyms: N/A|Noun

USAGE->

一般现在时 (Simple Present):
A byte contains eight bits. 一个字节包含八位。
Many files are measured in bytes. 很多文件以字节为单位进行衡量。


现在进行时 (Present Continuous):
The computer is processing bytes of data.  计算机正在处理字节数据。
The program is reading bytes from the file. 程序正在从文件中读取字节。


现在完成时 (Present Perfect):
The system has processed millions of bytes. 系统已经处理了数百万字节。
The program has written several bytes to the disk. 程序已经向磁盘写入了一些字节。


现在完成进行时 (Present Perfect Continuous):
The network has been transferring bytes of data all day. 网络一整天都在传输字节数据。
The computer has been reading bytes from the memory for hours. 计算机已经从内存中读取字节数小时了。


一般过去时 (Simple Past):
The program read a byte from the file. 程序从文件中读取了一个字节。
The computer processed the byte quickly. 计算机快速地处理了这个字节。


过去进行时 (Past Continuous):
The computer was processing bytes of data when the power went out.  断电时，计算机正在处理字节数据。
The program was writing bytes to the disk when it crashed. 程序崩溃时，它正在向磁盘写入字节。


过去完成时 (Past Perfect):
The system had processed the byte before the error occurred. 在错误发生之前，系统已经处理了这个字节。
The program had written the byte to memory before it terminated. 程序终止之前，它已经将字节写入内存。


过去完成进行时 (Past Perfect Continuous):
The network had been transferring bytes of data for hours before the connection failed.  连接失败之前，网络已经传输了数小时的字节数据。
The computer had been reading bytes from the memory for a while before it froze. 计算机冻结之前，它已经从内存中读取字节一段时间了。


一般将来时 (Simple Future):
The program will read the byte from the file. 程序将从文件中读取字节。
The computer will process the byte quickly. 计算机将快速处理字节。


将来进行时 (Future Continuous):
The computer will be processing bytes of data tomorrow. 计算机明天将处理字节数据。
The program will be writing bytes to the disk next week. 程序下周将向磁盘写入字节。


将来完成时 (Future Perfect):
By tomorrow, the system will have processed millions of bytes. 到明天，系统将已经处理了数百万字节。
By next week, the program will have written several bytes to the disk. 到下周，程序将已经向磁盘写入了一些字节。


将来完成进行时 (Future Perfect Continuous):
By next month, the network will have been transferring bytes of data for a year. 到下个月，网络将已经传输字节数据一年了。
By the end of the year, the computer will have been reading bytes from the memory for months. 到年底，计算机将已经从内存中读取字节数月了。



PHRASE->
kilobyte (KB) 千字节
megabyte (MB) 兆字节
gigabyte (GB) 吉字节
terabyte (TB) 太字节
petabyte (PB) 拍字节
