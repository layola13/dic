compiler:/kəmˈpaɪlər/ (US), /kəmˈpaɪlə(r)/ (UK)| n.|编译器|A program that translates source code written in a high-level language (like Java or C++) into a set of machine-language instructions that can be understood by a digital computer's CPU.|The compiler transformed the code into an executable file.|近义词:translator|反义词:interpreter|词性:noun

USAGE->

一般现在时 (Simple Present):
The compiler translates source code into machine code. 编译器将源代码翻译成机器码。
This compiler supports multiple programming languages.  这个编译器支持多种编程语言。

现在进行时 (Present Continuous):
The compiler is processing the code right now. 编译器正在处理代码。
He is using a new compiler for his project. 他正在为他的项目使用一个新的编译器。

现在完成时 (Present Perfect):
The compiler has finished compiling the program. 编译器已经完成了程序的编译。
They have used this compiler for many years. 他们已经使用这个编译器很多年了。

现在完成进行时 (Present Perfect Continuous):
The compiler has been working for hours. 编译器已经工作了几个小时。
She has been trying to fix the compiler error all day. 她一整天都在尝试修复编译器错误。

一般过去时 (Simple Past):
The compiler generated an error message. 编译器生成了一个错误信息。
He downloaded the compiler yesterday. 他昨天下载了编译器。

过去进行时 (Past Continuous):
The compiler was compiling the code when the power went out.  断电时，编译器正在编译代码。
I was using that compiler when I first learned to program. 我第一次学习编程时就在使用那个编译器。

过去完成时 (Past Perfect):
The compiler had finished compiling before I ran the program. 在我运行程序之前，编译器已经完成了编译。
He had already installed the compiler before starting the project. 在开始项目之前，他已经安装了编译器。

过去完成进行时 (Past Perfect Continuous):
The compiler had been running for hours before it crashed. 编译器运行了几个小时后崩溃了。
They had been using that compiler for years before switching to a new one. 在切换到新的编译器之前，他们已经使用那个编译器很多年了。

一般将来时 (Simple Future):
The compiler will optimize the code for performance. 编译器将优化代码以提高性能。
I will install the new compiler tomorrow. 我明天将安装新的编译器。


将来进行时 (Future Continuous):
The compiler will be processing the code overnight. 编译器将通宵处理代码。
We will be using this compiler for the next project. 我们将在下一个项目中使用这个编译器。

将来完成时 (Future Perfect):
By next week, the compiler will have finished processing all the files. 到下周，编译器将完成所有文件的处理。
They will have released the new version of the compiler by the end of the year.  到年底，他们将发布新版本的编译器。

将来完成进行时 (Future Perfect Continuous):
By the time the project is finished, we will have been using this compiler for over a year.  到项目完成时，我们将已经使用这个编译器一年多了。
He will have been working on improving the compiler for five years by next year. 到明年，他将致力于改进编译器五年了。

虚拟语气:
If I had a better compiler, I could compile this code faster. 如果我有一个更好的编译器，我可以更快地编译这段代码。
I wish I had a compiler that could detect all errors. 我希望我有一个可以检测所有错误的编译器。

被动语态:
The code was compiled successfully. 代码编译成功。
The new compiler is being developed by a team of experts. 新的编译器正在由一个专家团队开发。

疑问句:
Which compiler are you using? 你在使用哪个编译器？
What is the output of the compiler? 编译器的输出是什么？

否定句:
This code doesn't compile with this compiler.  这段代码无法用这个编译器编译。
I haven't used that compiler before. 我以前没有使用过那个编译器。



PHRASE->
compiler error 编译器错误
compiler optimization 编译器优化
just-in-time compiler 即时编译器
cross-compiler 交叉编译器
source-to-source compiler 源到源编译器


词源: From compile + -er.  Compile comes from the Old French compiler, meaning "to collect, gather, heap up," ultimately from the Latin compilare, "to plunder, rob, pillage."  The meaning shifted to "collect and arrange literary materials" and then to "translate into machine code."
