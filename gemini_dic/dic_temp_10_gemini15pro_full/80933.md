gpus:/ˈdʒiːpiːjuːz/| n.|图形处理器（Graphics Processing Units）的复数形式|Multiple Graphics Processing Units, specialized electronic circuits designed to rapidly manipulate and alter memory to accelerate the creation of images in a frame buffer intended for output to a display device.|Our new server has four powerful GPUs. 我们的新服务器有四个强大的GPU。|The researchers used GPUs to accelerate their computations. 研究人员使用GPU来加速他们的计算。|近义词：graphic cards, video cards|反义词:CPU (Central Processing Unit)|noun


USAGE->

一般现在时 (Simple Present):
The system uses two GPUs for rendering.  系统使用两个GPU进行渲染。
GPUs process data much faster than CPUs for certain tasks. 对于某些任务，GPU处理数据比CPU快得多。

现在进行时 (Present Continuous):
The program is utilizing all available GPUs. 程序正在利用所有可用的GPU。
He is installing new GPUs in the server. 他正在服务器中安装新的GPU。

现在完成时 (Present Perfect):
The price of GPUs has decreased significantly. GPU的价格已经大幅下降。
We have used these GPUs for several years. 我们已经使用这些GPU好几年了。

现在完成进行时 (Present Perfect Continuous):
Researchers have been developing new GPU architectures. 研究人员一直在开发新的GPU架构。
The system has been relying on these GPUs for high-performance computing. 系统一直依靠这些GPU进行高性能计算。

一般过去时 (Simple Past):
We purchased four new GPUs last month. 我们上个月购买了四个新的GPU。
The old GPUs didn't have enough memory. 旧的GPU没有足够的内存。

过去进行时 (Past Continuous):
The system was using the GPUs for machine learning when the power went out.  断电时，系统正在使用GPU进行机器学习。
He was testing the new GPUs when he discovered a problem. 他在测试新的GPU时发现了一个问题。

过去完成时 (Past Perfect):
We had already installed the GPUs before the software arrived.  在软件到达之前，我们已经安装了GPU。
They had used older GPUs for years before upgrading.  在升级之前，他们已经使用了多年的旧GPU。

过去完成进行时 (Past Perfect Continuous):
The researchers had been working with GPUs for months before they published their findings. 研究人员在发表他们的发现之前已经使用GPU工作了好几个月。
The company had been developing its own GPUs before it decided to partner with another firm.  该公司在决定与另一家公司合作之前一直在开发自己的GPU。

一般将来时 (Simple Future):
We will upgrade our GPUs next year. 明年我们将升级GPU。
New games will require more powerful GPUs. 新游戏将需要更强大的GPU。

将来进行时 (Future Continuous):
Next week, we will be testing the new GPUs. 下周，我们将测试新的GPU。
The team will be utilizing the GPUs for their research project. 团队将利用GPU进行他们的研究项目。

将来完成时 (Future Perfect):
By the end of the year, we will have installed the new GPUs. 到今年年底，我们将安装新的GPU。
By next month, the team will have completed their GPU-based simulations. 到下个月，团队将完成基于GPU的模拟。

将来完成进行时 (Future Perfect Continuous):
By next year, we will have been using these GPUs for five years. 到明年，我们将已经使用这些GPU五年了。
By the time the project is finished, they will have been working with GPUs for over a year. 到项目完成时，他们将已经使用GPU工作了一年多。


PHRASE->
high-end GPUs = 高端GPU
multiple GPUs = 多个GPU
dedicated GPUs = 专用GPU
integrated GPUs = 集成GPU
GPU performance = GPU性能
