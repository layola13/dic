array's:/əˈreɪz/| n.|数组的|The possessive form of "array," indicating ownership or association.  It refers to something belonging to or associated with an array.|"The array's size is determined at compile time." 数组的大小在编译时确定。|近义词: (No direct synonym for a possessive.  Could consider alternatives depending on context, like "the elements of the array," "the data within the array," or "the array's contents") |反义词: N/A|词性:Possessive noun (formed from a noun)


USAGE->

一般现在时 (Simple Present):
The array's length determines its capacity.  数组的长度决定了它的容量。
The array's elements are accessed by their index.  数组的元素通过它们的索引访问。

现在进行时 (Present Continuous):
The program is accessing the array's data. 程序正在访问数组的数据。
The developer is modifying the array's contents. 开发人员正在修改数组的内容。

现在完成时 (Present Perfect):
The array's size has been initialized. 数组的大小已初始化。
The program has accessed the array's first element. 程序访问了数组的第一个元素。

现在完成进行时 (Present Perfect Continuous):
The system has been processing the array's data for hours. 系统已经处理数组的数据好几个小时了。
The algorithm has been sorting the array's elements efficiently.  算法一直在有效地排序数组的元素。

一般过去时 (Simple Past):
The array's initial size was too small. 数组的初始大小太小了。
The program accessed the array's last element successfully. 程序成功访问了数组的最后一个元素。

过去进行时 (Past Continuous):
The system was processing the array's data when the error occurred.  系统正在处理数组的数据时发生了错误。
The algorithm was sorting the array's elements when it ran out of memory. 算法正在排序数组的元素时内存不足。

过去完成时 (Past Perfect):
The array's size had been initialized before the function call. 数组的大小在函数调用之前已经初始化。
The program had accessed all the array's elements before terminating. 程序在终止之前访问了所有数组的元素。

过去完成进行时 (Past Perfect Continuous):
The system had been processing the array's data for hours before the crash. 系统崩溃前已经处理了几个小时的数组数据。
The algorithm had been sorting the array's elements for a long time before completing. 算法在完成之前已经对数组的元素进行了很长时间的排序。


一般将来时 (Simple Future):
The array's size will be adjusted dynamically. 数组的大小将动态调整。
The program will access the array's elements in reverse order. 程序将以相反的顺序访问数组的元素。


将来进行时 (Future Continuous):
The system will be processing the array's data tomorrow. 系统明天将处理数组的数据。
The algorithm will be sorting the array's elements during the next cycle.  算法将在下一个周期中对数组的元素进行排序。


将来完成时 (Future Perfect):
By next week, the program will have processed all the array's data. 到下周，程序将处理完所有数组的数据。
The algorithm will have sorted the array's elements by the end of the day.  算法将在一天结束时对数组的元素进行排序。


将来完成进行时 (Future Perfect Continuous):
By next year, the system will have been processing the array's data for five years. 到明年，系统将已经处理数组的数据五年了。
The algorithm will have been sorting the array's elements continuously for days. 算法将连续几天对数组的元素进行排序。


PHRASE->
the array's length 数组的长度
the array's capacity 数组的容量
the array's elements 数组的元素
the array's size 数组的大小
the array's contents 数组的内容
