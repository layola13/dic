asciis:/ˈæskiːz/| n. |ASCII码（复数形式）|American Standard Code for Information Interchange (ASCII) is a character encoding standard for electronic communication. ASCII codes represent text in computers, telecommunications equipment, and other devices.|Most keyboards are based on the ASCII character set.|Synonym: ASCII character set|Antonym: Unicode|noun


USAGE->

一般现在时 (Simple Present):
ASCII represents characters as numbers. = ASCII 将字符表示为数字。
Many devices still use ASCII for basic text communication. = 许多设备仍然使用 ASCII 进行基本的文本通信。

现在进行时 (Present Continuous):
We are learning about how ASCII codes work. = 我们正在学习 ASCII 码是如何工作的。
The system is converting the text into ASCII. = 系统正在将文本转换为 ASCII。

现在完成时 (Present Perfect):
ASCII has been a fundamental standard for decades. = ASCII 几十年来一直是一项基本标准。
They have used ASCII for their data transmission. = 他们一直使用 ASCII 进行数据传输。

现在完成进行时 (Present Perfect Continuous):
Programmers have been using ASCII since the early days of computing. = 程序员从计算机早期就开始使用 ASCII。
We have been studying ASCII and its limitations. = 我们一直在研究 ASCII 及其局限性。


一般过去时 (Simple Past):
They used ASCII to encode the message. = 他们使用 ASCII 编码信息。
ASCII was developed in the early 1960s. = ASCII 是在 20 世纪 60 年代初开发的。

过去进行时 (Past Continuous):
We were discussing the limitations of ASCII when the power went out. = 我们正在讨论 ASCII 的局限性时停电了。
The computer was processing the ASCII data when it crashed. = 计算机在处理 ASCII 数据时崩溃了。

过去完成时 (Past Perfect):
Before Unicode, ASCII had been the dominant character encoding. = 在 Unicode 之前，ASCII 一直是主要的字符编码。
They had already encoded the file in ASCII before realizing the need for Unicode. = 他们在意识到需要 Unicode 之前就已经用 ASCII 编码了文件。


过去完成进行时 (Past Perfect Continuous):
They had been working with ASCII for years before switching to Unicode. = 在切换到 Unicode 之前，他们已经使用 ASCII 多年了。
She had been studying ASCII codes for weeks before the exam. = 她在考试前几周一直在学习 ASCII 码。


一般将来时 (Simple Future):
We will use ASCII for this simple text file. = 我们将对这个简单的文本文件使用 ASCII。
The system will convert the data to ASCII automatically. = 系统将自动把数据转换为ASCII。

将来进行时 (Future Continuous):
We will be using ASCII for the data transfer next week. = 下周我们将使用 ASCII 进行数据传输。
The system will be processing ASCII data during the downtime. = 系统将在停机期间处理 ASCII 数据。

将来完成时 (Future Perfect):
By the end of the project, we will have converted all the files to ASCII. = 到项目结束时，我们将把所有文件转换为 ASCII。
They will have finished encoding the data in ASCII by tomorrow. = 到明天他们将完成用 ASCII 编码数据。


将来完成进行时 (Future Perfect Continuous):
By next year, we will have been using ASCII for over a decade. = 到明年，我们将已经使用 ASCII 超过十年了。
By the time you finish the course, you will have been studying ASCII for several weeks. = 当你完成课程时，你将已经学习 ASCII 好几周了。


虚拟语气:
If I were to choose a character encoding, I would use ASCII for its simplicity. = 如果我要选择一种字符编码，我会因为其简单性而使用 ASCII。
I wish all systems still used ASCII. = 我希望所有系统仍然使用 ASCII。

被动语态:
The data was encoded using ASCII. = 数据使用 ASCII 编码。
ASCII is still used by many legacy systems. = ASCII 仍然被许多遗留系统使用。

疑问句:
Are these characters represented in ASCII? = 这些字符是用 ASCII 表示的吗？
What is the ASCII code for the letter "A"? = 字母“A” 的 ASCII 码是多少？

否定句:
This file is not encoded in ASCII. = 此文件不是用 ASCII 编码的。
They did not use ASCII for the data transfer. = 他们没有使用 ASCII 进行数据传输。


PHRASE->
ASCII code = ASCII码
ASCII character set = ASCII字符集
ASCII table = ASCII表
convert to ASCII = 转换为 ASCII
based on ASCII = 基于 ASCII
