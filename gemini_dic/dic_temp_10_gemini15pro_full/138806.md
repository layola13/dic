numpy:/ˈnʌmpaɪ/ (US), /ˈnʌmpi/ (UK)| n.|NumPy（Numerical Python的缩写）|NumPy is a Python library used for working with arrays. It also has functions for working in domain of linear algebra, fourier transform, and matrices.|We used NumPy to perform complex mathematical operations on the data.|近义词:scipy (partially), pandas (partially)|反义词: N/A|名词

USAGE->

一般现在时 (Simple Present):
NumPy simplifies array manipulation. = NumPy简化了数组操作。
NumPy provides efficient tools for numerical computation. = NumPy为数值计算提供了高效的工具。

现在进行时 (Present Continuous):
We are using NumPy to process this large dataset. = 我们正在使用NumPy处理这个大型数据集。
He is learning how to use NumPy for his research. = 他正在学习如何使用NumPy进行研究。

现在完成时 (Present Perfect):
I have used NumPy for several data analysis projects. = 我在几个数据分析项目中使用过NumPy。
She has implemented the algorithm using NumPy. = 她已经使用NumPy实现了该算法。

现在完成进行时 (Present Perfect Continuous):
They have been working with NumPy for months. = 他们使用NumPy已经好几个月了。
We have been exploring the different functions of NumPy. = 我们一直在探索NumPy的不同功能。

一般过去时 (Simple Past):
We used NumPy to analyze the experimental data. = 我们使用NumPy分析了实验数据。
He learned the basics of NumPy last week. = 他上周学习了NumPy的基础知识。

过去进行时 (Past Continuous):
I was using NumPy when the computer crashed. = 电脑崩溃时我正在使用NumPy。
She was studying the NumPy documentation. = 她正在学习NumPy文档。

过去完成时 (Past Perfect):
I had already installed NumPy before starting the project. = 在开始项目之前，我已经安装了NumPy。
They had used NumPy in a previous project. = 他们在之前的项目中使用过NumPy。

过去完成进行时 (Past Perfect Continuous):
We had been using NumPy for a year before switching to another library. = 在切换到另一个库之前，我们已经使用NumPy一年了。
He had been learning NumPy for several months. = 他学习NumPy已经好几个月了。

一般将来时 (Simple Future):
We will use NumPy for the next phase of the project. = 我们将在项目的下一阶段使用NumPy。
I will learn more about NumPy in the future. = 我将来会学习更多关于NumPy的知识。

将来进行时 (Future Continuous):
I will be using NumPy extensively in my new role. = 在我的新职位中，我将广泛使用NumPy。
They will be working with NumPy on the new server. = 他们将在新服务器上使用NumPy。

将来完成时 (Future Perfect):
By next month, I will have finished the NumPy tutorial. = 到下个月，我将完成NumPy教程。
We will have implemented the algorithm using NumPy by the end of the year. = 到年底，我们将使用NumPy实现该算法。

将来完成进行时 (Future Perfect Continuous):
By next year, I will have been using NumPy for two years. = 到明年，我将已经使用NumPy两年了。
They will have been working with NumPy for over a decade by the time the project is complete. = 到项目完成时，他们将已经使用NumPy超过十年了。


PHRASE->
NumPy array = NumPy数组
NumPy functions = NumPy函数
import NumPy = 导入NumPy
NumPy data types = NumPy数据类型
NumPy operations =  NumPy运算
