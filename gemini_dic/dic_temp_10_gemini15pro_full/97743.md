initialized: /ɪˈnɪʃəlaɪzd/ | adj. | 已初始化的，已设定初始值的 |  Having been given an initial value or state.  The process typically involves setting variables, counters, or other data structures to a known starting point before they are used in a program or system. | The program initialized the variables to zero before starting the calculations. 程序在开始计算之前将变量初始化为零。| The system was initialized with the default settings. 系统使用默认设置进行了初始化。 |  set up, prepared, configured, bootstrapped | uninitialized, unset | adjective


USAGE->

一般现在时 (Simple Present):
The program initializes the variables every time it starts.  程序每次启动时都会初始化变量。
The system initializes with the default settings. 系统使用默认设置初始化。


现在进行时 (Present Continuous):
The system is initializing now, please wait. 系统正在初始化，请稍候。
The program is initializing the database connection. 程序正在初始化数据库连接。


现在完成时 (Present Perfect):
The program has initialized the variables successfully. 程序已成功初始化变量。
The system has initialized and is ready to use. 系统已初始化并准备就绪。


现在完成进行时 (Present Perfect Continuous):
The system has been initializing for several minutes. 系统已经初始化几分钟了。
The program has been initializing the network settings. 程序一直在初始化网络设置。


一般过去时 (Simple Past):
The program initialized the variables before starting the calculations. 程序在开始计算之前初始化了变量。
The system initialized successfully yesterday. 系统昨天成功初始化。


过去进行时 (Past Continuous):
The system was initializing when the power went out.  断电时系统正在初始化。
The program was initializing the database when the error occurred.  发生错误时，程序正在初始化数据库。


过去完成时 (Past Perfect):
The program had initialized the variables before the error occurred. 在错误发生之前，程序已经初始化了变量。
The system had initialized successfully before the user logged in. 在用户登录之前，系统已经成功初始化。


过去完成进行时 (Past Perfect Continuous):
The system had been initializing for an hour before it finally started. 系统初始化了一个小时才最终启动。
The program had been initializing the network settings for several minutes before the connection was established. 在连接建立之前，程序已经初始化网络设置几分钟了。


一般将来时 (Simple Future):
The program will initialize the variables when it starts. 程序启动时将初始化变量。
The system will initialize with the new settings tomorrow. 系统明天将使用新设置进行初始化。


将来进行时 (Future Continuous):
The system will be initializing tomorrow morning. 系统将在明天早上初始化。
The program will be initializing the database during the installation process. 程序将在安装过程中初始化数据库。


将来完成时 (Future Perfect):
By tomorrow, the system will have initialized successfully. 到明天，系统将已经成功初始化。
The program will have initialized all the necessary variables by the time the user logs in.  在用户登录时，程序将已经初始化了所有必要的变量。


将来完成进行时 (Future Perfect Continuous):
By next week, the system will have been initializing for seven days straight. 到下周，系统将已经连续初始化七天了。
The program will have been initializing the data for hours before it completes the process. 在程序完成该过程之前，它将已经初始化数据数小时。



PHRASE->
The system was initialized with default settings. 系统使用默认设置初始化。
The program initialized the variables to zero. 程序将变量初始化为零。
The database was initialized before the application started.  数据库在应用程序启动之前初始化。
After the system is initialized, it will be ready to use. 系统初始化后即可使用。
The initialization process completed successfully. 初始化过程成功完成。


