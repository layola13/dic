```json
{
  "word": "nonlocals",
  "phonetics": "/nɒnˈləʊkəlz/",
  "part_of_speech": "noun",
  "translation": "非本地人, 外地人, 非局部变量(python)",
  "definition": "People who are not from the local area or region; in Python programming, variables that are neither local nor global.",
  "example": "The influx of nonlocals has changed the character of the town. In Python, `nonlocal` is used to refer to variables in an enclosing scope.",
  "synonyms": ["outsiders", "foreigners", "strangers", "out-of-towners"],
  "antonyms": ["locals", "residents", "natives"],
  "usage": {
    "general_use": [
      { "example": "Many locals resent the arrival of nonlocals who drive up housing prices.", "translation": "许多当地人对抬高房价的非本地人的到来感到不满。" },
      { "example": "The restaurant caters to both locals and nonlocals.", "translation": "这家餐厅既迎合当地人也迎合外地人。" }
    ],
    "python_programming": [
      { "code": "def outer_function():\n  message = 'Hello'\n  def inner_function():\n    nonlocal message\n    message = 'Goodbye'\n  inner_function()\n  return message\n\nprint(outer_function())", "explanation": "This code demonstrates the use of `nonlocal` in Python. The `inner_function` can modify the `message` variable in the `outer_function`'s scope.", "translation": "此代码演示了Python中`nonlocal`的用法。`inner_function`可以修改`outer_function`作用域中的`message`变量。" },
      { "code": "def example_function():\n    x = 5\n    def inner():\n        nonlocal x\n        x = 10\n    inner()\n    return x\n\nprint(example_function()) # Output: 10", "explanation": "The `nonlocal` keyword allows the inner function to modify the variable `x` declared in the enclosing function's scope.  Without nonlocal, `inner` would create its own local variable named `x`.", "translation": "`nonlocal`关键字允许内部函数修改在封闭函数的作用域中声明的变量`x`。 如果没有 nonlocal，`inner` 将创建自己的名为 `x` 的局部变量。" }
    ],
    "contrast_local_global": [
      { "example": "Understanding the difference between local, nonlocal, and global variables is crucial in programming.", "translation": "理解局部变量、非局部变量和全局变量之间的区别在编程中至关重要。" },
      { "example": "A variable declared inside a function is local; one declared outside any function is global; a variable accessed from within a nested function but not defined in its scope is nonlocal if specified as such.", "translation": "在函数内部声明的变量是局部变量；在任何函数之外声明的变量是全局变量；如果从嵌套函数内部访问但未在其作用域中定义的变量如果指定为 nonlocal，则为非局部变量。" }
    ]
  },
  "phrases": {
    "examples": [
      { "phrase": "influx of nonlocals", "translation": "非本地人的涌入" },
      { "phrase": "nonlocal keyword", "translation": "非本地关键字(python)" }
    ],
    "idioms": []
  }
}
```