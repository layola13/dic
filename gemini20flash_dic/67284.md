```json
{
  "word": "exscript",
  "phonetics": "ɪkˈskrɪpt",
  "part_of_speech": "noun",
  "translation": "退出脚本",
  "definition": "A command or function that exits or terminates a script's execution. This is often used to stop a program when a certain condition is met or when the program has completed its tasks.",
  "example": "The program uses `exscript` to terminate when the data processing is complete.",
  "synonyms": ["exit", "quit", "terminate"],
  "antonyms": ["continue", "proceed"],
  "usage": {
    "general_use": [
      {
        "example": "The script includes an `exscript` command to halt execution if an error is detected.",
        "translation": "如果检测到错误，该脚本包含一个 `exscript` 命令来停止执行。"
      },
      {
        "example": "Use `exscript` to gracefully exit the program after all tasks are finished.",
        "translation": "在所有任务完成后，使用 `exscript` 优雅地退出程序。"
      }
    ],
    "conditional_statements": [
      {
        "example": "If the file is not found, `exscript` will be called to prevent further errors.",
        "translation": "如果未找到文件，将调用 `exscript` 以防止进一步的错误。"
      },
      {
        "example": "The loop terminates and calls `exscript` when the counter reaches the maximum value.",
        "translation": "当计数器达到最大值时，循环终止并调用 `exscript`。"
      }
    ],
    "error_handling": [
      {
        "example": "In the error handling routine, `exscript` is used to stop the program and log the error.",
        "translation": "在错误处理程序中，`exscript` 用于停止程序并记录错误。"
      },
      {
        "example": "When a critical error occurs, the system calls `exscript` to prevent data corruption.",
        "translation": "当发生严重错误时，系统调用 `exscript` 以防止数据损坏。"
      }
    ],
    "function_calls": [
      {
        "example": "The `exscript` function takes an optional exit code as an argument.",
        "translation": "`exscript` 函数接受一个可选的退出代码作为参数。"
      },
      {
        "example": "Calling `exscript()` without arguments will terminate the script with a default exit code.",
        "translation": "不带参数调用 `exscript()` 将以默认退出代码终止脚本。"
      }
    ],
    "script_termination": [
      {
        "example": "The final line of the script includes `exscript` to ensure the program ends correctly.",
        "translation": "脚本的最后一行包括 `exscript`，以确保程序正确结束。"
      },
      {
        "example": "`exscript` is crucial for ending background processes initiated by the script.",
        "translation": "`exscript` 对于结束脚本启动的后台进程至关重要。"
      }
    ]
  },
  "phrases": {
    "examples": [
      {
        "phrase": "call exscript",
        "translation": "调用退出脚本"
      },
      {
        "phrase": "error exscript",
        "translation": "错误退出脚本"
      }
    ],
    "idioms": []
  }
}
``` 