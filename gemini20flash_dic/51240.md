```json
{
  "word": "destructuring",
  "phonetics": "/ˌdiːˈstrʌktʃərɪŋ/",
  "part_of_speech": "n. & v.",
  "translation": "解构，析构",
  "definition": "The extraction of individual elements from a data structure such as an array or object and assigning them to variables.",
  "example": "Destructuring assignment allows you to unpack values from arrays, or properties from objects, into distinct variables.",
  "synonyms": ["unpacking", "decomposition"],
  "antonyms": ["construction", "composition"],
  "usage": {
    "javascript": [
      {
        "example": "const [a, b] = [1, 2]; // Array destructuring",
        "translation": "const [a, b] = [1, 2]; // 数组解构"
      },
      {
        "example": "const { name, age } = { name: 'Alice', age: 30 }; // Object destructuring",
        "translation": "const { name, age } = { name: 'Alice', age: 30 }; // 对象解构"
      },
      {
        "example": "function getCoordinates() { return { x: 10, y: 20 }; }\nconst { x, y } = getCoordinates();",
        "translation": "function getCoordinates() { return { x: 10, y: 20 }; }\nconst { x, y } = getCoordinates(); // 从函数返回的对象中解构"
      },
      {
        "example": "const person = { firstName: 'John', lastName: 'Doe' };\nconst { firstName: fName, lastName: lName } = person;",
        "translation": "const person = { firstName: 'John', lastName: 'Doe' };\nconst { firstName: fName, lastName: lName } = person; // 为解构的变量指定不同的名称"
      },
      {
        "example": "const array = [1, 2, 3, 4, 5];\nconst [first, second, ...rest] = array;",
        "translation": "const array = [1, 2, 3, 4, 5];\nconst [first, second, ...rest] = array; // 使用剩余参数获取剩余元素"
      },
        {
            "example": "function displayInfo({name, age}){console.log(name,age)};  displayInfo({name: 'Bob', age: 25})",
            "translation": "function displayInfo({name, age}){console.log(name,age)};  displayInfo({name: 'Bob', age: 25}); // 将解构作为参数传入函数"
        }
    ],
    "python": [
      {
        "example": "a, b = (1, 2) # Tuple unpacking",
        "translation": "a, b = (1, 2) # 元组解包"
      },
      {
        "example": "a, b = [1, 2] # List unpacking",
        "translation": "a, b = [1, 2] # 列表解包"
      },
      {
        "example": "x, y, z = 'abc' # String unpacking",
        "translation": "x, y, z = 'abc' # 字符串解包"
      },
      {
        "example": "first, *rest = [1, 2, 3, 4, 5] # Using * to capture remaining elements",
        "translation": "first, *rest = [1, 2, 3, 4, 5] # 使用 * 捕获剩余元素"
      },
      {
        "example": "a, b, c = {'a': 1, 'b': 2, 'c':3}.values()",
        "translation": "a, b, c = {'a': 1, 'b': 2, 'c':3}.values() # 使用字典的值解包"
      }
    ]
  },
  "phrases": {
    "examples": [
      { "phrase": "destructuring assignment", "translation": "解构赋值" },
      { "phrase": "object destructuring", "translation": "对象解构" },
      { "phrase": "array destructuring", "translation": "数组解构" }
    ],
    "idioms": []
  }
}
```