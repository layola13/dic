```json
{
  "word": "callbacks",
  "phonetics": "/ˈkɔːlˌbæks/",
  "part_of_speech": "noun",
  "translation": "回调",
  "definition": "Functions passed as arguments to other functions, to be executed upon the completion of a specific action or event.",
  "example": "The function uses callbacks to handle asynchronous operations.",
  "synonyms": ["event handlers", "hooks", "delegates"],
  "antonyms": [],
  "usage": {
    "javascript": [
      {
        "example": "```javascript\nfunction fetchData(url, callback) {\n  setTimeout(() => {\n    const data = { result: 'Data fetched successfully!' };\n    callback(data);\n  }, 1000);\n}\n\nfunction processData(data) {\n  console.log('Data:', data.result);\n}\n\nfetchData('https://example.com/api', processData);\n```",
        "translation": "此示例展示了如何在JavaScript中使用回调函数处理异步数据获取。`fetchData` 函数模拟从URL获取数据，并在1秒后通过调用回调函数 `processData` 返回数据。"
      }
    ],
    "python": [
      {
        "example": "```python\nimport time\n\ndef fetch_data(url, callback):\n    time.sleep(1)  # Simulate network delay\n    data = {'result': 'Data fetched successfully!'}\n    callback(data)\n\n\ndef process_data(data):\n    print('Data:', data['result'])\n\n\nfetch_data('https://example.com/api', process_data)\n```",
        "translation": "此示例演示了如何在Python中使用回调函数处理异步操作。`fetch_data` 函数模拟从URL获取数据，并在1秒后通过调用回调函数 `process_data` 返回数据。"
      }
    ],
    "node.js": [
      {
        "example": "```javascript\nconst fs = require('fs');\n\nfs.readFile('/path/to/file', 'utf8', (err, data) => {\n  if (err) {\n    console.error('Error reading file:', err);\n    return;\n  }\n  console.log('File content:', data);\n});\n```",
        "translation": "此示例展示了如何在Node.js中使用回调函数读取文件。`fs.readFile` 函数异步读取文件内容，并在读取完成后调用回调函数。回调函数接收错误对象（如果发生错误）和文件数据。"
      }
    ],
    "event_listeners": [
      {
        "example": "```javascript\ndocument.getElementById('myButton').addEventListener('click', function() {\n  alert('Button clicked!');\n});\n```",
        "translation": "此示例演示了如何使用回调函数作为事件监听器。当点击ID为 `myButton` 的元素时，会触发回调函数，显示一个警告框。"
      }
    ],
    "asynchronous_operations": [
      {
        "example": "Callbacks are essential for handling asynchronous operations, ensuring that code executes in the correct order without blocking the main thread.",
        "translation": "回调对于处理异步操作至关重要，确保代码以正确的顺序执行，而不会阻塞主线程。"
      }
    ]
  },
  "phrases": {
    "examples": [
      {
        "phrase": "callback function",
        "translation": "回调函数"
      },
      {
        "phrase": "callback hell",
        "translation": "回调地狱 (指嵌套过多的回调函数，导致代码难以阅读和维护)"
      }
    ],
    "idioms": []
  }
}
``` 