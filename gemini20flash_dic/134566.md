```json
{
  "word": "nonlocal",
  "phonetics": "/ˌnɒnˈləʊkəl/",
  "part_of_speech": "keyword",
  "translation": "非局部变量",
  "definition": "In Python, nonlocal is a keyword used to declare that a variable inside a nested function (a function within a function) is not local to that function but belongs to the enclosing scope. It's used when you want to modify a variable in the nearest enclosing scope that is not global.",
  "example": "def outer_function():\n    x = 10\n    def inner_function():\n        nonlocal x\n        x = 20\n        print(\"Inner:\", x)  # Prints: Inner: 20\n    inner_function()\n    print(\"Outer:\", x)  # Prints: Outer: 20",
  "synonyms": [],
  "antonyms": [],
  "usage": {
    "basic_usage": [
      {
        "example": "def outer():\n    x = \"hello\"\n    def inner():\n        nonlocal x\n        x = \"world\"\n    inner()\n    print(x) # Output: world\nouter()",
        "translation": "定义一个外部函数，其中包含一个变量 x。内部函数使用 nonlocal 关键字来修改外部函数中定义的 x 变量。"
      }
    ],
    "modifying_variables": [
      {
        "example": "def counter():\n    count = 0\n    def increment():\n        nonlocal count\n        count += 1\n        return count\n    return increment\n\nc = counter()\nprint(c()) # Output: 1\nprint(c()) # Output: 2",
        "translation": "闭包中使用 nonlocal 修改计数器变量。"
      }
    ],
    "nested_functions": [
      {
        "example": "def outer_func():\n    message = \"Hello\"\n    def middle_func():\n        nonlocal message\n        message = \"Hi\"\n        def inner_func():\n            nonlocal message\n            message = \"Hey\"\n        inner_func()\n    middle_func()\n    return message\n\nprint(outer_func()) # Output: Hey",
        "translation": "在多层嵌套函数中使用 nonlocal。"
      }
    ],
    "scope": [
      {
        "example": "x = 50\n\ndef foo():\n    x = 100\n    def bar():\n        nonlocal x\n        x = 200\n        print(x)\n    bar()\n\nfoo()\n# The program prints 200",
        "translation": "展示 nonlocal 如何访问最近的非全局作用域。"
      }
    ],
    "error_handling": [
      {
        "example": "def outer():\n    def inner():\n        nonlocal x  # Raises SyntaxError: no binding for nonlocal 'x' found\n        x = 10\n    inner()\nouter()",
        "translation": "如果没有在闭包中定义变量，使用 nonlocal 会引发语法错误。"
      }
    ]
  },
  "phrases": {
    "examples": [],
    "idioms": []
  }
}
``` 