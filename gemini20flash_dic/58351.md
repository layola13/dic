```json
{
  "word": "dto",
  "phonetics": "/ˈdiː.tiː.oʊ/",
  "part_of_speech": "noun",
  "translation": "数据传输对象",
  "definition": "A Data Transfer Object (DTO) is an object that carries data between processes. DTOs are often used to reduce the number of calls to remote interfaces.",
  "example": "The service layer returns a DTO to the presentation layer.",
  "synonyms": ["value object", "transfer object"],
  "antonyms": [],
  "usage": {
    "general_usage": [
      {
        "example": "Using DTOs helps to decouple the data access layer from the service layer.",
        "translation": "使用 DTO 有助于将数据访问层与服务层分离。"
      },
      {
        "example": "The DTO pattern is useful in distributed systems.",
        "translation": "DTO 模式在分布式系统中很有用。"
      }
    ],
    "java_examples": [
      {
        "code": "public class UserDTO {\n    private String username;\n    private String email;\n\n    public UserDTO(String username, String email) {\n        this.username = username;\n        this.email = email;\n    }\n\n    public String getUsername() {\n        return username;\n    }\n\n    public String getEmail() {\n        return email;\n    }\n}",
        "translation": "用户 DTO 的 Java 示例。\n这个类包含用户名和电子邮件属性。\n构造函数用于初始化这些属性。\ngetter 方法用于访问这些属性。"
      },
      {
        "code": "// Usage\nUserDTO user = new UserDTO(\"john.doe\", \"john.doe@example.com\");\nString username = user.getUsername(); // john.doe",
        "translation": "// 用法\nUserDTO user = new UserDTO(\"john.doe\", \"john.doe@example.com\");\nString username = user.getUsername(); // john.doe"
      }
    ],
    "python_examples": [
      {
        "code": "class UserDTO:\n    def __init__(self, username, email):\n        self.username = username\n        self.email = email\n\n    def get_username(self):\n        return self.username\n\n    def get_email(self):\n        return self.email",
        "translation": "Python 中的用户 DTO 示例。\n此类包含用户名和电子邮件属性。\n构造函数用于初始化这些属性。\ngetter 方法用于访问这些属性。"
      },
      {
        "code": "# Usage\nuser = UserDTO(\"john.doe\", \"john.doe@example.com\")\nusername = user.get_username() # john.doe",
        "translation": "# 用法\nuser = UserDTO(\"john.doe\", \"john.doe@example.com\")\nusername = user.get_username() # john.doe"
      }
    ]
  },
  "phrases": {
    "examples": [
      {
        "phrase": "DTO pattern",
        "translation": "DTO 模式"
      },
      {
        "phrase": "Data Transfer Object",
        "translation": "数据传输对象"
      }
    ],
    "idioms": []
  }
}
```
 