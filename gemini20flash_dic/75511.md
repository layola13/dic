```json
{
  "word": "fstream",
  "phonetics": null,
  "part_of_speech": "n.",
  "translation": "文件流",
  "definition": "In C++, fstream is a header file that defines several classes for performing file input and output operations. The name `fstream` is a combination of `file` and `stream` and provides classes to work with files as streams of data.",
  "example": "To read from or write to a file in C++, you need to include the `<fstream>` header and use classes like `ifstream` (input file stream), `ofstream` (output file stream), or `fstream` (file stream, for both input and output).",
  "synonyms": ["file stream"],
  "antonyms": [],
  "usage": {
    "basic_operations": [
      {
        "example": "#include <fstream>\n\nint main() {\n  std::ofstream outfile(\"example.txt\");\n  if (outfile.is_open()) {\n    outfile << \"This is a line of text.\\n\";\n    outfile.close();\n  }\n  return 0;\n}",
        "translation": "这是一个使用`ofstream`向文件写入的例子。首先包含`<fstream>`头文件，然后创建一个`ofstream`对象并将其与文件关联。如果文件成功打开，则可以使用`<<`运算符将数据写入文件，最后关闭文件。"
      },
      {
        "example": "#include <fstream>\n#include <iostream>\n#include <string>\n\nint main() {\n  std::ifstream infile(\"example.txt\");\n  std::string line;\n  if (infile.is_open()) {\n    while (std::getline(infile, line)) {\n      std::cout << line << '\\n';\n    }\n    infile.close();\n  }\n  return 0;\n}",
        "translation": "这是一个使用`ifstream`从文件读取的例子。首先包含`<fstream>`、`<iostream>`和`<string>`头文件。创建一个`ifstream`对象并将其与文件关联。使用`std::getline`逐行读取文件内容，然后将每一行打印到控制台。"
      }
    ],
    "ifstream": [
      {
        "example": "std::ifstream myFile(\"data.txt\"); // Creates an input file stream object",
        "translation": "创建用于读取文件 `data.txt` 的输入文件流对象。"
      },
      {
        "example": "if (myFile.is_open()) { /* ... */ } // Checks if the file is successfully opened",
        "translation": "检查文件是否成功打开。"
      }
    ],
    "ofstream": [
      {
        "example": "std::ofstream myFile(\"output.txt\"); // Creates an output file stream object",
        "translation": "创建用于写入文件 `output.txt` 的输出文件流对象。"
      },
      {
        "example": "myFile << \"Writing to a file.\\n\"; // Writes data to the file",
        "translation": "向文件写入数据。"
      }
    ],
    "fstream_both_io": [
        {
        "example": "std::fstream myFile(\"data.txt\", std::ios::in | std::ios::out); // Creates a file stream object for both reading and writing",
        "translation": "创建既能读取又能写入文件的流对象。"
      },
      {
        "example": "myFile.seekg(0); // Sets the read position to the beginning of the file",
        "translation": "设置读取位置到文件开头。"
      }
    ],
     "error_handling": [
        {
        "example": "if (!myFile.is_open()) { std::cerr << \"Unable to open file\"; return 1; }",
        "translation": "如果文件打开失败，则输出错误信息并退出程序。"
      }

    ]

  },
  "phrases": {
    "examples": [
      { "phrase": "File I/O", "translation": "文件输入/输出" },
      { "phrase": "Stream manipulation", "translation": "流操作" }
    ],
    "idioms": []
  }
}
```