```json
{
  "word": "numpy",
  "phonetics": null,
  "part_of_speech": "noun",
  "translation": "NumPy（Numerical Python）是一个开源的Python科学计算库",
  "definition": "NumPy is the fundamental package for numerical computation in Python. It provides support for large, multi-dimensional arrays and matrices, along with a large collection of high-level mathematical functions to operate on these arrays.",
  "example": "import numpy as np\narr = np.array([1, 2, 3, 4, 5])",
  "synonyms": [
    "numerical python",
    "array manipulation library",
    "scientific computing library"
  ],
  "antonyms": [],
  "usage": {
    "array_creation": [
      {
        "example": "arr = numpy.array([1, 2, 3])",
        "translation": "创建一个NumPy数组。",
        "code": "import numpy\narr = numpy.array([1, 2, 3])"
      },
      {
        "example": "arr = numpy.zeros((2, 3))",
        "translation": "创建一个2x3的零矩阵。",
        "code": "import numpy\narr = numpy.zeros((2, 3))"
      }
    ],
    "array_indexing": [
      {
        "example": "element = arr[0, 1]",
        "translation": "访问数组中第一行第二列的元素。",
        "code": "import numpy\narr = numpy.array([[1, 2, 3], [4, 5, 6]])\nelement = arr[0, 1]"
      },
      {
        "example": "subset = arr[0:2, 1:3]",
        "translation": "获取数组中第一行到第二行、第二列到第三列的子集。",
        "code": "import numpy\narr = numpy.array([[1, 2, 3], [4, 5, 6]])\nsubset = arr[0:2, 1:3]"
      }
    ],
    "mathematical_operations": [
      {
        "example": "result = numpy.sin(arr)",
        "translation": "计算数组中每个元素的正弦值。",
        "code": "import numpy\narr = numpy.array([0, 1, 2])\nresult = numpy.sin(arr)"
      },
      {
        "example": "result = numpy.mean(arr)",
        "translation": "计算数组的平均值。",
        "code": "import numpy\narr = numpy.array([1, 2, 3, 4, 5])\nresult = numpy.mean(arr)"
      }
    ],
    "linear_algebra": [
      {
        "example": "result = numpy.linalg.dot(matrix_a, matrix_b)",
        "translation": "计算两个矩阵的点积。",
        "code": "import numpy\nmatrix_a = numpy.array([[1, 2], [3, 4]])\nmatrix_b = numpy.array([[5, 6], [7, 8]])\nresult = numpy.linalg.dot(matrix_a, matrix_b)"
      },
      {
        "example": "eigenvalues, eigenvectors = numpy.linalg.eig(matrix)",
        "translation": "计算矩阵的特征值和特征向量。",
        "code": "import numpy\nmatrix = numpy.array([[1, 2], [3, 4]])\neigenvalues, eigenvectors = numpy.linalg.eig(matrix)"
      }
    ],
    "random_number_generation": [
      {
        "example": "random_numbers = numpy.random.rand(5)",
        "translation": "生成一个包含5个随机数的数组（0到1之间均匀分布）。",
        "code": "import numpy\nrandom_numbers = numpy.random.rand(5)"
      },
      {
        "example": "random_integers = numpy.random.randint(1, 10, 5)",
        "translation": "生成一个包含5个1到9之间随机整数的数组。",
        "code": "import numpy\nrandom_integers = numpy.random.randint(1, 10, 5)"
      }
    ],
    "data_analysis": [
      {
        "example": "max_value = numpy.max(data)",
        "translation": "求数据的最大值",
        "code": "import numpy\ndata= numpy.array([1,3,5,7])\nmax_value = numpy.max(data)"
      },
      {
        "example": "min_value = numpy.min(data)",
        "translation": "求数据的最小值",
        "code": "import numpy\ndata= numpy.array([1,3,5,7])\nmin_value = numpy.min(data)"
      }
    ]
  },
  "phrases": {
    "examples": [
      {
        "phrase": "NumPy array",
        "translation": "NumPy 数组"
      },
      {
        "phrase": "NumPy functions",
        "translation": "NumPy 函数"
      }
    ],
    "idioms": []
  }
}
``` 