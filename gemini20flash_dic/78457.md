```json
{
  "word": "getters",
  "phonetics": "/ˈɡɛtərz/",
  "part_of_speech": "noun (plural)",
  "translation": "获取器 (getter 的复数形式)",
  "definition": "In programming, a getter is a method that retrieves the value of a particular property of an object. Getters are often used to implement encapsulation and control access to an object's internal state.",
  "example": "In object-oriented programming, getters and setters are used to access and modify private variables.",
  "synonyms": [
    "accessor methods",
    "retrievers"
  ],
  "antonyms": [
    "setters",
    "mutators"
  ],
  "usage": {
    "general_usage": [
      {
        "example": "The `getName()` method is a getter that returns the name of the user.",
        "translation": "`getName()` 方法是一个 getter，返回用户的名字。"
      },
      {
        "example": "Using getters allows us to control how the data is accessed and potentially perform validation or transformations.",
        "translation": "使用 getter 允许我们控制数据的访问方式，并可能执行验证或转换。"
      }
    ],
    "code_examples": [
      {
        "language": "Java",
        "code": "public class Person {\n  private String name;\n\n  public String getName() {\n    return name;\n  }\n}",
        "explanation": "This Java code defines a class 'Person' with a private variable 'name'. The 'getName()' method is a getter that returns the value of 'name'."
      },
      {
        "language": "JavaScript",
        "code": "class Person {\n  constructor(name) {\n    this._name = name;\n  }\n\n  get name() {\n    return this._name;\n  }\n}",
        "explanation": "This JavaScript code defines a class 'Person' with a private property '_name'. The 'get name()' method is a getter that returns the value of '_name'."
      },
      {
        "language": "Python",
        "code": "class Person:\n    def __init__(self, name):\n        self._name = name\n\n    @property\n    def name(self):\n        return self._name",
        "explanation": "This Python code defines a class 'Person' with a private attribute '_name'. The '@property' decorator makes the 'name' method act as a getter, returning the value of '_name'."
      }
    ],
    "best_practices": [
      {
        "recommendation": "Use getters to access private variables instead of accessing them directly.",
        "reason": "This helps to encapsulate the object's internal state and allows for future modifications without breaking the code that uses the object."
      },
      {
        "recommendation": "Consider using getters to perform validation or transformations on the data before returning it.",
        "reason": "This ensures that the data is in a consistent and expected format."
      }
    ]
  },
  "phrases": {
    "examples": [
      {
        "phrase": "Getter and setter methods",
        "translation": "Getter 和 setter 方法"
      },
      {
        "phrase": "Using a getter to access a property",
        "translation": "使用 getter 来访问属性"
      }
    ],
    "idioms": []
  }
}
```