```json
{
  "word": "lld",
  "phonetics": "/ˌel.elˈdiː/",
  "part_of_speech": "noun",
  "translation": "链接器",
  "definition": "lld is the LLVM linker, a replacement for system linkers, designed to be faster than them.",
  "example": "lld is used as a faster alternative to traditional linkers in the LLVM toolchain.",
  "synonyms": ["linker"],
  "antonyms": [],
  "usage": {
    "general_usage": [
      {
        "example": "Developers use lld to link object files into executables.",
        "translation": "开发者使用 lld 将目标文件链接成可执行文件。"
      },
      {
        "example": "lld is often integrated into build systems like CMake.",
        "translation": "lld 经常被集成到像 CMake 这样的构建系统中。"
      }
    ],
    "command_line_usage": [
      {
        "example": "The command `lld main.o -o main` links `main.o` to create an executable `main`.",
        "translation": "命令 `lld main.o -o main` 链接 `main.o` 以创建一个可执行文件 `main`。"
      },
      {
        "example": "You can specify libraries to link with using the `-l` flag, like `lld main.o -lstdc++ -o main`.",
        "translation": "您可以使用 `-l` 标志指定要链接的库，例如 `lld main.o -lstdc++ -o main`。"
      }
    ],
    "build_system_integration": [
      {
        "example": "In CMake, you can set the linker to lld using `set(CMAKE_EXE_LINKER_FLAGS \"-fuse-ld=lld\")`.",
        "translation": "在 CMake 中，您可以使用 `set(CMAKE_EXE_LINKER_FLAGS \"-fuse-ld=lld\")` 将链接器设置为 lld。"
      },
      {
        "example": "Many large projects use lld to reduce link times during the build process.",
        "translation": "许多大型项目使用 lld 来减少构建过程中的链接时间。"
      }
    ],
    "error_handling": [
      {
        "example": "lld provides informative error messages when linking fails due to missing symbols.",
        "translation": "当由于缺少符号而导致链接失败时，lld 提供信息丰富的错误消息。"
      },
      {
        "example": "Debugging with lld involves understanding its diagnostics for undefined references.",
        "translation": "使用 lld 进行调试涉及理解其对未定义引用的诊断。"
      }
    ],
    "optimization": [
      {
        "example": "lld performs optimizations such as dead code stripping to reduce the size of the final executable.",
        "translation": "lld 执行诸如死代码删除之类的优化，以减小最终可执行文件的大小。"
      },
      {
        "example": "Using lld can result in faster link times and smaller binaries compared to traditional linkers.",
        "translation": "与传统的链接器相比，使用 lld 可以带来更快的链接时间和更小的二进制文件。"
      }
    ]
  },
  "phrases": {
    "examples": [
      {
        "phrase": "Using lld",
        "translation": "使用 lld"
      },
      {
        "phrase": "lld linker",
        "translation": "lld 链接器"
      }
    ],
    "idioms": []
  }
}
``` 