```json
{
  "word": "destructuring",
  "phonetics": "/ˌdiːˈstrʌktʃərɪŋ/",
  "part_of_speech": "noun",
  "translation": "解构",
  "definition": "Destructuring is a convenient way of extracting multiple values from data stored in objects and arrays.",
  "example": "Destructuring can simplify the way you extract data from objects and arrays in JavaScript.",
  "synonyms": ["decomposition", "extraction"],
  "antonyms": ["construction", "composition"],
  "usage": {
    "object_destructuring": [
      {
        "example": "const { name, age } = person; // Extracts name and age properties from the person object",
        "translation": "const { name, age } = person; // 从 person 对象中提取 name 和 age 属性"
      },
      {
        "example": "const { street: myStreet } = address; // Extracts the street property and assigns it to the myStreet variable",
        "translation": "const { street: myStreet } = address; // 提取 street 属性并将其分配给 myStreet 变量"
      }
    ],
    "array_destructuring": [
      {
        "example": "const [first, second] = numbers; // Assigns the first and second elements of the numbers array to first and second variables",
        "translation": "const [first, second] = numbers; // 将 numbers 数组的第一个和第二个元素分配给 first 和 second 变量"
      },
      {
        "example": "const [head, ...tail] = list; // Extracts the first element as head and the rest of the array as tail",
        "translation": "const [head, ...tail] = list; // 将第一个元素提取为 head，数组的其余部分提取为 tail"
      }
    ],
    "function_parameters": [
      {
        "example": "function displayPerson({ name, age }) { console.log(name, age); } // Destructures the object passed as an argument",
        "translation": "function displayPerson({ name, age }) { console.log(name, age); } // 解构作为参数传递的对象"
      },
      {
        "example": "function printFirst([first]) { console.log(first); } // Destructures the array passed as an argument to get the first element",
        "translation": "function printFirst([first]) { console.log(first); } // 解构作为参数传递的数组以获取第一个元素"
      }
    ],
    "nested_destructuring": [
      {
        "example": "const { address: { street, city } } = person; // Extracts street and city from a nested address object within person",
        "translation": "const { address: { street, city } } = person; // 从 person 中嵌套的 address 对象中提取 street 和 city"
      }
    ],
    "default_values": [
      {
        "example": "const { width = 100, height = 200 } = options; // Assigns default values if width or height are not provided in the options object",
        "translation": "const { width = 100, height = 200 } = options; // 如果 options 对象中未提供 width 或 height，则分配默认值"
      }
    ],
    "ignoring_values": [
      {
        "example": "const [first, , third] = data; // Ignores the second element in the data array",
        "translation": "const [first, , third] = data; // 忽略 data 数组中的第二个元素"
      }
    ],
    "computed_property_names": [
      {
        "example": "const { [key]: value } = obj; // key is a variable that holds the name of the property to be extracted",
        "translation": "const { [key]: value } = obj; // key 是一个变量，它保存要提取的属性的名称"
      }
    ]
  },
  "phrases": {
    "examples": [
      {
        "phrase": "destructuring assignment",
        "translation": "解构赋值"
      },
      {
        "phrase": "object destructuring",
        "translation": "对象解构"
      }
    ],
    "related_terms": [
      {
        "term": "ES6",
        "definition": "ECMAScript 2015 (ES6) introduced destructuring to JavaScript"
      },
      {
        "term": "spread operator",
        "definition": "Often used with destructuring, the spread operator (...) allows you to expand an iterable into individual elements"
      }
    ]
  }
}
``` 