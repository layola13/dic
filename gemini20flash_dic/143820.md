```json
{
  "word": "outpath",
  "phonetics": null,
  "part_of_speech": "noun",
  "translation": "输出路径",
  "definition": "The path or directory where the output files or results of a program or process are stored.",
  "example": "The program allows you to specify the `outpath` where the processed images will be saved.",
  "synonyms": ["output directory", "destination path"],
  "antonyms": ["input path", "source path"],
  "usage": {
    "general": [
      {
        "example": "Please set the `outpath` to a valid directory.",
        "translation": "请将 `outpath` 设置为有效的目录。"
      },
      {
        "example": "The script saves the report to the defined `outpath`.",
        "translation": "该脚本将报告保存到定义的 `outpath`。"
      },
       {
        "example": "The software uses the `outpath` to organize output files.",
        "translation": "该软件使用 `outpath` 来组织输出文件。"
      }
    ],
     "command_line_usage": [
      {
        "example": "The command `process_data --outpath /path/to/output` specifies where the processed data should be placed.",
        "translation": "命令 `process_data --outpath /path/to/output` 指定了应该放置处理后的数据的位置。"
      },
      {
        "example": "Use the `--outpath` flag to define where the results should be written.",
        "translation": "使用 `--outpath` 标志来定义结果应写入的位置。"
      }
    ],
    "programming_context": [
      {
        "example": "In the configuration file, the `outpath` is a crucial setting.",
        "translation": "在配置文件中, `outpath` 是一个至关重要的设置。"
      },
      {
        "example": "The function takes `outpath` as an argument to determine where to write the output.",
        "translation": "该函数将 `outpath` 作为参数，以确定将输出写入的位置。"
      },
       {
        "example": "The `outpath` variable is dynamically set based on the user's choice.",
        "translation": "`outpath` 变量根据用户的选择动态设置。"
      }

    ]
  },
   "phrases": {
    "examples": [
      { "phrase": "Set the outpath", "translation": "设置输出路径" },
      { "phrase": "Verify the outpath", "translation": "验证输出路径" }
    ],
    "idioms": []
  }
}
```